# dev/student_ios/

学生 iOS 端的父目录 — iOS 専属设计文档（`IOS_DESIGN_LOG.md`）+ Swift 实装本体（`v1/`）。

早期的 HTML 原型（视觉参考产物）已归档到本地 archive，不在公开仓库；Swift 实装以设计文档为准。

---

## 文件一览

| 文件 / 目录 | 作用 |
|---|---|
| `IOS_DESIGN_LOG.md` | iOS 専属设计决定的完整档案（注册流程、学号 6 位、房间号、改动履历等）|
| `v1/` | Swift 实装本体（TomoshibiApp Xcode 工程 + Foundation + Features + TASKS）— 构建方式见 `v1/README.md` + `v1/BUILD.md` |

---

## 相关文档（外部）

- 共用功能矩阵（长期单源真值）：`../../design/system_features.md`

---

## 履历

- 2026-04-22：以 `dev/demo_4-28/Student_iOS_new/` 建立（demo 冲刺命名）
- 2026-04-23：改名 `dev/student_ios/`（去掉冲刺日期，作为 v1.0 前的正式位置）
- 早期 Swift 实装曾放在独立仓库 `otogi2025/Tomoshibi-iOS`，2026-05-06 退役，代码统一并入本目录 `v1/`
