# Tomoshibi Backend v1.0 — 依存 (依赖)
# 与 demo/requirements.txt 分离 (demo は触らない方針)

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy>=2.0.30
pydantic[email]>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0
# C-039 (2026-05-21): python-jose >= 3.4.0 (修复 CVE-2024-33663 algorithm confusion + CVE-2024-33664 JWT bomb DoS)
python-jose[cryptography]>=3.4.0   # JWT
# 点呼机接入（Device_Contract §2）：Ed25519 设备验签直接依赖 cryptography（此前经 python-jose[cryptography] 间接引入，改直接声明）
cryptography>=43.0.0
bcrypt>=4.2.0                       # 密码哈希 (passlib 不再使用 — bcrypt 4.x 互換性)
python-multipart>=0.0.12            # FastAPI form/file
python-dotenv>=1.0.1                # .env 读取

# 邮件 (D1 SendGrid)
sendgrid>=6.11.0

# 食堂 Excel 导出 (#7)
openpyxl>=3.1.5

# DB drivers (用 SQLite for dev, PostgreSQL for prod)
# sqlite 内蔵, psycopg は production deploy 時のみ
psycopg[binary]>=3.2.0   # PostgreSQL 16+ (v1 production)

# H-11：生产 WSGI server gunicorn 移进本文件统一锁版本，不再在 Dockerfile 内联裸 pip install
# （psycopg[binary] 上面已声明，Dockerfile 那条重复 install 一并删）。沿用本文件 >= 约定。
gunicorn>=23.0.0         # 生产用 uvicorn worker 进程管理

# APNs 苹果推送真实装（spec §7.13，push.py）
PyJWT>=2.9.0             # provider token 签名（ES256）— 与 python-jose 并存：jose 管登录 JWT，PyJWT 管 APNs
httpx>=0.27.0            # 推送 HTTP 客户端（测试依赖里已有，生产运行时也需要 → 显式声明）
h2>=4.1.0                # httpx 的 HTTP/2 支持（APNs 只接受 HTTP/2）

# migration
alembic>=1.13.0

# 限速（接口防暴力枚举）
slowapi>=0.1.9

# 测试
pytest>=9.1.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
