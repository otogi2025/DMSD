# DMSD iOS 学生端 — 2026-06-03 压缩点接手提示词

> 用途：会话压缩 / 新会话接手时，把本文件全文粘给 AI。自包含。

---

## 你是谁 / 项目背景

你是 Claude Code，在 itsuki（中国留学生，日本高三，编程零基础，目标筑波大学 AC 入试）的 DMSD 项目工作。
DMSD = 宿舍点呼数字化系统（产品名 Tomoshibi / 灯火），5 端联动：后端 FastAPI+PostgreSQL / iOS Swift+SwiftUI / Android Kotlin+Compose / 老师网页 / 点呼机 Raspberry Pi。
当前主线 = iOS 学生端「演示假数据 → 真后端」接线 + 删废弃功能 + 准备公开上架 App Store。

## 开局必读（按顺序）

1. `05_logs/ios接后端_进度与handoff.md` — iOS 接后端进度全量（含 codex 命令 §2、编译命令 §1）
2. `00_admin/TODO.md` §📱（iOS 上架三阶段 + 6-03 v1.0 功能范围）+ §🔧
3. `00_admin/项目心智模型.md` — 5 端骨架
4. `03_dev/student_ios/IOS_DESIGN_LOG.md` §14.x — iOS 设计记录（§14.13 低风险修复 / §14.14 删寮ウォール）

## 本轮（2026-06-03）已完成（commit，未 push）

- `5b36518` 低风险：3 申请表单（StayForm / FridgePurchaseForm / ItemPossessionForm）预填改从 `app.displayUser` 取 + 在线自习表单（StudyOnlineForm）日期固定 JST（日本时间）
- `43050a1` TODO 决策固化：App Store 公开上架路线 + v1.0 功能范围
- `4c14b3e` + `423a484` 删除寮ウォール（学生掲示板 = WallView，落实 4-29 拍板，五处删净 0 残留，编译双 scheme 过）
- `5971054` Codex 审出 2 中风险修复：① `ApplyFormDate.threeDaysLater` 改 JST 固定日历 `tokyoCalendar` ② 3 表单加 `.onChange(of: app.currentUser?.account)` 补填，防自动登录冷启动竞态

## 待办（itsuki 2026-06-03 拍板，建议按此顺序）

### 1. 【压缩后第一件】中风险修复再派 Codex 确认收敛
审 `5971054` 的 2 个中风险修复是否真收敛（threeDaysLater JST + 表单 onChange 补填）。多轮对抗复审到 Codex 自己说「收敛 / 0 blocker」。codex 命令格式见 handoff §2（`codex exec -s read-only -c model_reasoning_effort=xhigh ...`）。逐条核实真实代码再判断，不盲信 codex 推理。

### 2. 删除匿名建議（SuggestView）
itsuki 拍板删。跟删寮ウォール同流程（见 IOS_DESIGN_LOG §14.14）：先 grep 全工程定位所有 Suggest 引用，再删：`CommunityStubs.swift` 的 `SuggestView`（§16）+ `SeedModels.swift` 的 `SuggestItem` 类型 + `SEED.swift` 的 `suggestions` 数组 + `Route.swift` 的 `homeSuggest` case + `RootView.swift` 的映射 + 任何入口 + 设计文档标注（IOS_DESIGN_LOG 加段 + system_features §891 补「代码已删」）。依据：`system_features.md §891` 已写「匿名建議 投稿 🚫 砍」。删完编译双 scheme 验证 + 派 codex 审。

### 3. 月度扣分统计接后端（iosmypage-07/08/09，B 类大功能）
iOS MyPage 的月度统计 / 详情 / 总分现在读 SEED 假数据，接后端真数据。后端 discipline 已有 `/discipline/me/summary`（当月汇总），要补「按月明细」接口。后端 + iOS 联动。

### 4. 联系人改后端配置（home-08）
`HomeStubs.swift:592` 紧急联系人写死「寮監：田中先生（内線 101）」，改成从后端配置拉（老师能在后台改）。后端加配置接口 + iOS 拉取。

### 5. 中风险 1 彻底改法（启动等真实用户信息）
自动登录冷启动竞态的根治：`AuthStubs.swift` 的 `SplashView` 自动登录路径（2.2 秒后 `authToken != nil` 就进 home）改成等 `AppStore.loadMe()` 完成再进 home（现在 `AppStore.init()` 只 `Task { await loadMe() }` 异步、不挡 splash）。启动会慢零点几秒，但所有页面冷启动都拿真实用户、不再有假数据闪现。改完可去掉第 5971054 的 onChange 补填（彻底解决后那是冗余兜底，也可保留）。

## 其余 v1.0 待办（之前定，未做）

- **老师退回（差戻 returned）动作 v1.0 必做** — 后端 `_recompute_application_status` 现产不出 `returned`；要后端 + teacher_web 加「退回」决策按钮；iOS 已前向兼容（returned 可编辑已埋）。spec §7.2.4-5。
- **学習対象 `is_study_target` 后端字段** — 要做（之前跟晚自习 UI 决策缠一起）。
- **通知子系统完整**（IX-009 扩展）— 内容源 5 类：公告✅已接 / 审批结果 / 包裹 / 扣分迟到 / 签到提醒；送达：app 内列表 + push 推送；**通知设置页每类可单独关闭**（苹果审核合规必需，push 必须可拒绝）。push 要 APNs（苹果推送服务）证书 + 后端推送服务，是新大块。
- **iOS 上架三阶段**（TODO §📱）：① 本地做完功能 ② 后端一次性部署到线上（`api.tomoshibi.cc` 现跑 5-08 骨架版 40 接口 / 本地 61，本地 SQLite → 线上 PostgreSQL，踩 §🐛 段 3 个部署坑）③ 提交审核（填 `DEVELOPMENT_TEAM` 现空 / 开真机签名现 `CODE_SIGNING_ALLOWED:NO` / 补 Info.plist `NFCReaderUsageDescription` / 降 deploymentTarget 现 iOS 26 偏高 / Archive→Validate→Upload）。itsuki 拍板公开上架 + 工作流「后端本地做完再一次性部署」。开发者账号续费中、谷歌云服务器 itsuki 自付费在跑、5-08 卡 Validate 没提交=首次上架。

## 推迟 / 不进 v1.0

- IX-007 其它类申请（修繕 / 来訪 / 代理受取）Option B — 不进 v1.0（后端零实装，往后堆；Option A「生产不读假数据」已做 `457077f`）
- 5-30 审查后端 migration 批（改数据库约束有风险，多 PostgreSQL 专属、推迟）
- 5-30 审查剩余 iOS bug（多数接后端 / 上不上线决策）

## 工作规矩（itsuki 铁律）

1. iOS 改完必须 `xcodebuild` 真编译双 scheme（生产 `TomoshibiApp`/Debug + 演示 `TomoshibiAppDemo`/Demo），不信 SourceKit 单文件误报（跨文件符号会假报 `Cannot find ... in scope`，真编译能找到）。命令见 handoff §1，工程在 `03_dev/student_ios/v1/`。
2. 每阶段做完派 Codex 5.5 xhigh 只读审查，逐条核实真实代码再修，不盲信 codex 推理（codex 沙箱跑不了 iOS 真编译，只语法解析）。
3. 精确 `git add <文件>` commit，**绝不 `git add -A`** — 工作区有别会话遗留的未提交改动（`flow_design.md` / `README.md` / `项目心智模型.md` / `hardware_design.md` 等），别卷进你的 commit。不 push（除非 itsuki 明示）。commit 不写 `Co-Authored-By`。
4. 代码注释 + 内部文档 100% 中文，UI 字符串保持日语（注释引用 UI 日语文字用「」角括号）。
5. 删功能要五端 + 路由 + 假数据 + 类型 + 设计文档全删（memory `feedback_decision_to_cut_must_delete_code`）。
6. 本地后端在 `localhost:8000`（开发模式 + SQLite `tomoshibi_dev.db`），iOS Debug 构建写死连它。学生登录：`060218`（留学生リュウ）/ `060103`（一般田中），密码都 `123456`。
7. 跟 itsuki 沟通：中文、零基础解释、英文/日语/缩写第一次出现必翻译。
