# WIP / TODO 瘦身 — 会话交接提示词

> 2026-06-10「优化DMSD项目框架」会话起草。itsuki 把本文件发给（或让另一个 Claude Code 会话读取）后，接手会话照此执行。
> 接手会话做完后：把本文件 `git mv` 到 `99_archive/2026-06-10_handoff已完成/`，并更新 `00_admin/handoff/README.md` 末尾清单。

---

## 给接手会话的完整指令（itsuki 直接粘贴下面这段即可）

请读 `00_admin/handoff/WIP_TODO瘦身_交接_2026-06-10.md` 并照做。任务是给 `00_admin/WIP.md` 和 `00_admin/TODO.md` 瘦身。做之前先跑 `scan.sh` 确认没有别的会话正占用这两个文件（它们是 advisory_lock 文件）。

---

## 任务一：TODO.md 分层重组

**现状问题**（2026-06-10 测量）：TODO.md 约 2035 行、未完成条目约 462 条。没人能从中看出「下一步该干嘛」，它已经从任务清单退化成垃圾场。

**目标结构**（重组后 TODO.md 顶部到底部）：

1. **§A v1.0 上线必做** — 不做完就不能在宿舍正式上线的事。判断标准：影响真实学生/老师使用、或安全问题、或数据会错。预计是少数（几十条以内）。把散在全文件的这类条目挑出来移进来。已知必属于这层的：demo scaffold 清除清单（memory `project_demo_scaffolds_to_remove_before_v1` 提到的）、系统bug专栏里标「仍存在」的鉴权类、Android 签名密钥备份、扣分月结算 cron 确认。
2. **§B 该做但不挡上线** — 体验改进、文档完善、重构类。
3. **§C 长尾/存疑** — 三个月没人动过、或不确定还要不要做的。**不删除**，只归到这层。每条保留原文。
4. **§D 等 itsuki 拍板** — 现有「等拍板」类条目集中。
5. 已完成（标 ✅ 的）条目：全部移到文件最底部「已完成归档」段，或者数量太大的话移到新文件 `00_admin/TODO_完成归档.md`（移动用剪切，保留原文）。

**铁律**：
- 只搬家和归类，**不删任何未完成条目的内容**（itsuki 没逐条拍板砍）
- 每条挪动保留原文不改写
- 归类拿不准的进 §C，不要猜进 §A
- 4 月老 AC 待办（占位符 / 真人访谈，在「progress_overview 退役提取」段里）保持原位等 itsuki 拍板，不归类
- 改完跑一遍 `grep -c "^- \[ \]"` 前后对数，未完成条目数必须一致（证明没弄丢）

## 任务二：WIP.md 顶部瘦身

**现状问题**：顶部「最后更新」段积了几千字，每次会话启动 AI 都全量读，烧上下文且没重点。

**目标**：
1. 顶部「最后更新」只保留**最近 5次会话**的条目，每条不超过 5 行
2. 更老的条目剪切到 `99_archive/2026-06-10_WIP历史段归档.md`（新建，开头写一句这是什么）
3. 在 WIP.md 顶部加一行滚动规则注释：`<!-- 滚动规则：最后更新段只留最近 5 次会话，老条目收尾时移 99_archive -->`

## 完成后

1. 分两笔 commit（TODO 一笔、WIP 一笔），显式文件路径提交
2. 本文件 `git mv` 到 `99_archive/2026-06-10_handoff已完成/`，更新 `00_admin/handoff/README.md`
3. 给 itsuki 报告：前后行数对比 + 未完成条目数核对结果 + §A 层最终条数
