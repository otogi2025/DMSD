# DMSD v0.1 冻结决策（当前唯一真值）

更新时间：2026-02-12
v0.1 原版冻结于此日期；2026-04-22 顶部加同步注（本体未改）。

> **2026-04-17 Q1 决策同步注**（4-22 补加 — S14 修复）
>
> 2026-02-12 本文件冻结时，`§4 座位显示原则` 写了"特殊状态：红十字 / 免 / 申（叠加标记）" —— 这反映的是 v0.1 的 **overlay-only 模型**。
>
> **4-17 Q1 决策后**：
> - `exempt_range`（免）从 overlay **升为 `base_status`**（与 `init/present/late/absent` 同级五选一）
> - `absence_request_pending`（申）仍是 overlay（改底色型）
> - `health_issue`（红十字）仍是 overlay（纯装饰型）
>
> → 具体建模以 `ENUM_REGISTRY.md §3-4` + `RollCall_Spec.md §2` 为准（单源真值）。
>
> 本文件其他段落（纪律阈值 / session_status 等）**未受 Q1 影响**，保留原始冻结版叙述作为历史快照。

## 1. 纪律阈值（已固定）
- 迟到：`0.5` 分
- 缺席：`1.0` 分
- 月累计 `>= 4.0`：下月罚扫
- 月累计 `>= 9.0`：下月禁足

## 2. 点呼场次状态（已统一）
- `session_status = draft | running | ended`

说明：
- 现阶段不引入 `cancelled` / `archived`。
- 需要取消场次时，按运营流程处理并记录审计日志，后续版本再扩展状态枚举。

## 3. 规则优先级（按 RollCall_Spec）
- 以 `RollCall_Spec_v0.1` 为准。
- 全流程状态机文档已移除，当前不作为实现依据。

## 4. 座位显示原则（按 RollCall_Spec）
- 主色状态：灰（初始）/绿（准时）/黄（迟到）/红（缺席）
- 特殊状态：
  - 红十字：健康异常（叠加图标）
  - 免：免点呼（叠加标记）
  - 申：本场不点呼申请中（叠加标记，需老师处理）

## 5. 实施要求
- 前端、后端、测试样例都必须引用本文件中的阈值与枚举。
- 如有变更，先改本文件，再改 API 契约与代码。

## 6. 关联规则文件（按需参考）
- `01_specs/rollcall/FIELD_REGISTRY.md`
- `01_specs/rollcall/ENUM_REGISTRY.md`
- `01_specs/rollcall/ERROR_CODES.md`
- `01_specs/API_CONVENTIONS.md`
