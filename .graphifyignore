# DMSD .graphifyignore
# 语法跟 .gitignore 完全一样。命中模式的文件 graphify 不扫。
# 加这个文件起因 → 见 05_logs/raw/2026-05-11.md §D（5-11 graphify 装上线后发现 vendor 污染）

# ─── 第三方库源码（vendor 污染）─────────────────────────────────
# iOS demo + teacher_web demo 把 React/Babel 整个压缩库塞进了 demo 文件夹。
# graphify 不分项目代码 vs 第三方库会全扫 → React 内部几千个互调函数霸占 god nodes 前 N 名 → 真正的 DMSD 核心被挤走。
**/vendor/
**/phaseB_src/

# 压缩后的 JS 库（变量名都是 a/b/c/aa/ab 混淆，AST 抽出来全是噪声）
**/*.min.js
**/babel.min.js
**/react.development.js
**/react.production.min.js
**/react-dom.development.js
**/react-dom.production.min.js

# Source map（map 文件不是源码，扫了没意义）
**/*.js.map
**/__sourceMappingURL_*.js

# ─── graphify 自己产物（避免图谱扫自己）───────────────────────
graphify-out/

# ─── NFC/NFD 鬼影文件（macOS APFS Unicode 残留，不是真文件）──
**/NFC_NFD_鬼影文件/

# ─── Git / 工具元数据（graphify 默认会跳但显式写一遍更稳）────
.git/
.beads/
.claude/sessions/
