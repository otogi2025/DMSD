# DMSD 公开仓库大整理 — 执行计划（2026-06-13）

> 目标：让 GitHub 公开仓库只剩「DMSD 软件本体」，其余（开发工具 / 个人文件 / AC 归档证据）从公开消失。
> 机制：**白名单 .gitignore**（默认全不公开，只放行项目本体）+ 个别物理迁 iCloud。
> 原则：**本地文件一个不删**（除 05_logs 迁 iCloud）；不重写已公开历史；push 必须 itsuki 明示。

---

## A. 已完成（本会话前段，已 commit，不再动）

| commit | 内容 |
|---|---|
| `0c83dbc` | AC_叙事 12 文件 + raw 滞留 2 文件 → 已复制 iCloud + 删仓库 |
| `159ed7a` | .gitignore 补带后缀备份文件格式 + 运维文档登记不入库 |
| filter-branch | 运维文档 `个人网站_pj部署运维.md`（含服务器 IP/SSH）从未推送历史彻底抹除，本地保留 |
| `e17bd21` | 文件清单刷新 + 6-09「GitHub历史清理计划」作废归档 |

## B. 这次要做的三件事

### B1. .gitignore 改成白名单模式
默认忽略一切，只放行以下「项目本体」：
- 目录：`01_specs/` `02_design/` `03_dev/` `04_ops/`
- 门面文件：`README.md` `LICENSE` `CHANGELOG.md`
- 必要配置：`.gitignore` `.github/`

效果：其余一切（现在的 + 以后新加的）默认不公开，零维护。

### B2. 从 git 移除跟踪（`git rm -r --cached`，本地全保留）
- `00_admin/`（项目管理台账）
- `.claude/`（给 AI 的配置 + skills）
- `docs/`（外部 AI 工具配置）
- `bin/`（维护脚本）
- `99_archive/`（早期代码快照 654 个）
- `06_assets/`（图标设计素材 + 学习 html + 公交真实样本）← 整个不公开（公交样本不单独放行，itsuki 6-13 确认）
- 根目录散文件：`CLAUDE.md`、`PROJECT_GUIDE.md`、`start-teacher-web.command`、`.graphifyignore`

### B3. 05_logs/ 物理迁 iCloud（移出 DMSD 文件夹）
- 复制 `05_logs/` 全部（dev_log / decisions / project_evolution / problem_solving / audit_* / 版本演变一览 / learning_path / 漏洞清单 等）到 iCloud 素材池（沿用 raw 的去向体系）
- 复制校验一致后，从 DMSD 删除，留一份 README 指路牌
- 注意：`05_logs/raw/` 已大部分迁出，只剩 README + 未跟踪滞留文件

## C. 执行后要善后的（牵连影响，CC 处理）

1. **pre-commit hook 失效点**：`00_admin/hooks/pre-commit` 有一段「CHANGELOG 顶部版本号必须能在 `05_logs/版本演变一览.md` 查到」的拦截。05_logs 迁走后该文件不在 DMSD → 此检查失效。处理选项：① 把 `版本演变一览.md` 留在 DMSD 本地（不公开）单独保留 ② 去掉这段检查。执行时定。
2. **要更新的 skill / 工作流清单**（移除跟踪后这些动作要调整）：
   - `session-wrap`：收尾「提交 WIP / dev_log」动作 → 这些文件不再被跟踪，commit 动作失效，dev_log 改写 iCloud
   - `dmsd-startup`：开局读 `00_admin/项目心智模型.md` + `WIP.md` → 本地还在，能读，无影响
   - `version-bump` / 文档同步 hook：引用 05_logs 路径的要改
   - → 做完后 CC 出一张「受影响 skill / hook」清单
3. **失去的保护**：移除跟踪的文件不再有 git 版本历史 + 多会话防覆盖。00_admin 影响小（当下状态文件）；05_logs 用 iCloud 兜。

## D. 这次不做（留作单独后续任务）

- **数字编号目录改名**（`01_specs/`→`specs/` 等）：牵动 5 端源码相对路径 + 构建配置 + 几百处文档/hook/skill 引用，要 5 端逐个重新构建验证，风险高。单独做 + 留足验证时间，不捆绑本次。

## E. itsuki 已确认（2026-06-13）

1. ✅ `06_assets/` 整个不公开（公交样本 `bus_schedule_real.md` / `bus_notice_*.md` 不单独放行）。
2. ✅ 白名单方案、05_logs 迁 iCloud、数字编号改名留后续 — 全部敲定。
3. 未拍板的只剩「执行」本身 — itsuki 要先 compact 再开始执行（本计划即执行依据）。

## 最终 GitHub 首页预览（执行 + push 后）

```
DMSD/
├── 01_specs/        ← 规格
├── 02_design/       ← 设计
├── 03_dev/          ← 5 端源代码
├── 04_ops/          ← 部署运维
├── .github/         ← CI
├── README.md
├── LICENSE
└── CHANGELOG.md
```

一个干净专业的软件项目 + 完整 commit 历史（持续几个月开发的铁证）。

## 执行顺序（敲定后照此走）

1. 改 .gitignore 为白名单（B1）
2. 05_logs 复制 iCloud + 校验（B3 前半）
3. `git rm -r --cached` B2 全部 + 05_logs（B2+B3 后半），留 README 指路牌
4. 善后 hook（C1）
5. 一个 commit 落地，给「待 push 预览」
6. push 等 itsuki 明示
7. 善后 skill 清单（C2）

## 红线

- 本地文件不删（除 05_logs 迁 iCloud 后删，但 iCloud 有备份）
- 不重写已公开历史
- push 必须 itsuki 逐次明示
- 暂存区提交前 `git diff --cached` 核对，防卷走别会话改动
