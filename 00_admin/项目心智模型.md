# 项目心智模型（AI 开局必读骨架）

> **这是什么**：DMSD 整个系统「怎么跑通 + 5 端各自现状 + 把它们绑在一起的契约」的最小骨架。**给 Claude Code（AI）每次开新会话第一眼读**，让它开局就有项目全貌，不会出现「改前端却不知道后端写到哪」这种事。
>
> **它不是谁**（避免跟别的文档搞混）：
> - 不是文件清单 —— 那是 `project-overview` skill（650+ 行，全部 1200 个文件）。
> - 不是进度叙事 —— 那是 `00_admin/progress_overview.md`（写给教授看的章节里程碑）。
> - 不是新人导览 —— 那是根目录 `PROJECT_GUIDE.md`。
> - **本文件只装「AI 干活前必须先知道的骨架」，短为美，目标 1 屏。**
>
> **维护规则**：
> - **开局自动读** —— `dmsd-startup` skill 启动时跟 WIP.md 一起读。
> - **收尾自动更** —— `session-wrap` skill 收尾时，如本会话改了某端状态 / 决了大问题，顺手更新 §4 + §6 + 顶部「最后更新」。
>
> **最后更新**：2026-05-31（§4 iOS 现状 — B 类接后端进行中 IX-004/IX-008）

---

## 1. 一句话项目 + 5 端是什么

**DMSD = 宿舍点呼数字化**。学生晚上回宿舍，用 NFC（近场通信 —— 手机或卡贴一下就传数据）刷一下完成签到，系统自动记考勤 + 算扣分，核心是防代刷。系统对外名 **Tomoshibi（灯火 / ともしび）**，DMSD 是项目/仓库代号。

5 个「端」（互相独立、各跑各的、靠后端连起来的 5 个程序）：

| 端 | 是什么 | 代码在哪 | 技术栈 |
|---|---|---|---|
| 后端 backend | 唯一判定者 + 数据库 + 给所有端提供接口 | `03_dev/backend/v1/` | FastAPI + PostgreSQL |
| 学生 iOS | 学生用的 iPhone App | `03_dev/student_ios/v1/` | Swift + SwiftUI |
| 学生 Android | 学生用的安卓 App | `03_dev/student_android/` | Kotlin + Compose |
| 老师网页 teacher_web | 老师看座位 / 改判 / 管学生 | `03_dev/teacher_web/v1/src/` | standalone HTML + client.js |
| 点呼机 rollcall_device | 宿舍门口那台刷卡机 | `03_dev/rollcall_device/` | Pi 3A+（树莓派）+ PN532 读头 |

---

## 2. 系统怎么跑通（签到一条主线）

详细流程 + 攻防分析 → `02_design/flow_design.md`。骨架一条线：

```
学生刷卡 / 手机 tap
  → 点呼机或手机拿到 device_id + nonce（10 秒一次性随机数）
  → POST 到后端 checkin 接口（手机端附 ECDSA 数字签名）
  → 后端校验：nonce 没过期? 签名对? 设备注册了? 在时间窗内?
  → 写 attendance_event（只追加、不改旧记录）
  → WebSocket 实时推：老师网页座位变绿 + 点呼机播报姓名 + 亮灯
```

**三条签到路径并存**：① NFC 卡贴点呼机 ② iPhone tap 动态贴纸 ③ Android tap 同一张贴纸。每个时间窗内一个学生只能签到一次（幂等）。

---

## 3. 把 5 端绑在一起的契约（改任何端都要遵守）

全部细节 → `01_specs/API_CONVENTIONS.md`。最关键 4 条：

1. **响应格式**：成功 `{ok:true, data:{}}`；失败 `{ok:false, error:{code,message,detail}}`。两者字段互斥 —— 成功没有 error 字段，失败没有 data 字段。
2. **鉴权**：Header 带 `Authorization: Bearer <token>`；学生 token 只能访问 `/student/*`，老师 token 只能访问 `/teacher/*`。
3. **URL 命名**：⚠️ 还没最终拍板（API_CONVENTIONS §8.2 的 L8 未决）。**临时统一用方案 A**：`/api/v1/<角色>/<资源>`（例 `/api/v1/student/me/checkins`）。新代码按 A 写。
4. **时间 + 命名**：全链路 JST（日本时间），后端 `server_now` 是唯一判定基准，客户端时间不参与判定。字段名一律小写蛇形（`student_id`，不要 `studentId`）。

> ⚠️ 真正的字段级对齐（后端 ↔ iOS ↔ Android 哪个字段叫什么、是不是 Optional 可空）→ 用 `spec-sync` skill 真扫真比，本文件不复述。

---

## 4. 5 端各自现状（⚠️ itsuki 请核对 — 这块最容易过时）

> 截至 2026-05-29，从 `project-overview §0` + `WIP 最近会话` 综合。**这一节的现状行最容易跟真实代码对不上（漂移）；itsuki 看到不对就直接改本节，你是 ground truth（真实状态的唯一权威）。**

| 端 | 成熟度 | 做到哪 | 已知坑 / 待续 |
|---|---|---|---|
| 后端 | 🟢 主干靠前，**但防作弊核心未实装** | 签到考勤主干已实装：present/late 时刻判定 + 写考勤记录（RollCallEvent 表）+ late 自动扣分 + WebSocket 实时推老师端 + 幂等；申请表 / 老师实名登录 / 扣分 / 打扫 / 前台已实装；13 路由 / 61 接口可加载 | ⚠️ **防作弊核心后端一行都没写**：nonce 校验 / ECDSA 签名验证 / device 注册校验 / 卡→学生映射 全缺。当前签到接口用老师令牌鉴权（= 老师操作点呼机模式），要手动传 student_id（代码注释标「P1 待实装」）。尚未真上线 |
| 学生 iOS | 🟢 较靠前，**B 类接后端进行中** | 登录 / 注册 / 多申请界面实装；演示版能跑模拟器；网络层就位；**5-31 起「演示假数据→真后端」：IX-004 修改届接后端(5轮Codex收敛) + IX-008 当前用户接 /me(73处假用户登录后真实化)，本地后端 localhost:8000 能真验证** | IX-008b扣分统计 / 老师退回动作 / IX-007详情 / IX-009通知等 B 类剩余；6 新申请界面逐屏点查待 itsuki 手走 |
| 老师网页 | 🟡 中 | 主线 = standalone HTML（Ryō 风格）`src/index.html` + `src/api/client.js`；实名账户登录页 + 教师管理页已做 | Vite + TypeScript 版已整体废弃归档；真后端联调待续 |
| 学生 Android | 🟡 偏早 | Compose 约 22 屏 bootstrap（界面骨架搭好） | 真后端接入 + 测试待续 |
| 点呼机 | 🟠 早 | 软件骨架 + 硬件全定稿（PN532 / LED / 喇叭 / ST25DV16K）；首单 1 台演示机采购中 | 真机 NFC 读写 / nonce 10 秒循环 / 喇叭播报模块待实装 |

成熟度图例：🟢 较靠前 / 🟡 中 / 🟠 早 / 🔴 未开工

---

## 5. 不能违反的核心不变量（设计铁律）

改任何端，下面这些都不能破（来源 `flow_design.md §5/§7` + `05_logs/decision_log.md`）：

1. **后端是唯一判定者** —— 客户端本地时间 / 状态都不算数，一切后端说了算。
2. **nonce 10 秒一次性** —— 防 URL 被复制后远距离代签（动态贴纸 ST25DV16K 每 10 秒刷新）。
3. **ECDSA 签名** —— 学生私钥存手机硬件（iOS Keychain / 安卓 Keystore），后端用注册时存的公钥验，防伪造身份。
4. **幂等** —— 同一次签到重复提交只记一次（卡用复合唯一索引，手机用 idempotency_key 幂等键）。
5. **老师入学日面签** —— 整个账号体系唯一的真人身份验证锚点；日常签到靠现场老师目视监督兜底。
6. **上线姿态**（4-19 G2 决策）：v1.0 = iOS + Android + 卡 一次性上线，不分阶段。

---

## 6. 当前未决的大问题（AI 别自己拍板，等 itsuki 决）

- **URL 命名统一**（API_CONVENTIONS §8.2 / 编号 L8）：方案 A / B / C 未最终拍板，CC 推荐 A，临时按 A 写。
- （以后有新的大未决项，收尾时往这加；决了就移走。）

---

## 7. 维护说明

| 谁 | 什么时候 | 做什么 |
|---|---|---|
| `dmsd-startup` skill | 每次开局 | 跟 WIP.md 一起自动读本文件 |
| `session-wrap` skill | 每次收尾 | 如本会话改了某端状态 / 决了大问题 → 更新 §4 + §6 + 顶部「最后更新」 |
| itsuki | 看到 §4 现状不对 | 直接改（你是真实状态的唯一权威） |
