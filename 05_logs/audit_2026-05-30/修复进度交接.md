# findings.md 修复进度交接（2026-05-31）

> **用途**：本会话上下文要 /compact 压缩，此文件保存完整状态，compact 后 Read 本文件即可无损恢复。
> **任务**：把 `05_logs/audit_2026-05-30/findings.md` 全部 175 条问题修好（itsuki 2026-05-31 指令）。

---

## 工作流（itsuki 拍板）

每修完一个阶段 → `git commit` 固化 → 派 Codex（模型 gpt-5.5 + 推理强度 xhigh）审一遍挑刺找 bug → 处理反馈 → 下一阶段。
Codex 调用方式：`codex exec -c model_reasoning_effort=xhigh "审查 commit XXX ..."`（review 子命令不能带自定义指令，故用 exec）。

## 环境约束（关键，别再踩）

- **后端**：本机能 pytest → `cd 03_dev/backend/v1 && .venv/bin/python -m pytest -q`，基线 **193 passed**。
- **iOS**：本机有 Xcode 能编译 → `cd 03_dev/student_ios/v1 && xcodebuild -project TomoshibiApp.xcodeproj -scheme TomoshibiApp -configuration Debug -destination 'generic/platform=iOS Simulator' build`。
- **Android：本机没装 SDK**（`~/Library/Android` 不存在），**无法 gradle 编译**。只能 Codex 静态审 + 标准 API 核对。
- ⚠️ **pytest 经 `| tail` 时 exit code 是 tail 的（恒 0），不是 pytest 的** → 必须 grep "N passed" 结果行核实，别信 exit code。（本会话因此误报过一次「193 全过」实为 182 error。）
- ⚠️ **ruff formatter 会删「分两步加、中间还没被用」的 import** → 加 import 必须和它的用法在同一次改动里，或改完立刻 grep 验证 import 还在。**已踩 2 次**（study.py 的 logging、main.py 的 asyncio），都导致运行时 NameError。

## 并发会话警惕

有别的会话在改 iOS（工作区里 `ApplicationsCreateBodies.swift` / `StayListStubs.swift` 未提交改动**不是我改的**）。
铁律：所有 commit 只 `git add` 自己改的具体文件，**绝不碰** iOS 那些 + `README.md` + `*.bak3` + `PROJECT_GUIDE.md`。

---

## 已修（本会话 commit 清单，均未 push）

| commit | 内容 | findings 编号 |
|---|---|---|
| `9a15aba` | 角色名「寮務一般教师→教師」代码 25 处 | teacherweb-11, teacherweb-06 |
| `13b5e1e` | 设计文档同步角色名 7 处 + findings 记 tw-11 真相 | （同上）|
| `60a2b3b` | 固化并行 iOS 会话改动作基线（编译过，**非我改**）| 一批 iOS（编号在那个会话）|
| `96eb249` | 清三端硬编码真实邮箱/手机号 | android-base-05, androidrest-06, + iOS SEED/AuthStubs + seed.py 隐私 |
| `017e121` | admin 邮箱改环境变量（修上面的隐私误伤，Codex 反馈）| —— |
| `c19a5d7` | Android 小 bug 批 | android-base-09, androidrest-11, androidrest-10, androidrest-03 |
| `1e12fa2` | 后端小 bug 批 | backend-biz-07, backend-biz-10, applchain-07, rollcall-11 |
| `2e07d58` | FeedbackScreen 提交竞态（Codex 反馈）| （androidrest-03 衍生）|
| `8b1d1da` | Codex 后端反馈批 | study logging, discipline month 严格, deps sub 容错, approval_chain JST, **rollcall-06 + applchain-12**（ws_manager broadcast 改 run_coroutine_threadsafe + main.py 注入主 loop）|
| `5aca68b` | FeedbackScreen 连点改早返回（Codex 反馈）| —— |
| `9fa41a9` | 补 main.py 漏的 import asyncio（修 8b1d1da 启动崩溃，真 193 passed）| —— |
| `7e13365` | schemas TeacherCreateIn email→EmailStr | models-entry-09 |

**已修 findings 编号汇总**：teacherweb-06, teacherweb-11, android-base-05, android-base-09, androidrest-03, androidrest-06, androidrest-10, androidrest-11, backend-biz-07, backend-biz-10, applchain-07, applchain-12, rollcall-06, rollcall-11, models-entry-09（+ iOS 基线那批由并发会话修）。
**判定无需改**：backend-biz-09（文件名由 date 类型生成、无注入风险，过度工程）。

待最后一批 Codex 审：`9fa41a9` + `7e13365`（任务 b6zxq02ry，已完成：确认无第三处漏 import + dorm_unit=2 已修）。

### 2026-05-31 续修第 2 阶段（compact 后 — 后端纯代码批）

| commit | 内容 | findings |
|---|---|---|
| `68a619f` | 后端纯代码 11 条修复（全量 199 passed）| backend-biz-04/11/14, applchain-09, rollcall-05/07/12/13, auth-account-09/10/11 |
| `3059fe8` | Codex 5.5 审查反馈：patch_event 授权顺序 + 重复改判刷扣分修 2 真问题 + 补 TestPatchEvent 4 回归（205 passed）| rollcall-07 加固 |

流程：17 分析员并行核实当前代码（不盲信 finding 描述）→ 8 真 bug 改 + 3 零风险加固 + 2 跳过 → commit → Codex 5.5 xhigh 审挑出 2 真问题（6 个核实点全误报）→ 修 + 补测试 → 再 Codex 复审确认收敛。
**这批已修**：backend-biz-04/11/14, applchain-09, rollcall-05/07/12/13, auth-account-09/10/11（11 条）。
**判定跳过**：applchain-10（Pydantic 默认丢弃额外字段，过量赋值当前不成立）/ rollcall-04（已由 d9e65f1 修）。
**残余记 TODO（需动迁移，非本次）**：rollcall-05 手动/NFC 无幂等键并发双写 — 需 PostgreSQL 部分唯一索引 `(session_id, student_id) WHERE idempotency_key IS NULL`。
**Codex 新发现（findings 175 条之外，需 itsuki 定扣分语义后再修）**：`_apply_override_demerit` 负 delta 分支（rollcall.py:611-626）撤销该学生本场次【所有】未撤销 rollcall_late/rollcall_absent 扣分，非按差额精确退。例 present→absent(+1.0) 再 absent→late 应净 +0.5，实际把整条 +1.0 撤光变 0、学生少扣分。CC 已读代码核实属实。既有设计坑、多步改判才触发、v1.0 风险低。修前要先定「多步改判正确净扣分语义」——属 itsuki 扣分制度设计领域，未擅自改。
**踩坑教训**：测试套件全量跑一度雪崩（79~190 error），定位是【并发会话改 test_applications.py 改到一半没提交】污染共享测试库；它提交后清库重跑即 205 passed。与本批改动无关（stash 对照证明）。

### 2026-05-31 续修第 3 阶段（migtest 测试基础设施批）

| commit | 内容 | findings |
|---|---|---|
| `78e37df` | migtest 5 条：conftest 防误清真库 + 强化假断言（210 passed）| migtest-02/05/06/07/10 |
| `7e03e28` | Codex 复审 3 点收紧：setdefault 保 CI ci_test.db + 安全闸 assert→RuntimeError + 签到测试 (200,201,422)→201 | （同上加固）|

- **migtest-02 最关键**：测试库地址用 setdefault + `_engine` 加安全闸（库文件名必含 test，否则 RuntimeError 拒绝 drop_all），防外部 env / .env 指向真库 `tomoshibi_dev.db` 时被清空。已离线验证：拦真库、放行 test_tomoshibi.db / ci_test.db / :memory:。
- **暴露设计问题（待 itsuki 定）**：晚自习签到 `/study/checkins` 是 teacher-only（`Depends(get_current_teacher)`），学生不能自助。点呼是学生刷卡 + 老师设备操作；晚自习若同理就没问题，若要学生自助是另开端点 / 放宽鉴权的设计变更。
- **migtest 跳过**：01（跑 alembic 要大改 conftest）/ 03 / 09（动 models.py）/ 04（applications 域、并发会话在补）/ 08（动历史迁移文件）。
- ⚠️ **Codex 用量到上限**（约今晚 23:57 恢复）：78e37df 的 Codex 审跑成（挑出并修了 3 点），但 7e03e28 的最终再确认那轮撞额度没跑成。3 点修正 CC 已直接自验（diff + 安全闸离线测 + study.py 确认恒 201 + 210 passed）。**后续阶段的 Codex gate 需等额度恢复再跑。**

---

## 剩余未修（分层 + 编号 + 修法提示）

### A. 重大工程 major_work（多日-多周实装，多为 /goal v1.0 施工领域，别跟它撞）
- **Android 整端**：android-base-01（注册码字段）、android-base-02（登录空密码放行）、android-base-04（整个无网络层）、androidrest-01/02（注册码/密码假动作）、androidrest-09（全 mock）
- **NFC 防代刷全栈**：rollcall-01（后端 card_uid/nonce/ECDSA 零实装）、android-base-03（Android NFC 模拟）、ios-home-01/02/11（iOS NFC 演示桩）、sysfeat-01/03/08（flow_design 标定稿但后端空）
- **iOS 接后端替换假数据**：ios-community-01（18 屏假数据）、ios-staylist-01（StayEditForm mock）、iosmypage-03/04（个人页假数据+假保存）
- **老师网页**：teacherweb-01/02（itsuki 已确认 /goal 修了）、teacherweb-08（假数据混杂）

### B. 需 itsuki 决策 needs_decision（积累清单，等他定）
1. **リュウイヒ + 学号 060218**（注释写"itsuki 本人"= 真实姓名+学籍号）要不要全端换成虚构 demo 人物？
2. **git 历史**里旧邮箱/手机号要不要重写清除（BFG/filter-repo）？（我建议不做：提交身份邮箱本就公开、当前源码已清、代价大）
3. **体調報告履歴 + 掃除提出履歴** 两个屏 Android 没实装（androidrest-05，入口跳首页），v1.0 要不要做？不做就删入口。
4. **注册密码下限**：代码要求 6 位、界面写"8 文字以上"（android-base-07 / androidrest-07）→ 统一成几位？需对齐后端。
5. **android-base-10** tier 字段语义（遅刻/欠席都标 tier=4）。
6. **auth-account-04/08**（注册码非一次性 / 6 位熵低）— 涉及注册码机制设计。
7. **sysfeat-02/04/09/11**（flow_design 签到模型、签到 URL 域名、注册码限流、audit log v1.1）— spec/文档决策。

### C. 能直接改、还没做（下一批候选，按可验证性排序）
- **iOS 小 bug**（能 xcodebuild 验证，安全）：ios-community-04（死字段 up/down）、06（数组下标当 id）、07（fallback 首条）、08（日历写死 4-5 月）、09（写死假内容）、11（认领无核验）、12（死方法）；ios-home-05（日期写死 4/22）、07（时间写死 21:02）、08（联系人写死）、10（公告回复静默失败）；iosmypage-07（月度未过滤）、08（详情假数据）、09（总分写死）、11（中文「快递」）、12（删号后视图栈）；ios-auth-09（splash 只验 token 存在）；ios-staylist-05（audit 静默吞）、06（mock chain 造假）、07（actor_type 粗暴）；ios-schedule-04（firstIndex??0）
- **老师网页**：teacherweb-05（client.ts 死代码删）、teacherweb-09（WS token 走 URL — 需后端鉴权配合，中等）
- **后端纯代码**（能 pytest）：✅ 全部完成或判定跳过（5-31 续修第 2 阶段 `68a619f` + `3059fe8`，见上）。残余 rollcall-05 manual/NFC 并发双写需迁移 → 挪到下方「后端要 migration」。
- **后端要 migration**（改 DB 约束，中等）：models-entry-05/06/07/08/10/11/12/13（外键/CHECK/server_default/append-only/Float 阈值）；rollcall-05 残余（点呼无幂等键并发双写，需部分唯一索引）
- **迁移与测试**：migtest-01（测试不跑 alembic）、02（可能清真库）、04（审批链没测）、05（永真断言）、06/07（断言恒真）、08/09/10
- **文档**：sysfeat-05（category ENUM vs Text）、06（时间窗硬编码）、10（旧时刻 19:30）

### D. 未核实（核实 workflow 失败单元，要重新核实再判）
- **iosmypage 全部**（iosmypage-01~12）+ **ios-schedule 全部** — 第一次核实 workflow 这两单元子代理没返回，状态未知，下次要补核实。

---

## Codex 审查建议（补测试 / 加固，非 bug，下次可做）
- broadcast_sync + run_coroutine_threadsafe 新路径补回归测试（lifespan set_loop 后从同步上下文调 broadcast_sync，验证事件进主 loop）
- POST /teachers 补 valid/invalid email API 测试；teacher_web 前端兼容 422 的 detail list（EmailStr 校验失败现在显示泛化「追加に失敗しました(422)」）
- WS dorm_unit=2 broadcast 过滤补一个回归测试（这次修了逻辑但没测试覆盖）

## 关键文件 / 命令速查
- 审查报告原文：`05_logs/audit_2026-05-30/findings.md`（175 条 + 修复进展章节）
- 核实结果分层脚本：`/tmp/parse_verify.py`（解析核实 workflow 输出，但 /tmp 会清，逻辑是读 task output 的 result 字段按 status 分组）
- 后端测试：`cd 03_dev/backend/v1 && .venv/bin/python -m pytest -q` → 必看 "N passed"（5-31 基线 205 passed；跑前先 `rm -f test_tomoshibi.db*` 防脏库雪崩）
