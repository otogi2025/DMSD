# 2026-04-22 raw log

> CC dump 区。itsuki 周末看完后挑 `#AC候选🌟` 升级到 iCloud `03_素材_候选/`。

## 目录

- §16:25 Claude Design Round 3 产出交付（standalone bundle 9.4MB）
- §16:28 Round 3 bundle 解包 · UUID → 人类可读组件名（#AC候选）
- §17:00 Round 3 走查发现日语文案 bug + 要求重做 コミュニティ 页 + 审 bug（#AC候选🌟）
- §17:40 4 项 UI 调整（詳細列 / リュウ イヒ 男寮迁移 / 清扫线 / 搜索 normalize）
- §17:55 白屏事件 2 连：Round 1 CORS / Round 2 数组越界（#AC候选🌟 双 bug 调试）
- §18:00 日本語 native-speaker 视角审查 · 「名単」「距X」等中文词发现 + 全量修正
- §18:11 单文件 self-contained bundle 脚本化（32MB base64 inline）
- §18:30 Web 学生アカウント管理页面新建（iOS §9.2 ✅ 直接做完）
- §18:45 **点呼機 代替モード · 银行卡 + iPhone 快捷指令**（#AC候选🌟 · 约束驱动设计 / thin client 思想验证）
- §19:00 IP 自动检测 + URL 一键复制 card（`/api/server-info` + `NfcQuickUrlCard`）
- §19:10 外泊申请提交期限规则实装（当週水曜 23:59 or 48h 前 · deadline badge + modal 面谈必须警示）
- §19:40 NfcQuickUrlCard 瘦身（1 行 3 元素）+ `./tomoshibi` CLI 启动脚本（ANSI 彩色 + 7 subcommands）
- §19:55 badge 换行 bug + 日语自然化（「期限超過」不自然 → 「期限後」）
- §20:30 **[Mac-demo-sprint] 砍 Pi 点呼机文档层落地 + 向管理员提问清单 + Wi-Fi 测试手册**（#AC候选🌟 · stakeholder 交付前的"问题意识"挖掘）

---

## §16:25 · Claude Design Round 3 产出交付

**标签**：`#demo4-28 #claude-design #round3 #web`  
**对应 AC 核心问题**：#3 問題解決（用 AI 工具高效出稿）

itsuki 今天下午跑完 Claude Design Round 3，把成品 `Tomoshibi_Prototype_v3__Standalone_.html`（9.4MB）放到 Downloads，通知 [Code-Agent] 导入。

**产出内容**：
- self-unpacking HTML（manifest + template + ext_resources 3 段 script block）
- 146 个资源：12 React 组件 + 3 vendor（React dev 18 + ReactDOM + Babel standalone）+ 1 Tomoshibi 火焰图 + 130 个 woff2 字体（Noto Sans JP 3 字重 × ~32 子集 + JetBrains Mono 3 字重 × ~32 子集）
- 完整 10 页 UI：login / select-teacher / roll-call dashboard / live grid / applications / outstay modal / discipline / records / search / Tier 2 skeletons

**Round 3 新增**（对比 Round 2）：late 黄 + 3-min 倒计时 + 男女寮 12+12 + 编辑模式 + 共享密码 + 老师选择页 + 7 日趋势图 + 外泊 4 段審認 + 自動アラート preview + デモコンソール + 全局検索 + Tomoshibi rebrand

**为什么值得记**：verify 了 "Claude Design 作为设计 sub-agent + Code-Agent 作为落地 sub-agent" 的 workflow 可以跑通一整个复杂 Web 产品。Round 1（3 variations）→ Round 2（核心 4 页）→ Round 3（完整 10 页）一共 ~3 小时出完一个产品级 UI。

---

## §16:28 · Round 3 bundle 解包

**标签**：`#demo4-28 #engineering #AC候选`  
**对应 AC 核心问题**：#3 問題解決 · #4 自己認識（技术问题 → 工具解法）

Claude Design 的 standalone 是为了 "双击即跑 / 断网也能 demo"，所有 JS / 字体 / 图片都塞进一个 HTML 里。缺点：没法读代码 / 没法接后端 / 没法二次开发。

**解包思路**：
1. 读 HTML 里 3 个 `<script type="__bundler/*">` block → 拿到 manifest（UUID → base64+gzip）和 template（整页 HTML，资源用 UUID 引用）
2. Python 遍历 manifest → base64 解码 + gzip 解压 → 写文件
3. 每个 JS 文件头部有 Claude Design 加的 `// ComponentName — 说明` 注释 → grep 出来作为 rename 依据
4. 把 template 里的 `_assets/UUID.ext` 替换成人类可读路径
5. standalone 依赖 `window.__resources` 对象（运行时注入）→ 解包后手动在 `<head>` 加一行 `<script>window.__resources={tomoshibiIcon:"assets/tomoshibi-icon.png"}</script>`

**产出**：
- `round3/Tomoshibi_Prototype_v3.html`（原 standalone，demo 直接用）
- `round3/src/` 可编辑版：index.html + 12 个 `components/*.jsx`（app / shell / theme / login / select-teacher / roll-call-landing / live-roll-call / override-modal / applications / outstay-detail-modal / discipline / pages-records-search-etc）+ vendor/（React/ReactDOM/Babel）+ assets/tomoshibi-icon.png + _assets/ 130 个 woff2

**AC 角度**：itsuki 可以在面试时说"我遇到一个问题 — Claude Design 给的是 self-unpacking 的单文件 bundle，demo 能跑但改不了代码。我就研究了它的 unpacker JS，写了个 Python 脚本逆向出 manifest 结构，把 146 个资源解出来，按组件头部注释自动重命名成人类可读的文件结构，最后修复运行时注入的 `window.__resources` 对象在静态版里的缺失。" → 体现 #3 問題解決 + #4 自己認識（"我不懂就去读源码然后写工具"）。

---

## §17:00 · Round 3 走查 — itsuki 的 QA 视角

**标签**：`#demo4-28 #web #QA #AC候选🌟`  
**对应 AC 核心问题**：#2 問題意識 · #3 問題解決

itsuki 打开 Claude Design Round 3 产出第一轮走查立刻指出 3 个问题：
1. `申請中心` 不对 → 日语应该是 `申請センター`（中文"中心"在日语里不这么说）
2. `寮コミュニティ` 不够明确 → 改 `コミュニティ管理`（突出老师管理视角）
3. コミュニティ 页 UI 做得太差："应该是一个真正的社区，不是一个 date + text 列表" → 要卡片 + 头像 + 点赞评论数 + 老师管理按钮（删除 / ピン留め / 通報解除）

还提出功能性需求：**老师应该有 お知らせ 投稿权限**（不只是看）。

**为什么值得记 🌟**：
- itsuki 在 Claude Design 的设计专业之上仍然能一眼看出"不对"的地方，这种"自然度直觉"是 Native 用户（日语流利的留学生）才能做到的
- 她不是被动接收 AI 产出，而是主动 QA — AC 面试素材的质量体现点
- 「应该做成像一个コミュニティ的地方」这种**用户体验批评**比我（AI）提出更敏锐，因为我容易停在"功能都有了就行"，她停在"用的人会不会觉得这是个社区"
- 证明了"AI 做 70% + 人做 30% 的 critical 决策"模型 — 可以拿来做 AC 面试里「AI 時代の人間の役割」这类问题的答案

AC 话术候选：
> "AI で UI を生成させた後、必ず自分で走査して『日本人がこれを見て違和感がないか』をチェックしました。例えば AI は『申請中心』と生成しましたが、日本語なら『申請センター』、『コミュニティ管理』の方が担当者視点で自然です。AI を過信せず、文化的・言語的な最後の判断は自分で下すというのが、自分の役割だと学びました。"

---

## §17:40 · 4 项 UI 调整一次性打包

**标签**：`#demo4-28 #web #engineering`  
**对应 AC 核心问题**：#3 問題解決

itsuki 继续补充 4 项：
1. 申請センター 列表「詳細 →」被挤没 → 列宽 80px→100px + **整行 click 可打开**（比单按钮更直观）
2. **リュウ イヒ（itsuki 本人的 demo binding）从女子寮 W101 → 男子寮 M101** — 理由：demo 当天如果管理员是男寮担任视角，也能看到她本人。连带修改 4 处 room/dorm 引用 + 级联 房号防撞
3. リュウ イヒ 点数要触发清扫罚则 → `discipline.jsx` demerit 数组调成 late=5 absent=2 → 4.5 点 ≥ 清扫线 4 点
4. 名前搜索搜不到「リュウイヒ」（没空格版） → **root cause**：roster 存的是「リュウ イヒ」（有空格），user 输入时不带空格。fix：normalize 函数去除空格再 includes 比对，Shell 顶栏 suggestions 从 hardcode 4 个改成从 ROSTER_ALL 动态生成，SearchPage 加"担当外寮 fallback + warn banner"

---

## §17:55 · 白屏事件 2 连 — 根因排查实录

**标签**：`#demo4-28 #debugging #AC候选🌟`  
**对应 AC 核心问题**：#3 問題解決 · #4 自己認識（debugging methodology）

**Round 1 · 双击 src/index.html 全白屏**
- 症状：浏览器地址栏是 `file:///.../src/index.html`，页面纯白
- 误判：最初怀疑 JSX 语法错，跑 Babel.transform 校验 12 个 jsx 全部 ✓ → 排除语法问题
- 真因：**file:// 协议下 Babel standalone 用 fetch() 拉 `<script type="text/babel" src="components/X.jsx">`，浏览器把 fetch 本地文件判定为跨源 block**。还有 `<script src integrity="sha384-..." crossorigin="anonymous">` 在 file:// 也被 CORS 拒绝（Claude Design 的 unpacker JS 专门 strip 了这 2 个属性，我解包时漏了）
- fix：(a) strip integrity/crossorigin (b) 做 `rebuild.command` 脚本把 jsx 内容直接内联进 index.html，不依赖 fetch（文件大小 560KB）

**Round 2 · 修好后点「記録」页面又白屏**
- 症状：登录 + 进主页正常，一点左 nav「記録」就白
- 误判：再怀疑 syntax → 又 verify 一遍，依然 clean
- 真因：**数组越界**。把リュウ イヒ移到男寮后男寮 12 人 → 13 人，但 records.jsx 里 `statuses = ['ok',...]` hardcode 12 项，`roster.map((x,i) => statuses[i])` 当 i=12 时返回 undefined → `RecStatusBadge` 里 `map[undefined]` → TypeError → React 整个组件树崩溃 → 白屏
- fix：(a) `statuses[i % statuses.length]` 保证不越界 (b) RecStatusBadge 加 fallback `|| [ink3, soft, '—']` 防未来同类 crash

**为什么值得记 🌟**：
- 真实 debugging 过程，2 次都最初走错方向（都怀疑 syntax）→ 诊断工具（Babel.transform）告诉我不是 syntax → 被迫思考运行时
- 学到教训：**「空白 = 组件 crash = 跑到一半的 React 错误」，不是 syntax**。React 组件树里任一节点 throw 都会让 root 白屏（没 ErrorBoundary 时）
- 第 2 个 bug 是**系统性 bug**：改了 roster 大小，但其他文件里的 hardcode 数组没跟上。教训：**demo 数据应该用固定长度 cycle（i % len），不要假设 roster 固定 12 人**
- AC 角度："硬编码数组长度耦合"是初学者常犯的错 → itsuki 已经能通过 bug 学到设计原则

---

## §18:00 · 日本語 native-speaker 视角文案审查

**标签**：`#demo4-28 #web #japanese #AC候选`  
**对应 AC 核心问题**：#4 自己認識（跨语言工作优势）

itsuki 问："名单这个词是日语吗？"  
→ **不是**。「名単」是中文词，日语只有「名簿」「リスト」「対象者一覧」。

走一次全量文案扫描发现一堆中文词残留（Claude Design 虽然是 Anthropic 产品但日语 + 中文都认，生成时会混用）：

| 中文残留 | 改成日语自然版 |
|---|---|
| 名単 | リスト |
| 距清掃まで / 距禁足まで | 清掃まで残り / 外出禁止まで残り |
| 本月 / 当月 | 今月 |
| 改判者 | 手動調整者 |
| 晚点呼 | 晩点呼（晚 是中文字，日语用 晩） |
| 部活早朝 | 部活生（原词不自然，早朝部活和部活早朝是不同意思） |
| push 送信済み | プッシュ通知送信済み |
| 自分で入力可 | 自分で記入可 |
| 担任 先生（有空格） | 担任 |
| スプレッドシート × 入力可 | 食数の自由記入可（× 含义不明，Claude Design 随便塞的）|
| 避難訓練 · 軍歌斉唱後 | 集合・点呼後（軍歌 避難訓練 语境太诡异）|

**为什么值得记**：
- **itsuki 的 language edge**：她是留日留学生，中文日文都 native，能一眼 spot 出 AI 生成内容里的"中日混血"文案。这是 AI 做不到的（它不知道"名単"在日本人眼里是异国词）
- AC 面试话术候选：
> "AI 生成的日语文案中经常夹杂中文词汇，比如'名单'、'距清扫まで'这种对日本人来说不自然的表达。我作为既懂中文又懂日语的用户，能在每个页面扫描时立刻发现这些问题。这让我意识到：**AI 不是语言上的 native，最终的语言 QA 永远需要真人**。这个视角也成了我对'AI 时代人类的角色'的回答的一部分。"

---

## §18:11 · 单文件 bundle 脚本化

**标签**：`#demo4-28 #engineering #tooling`

itsuki 问："以后双击 index.html 就能 demo 展示了吗？"  
→ 可以，但要带整个 `src/` 文件夹（vendor + assets + 130 字体）。

→ 干脆做**真正的单文件版本**，像 Claude Design 原版一样能 U 盘拷一个文件走：

**`build_single_file.py`** 脚本：
1. 把 `components/*.jsx` + `vendor/*.js` 全部 inline 进 index.html
2. 把 woff2 字体用 `base64` 编码嵌进 CSS `url("data:font/woff2;base64,...")`
3. 把 `tomoshibi-icon.png` 同样 base64 inline 到 `window.__resources`

**产出**：`Tomoshibi_v3_single.html` 32.2 MB（Claude Design 原版是 9.4 MB 因为 gzip 了，我的没 gzip 所以大一些）

配套 `打包单文件.command` 双击脚本。

**什么时候用这个**：demo 当天 U 盘拷走、借任何别人的电脑演示 / 管理员教授有要求把成果留下一份 / 4-28 演示当场 Wi-Fi 有问题的兜底。

---

## §18:30 · Web 学生アカウント管理页面新建

**标签**：`#demo4-28 #web #ios-sync`  
**对应 AC 核心问题**：#3 問題解決

itsuki 看完刚出炉的 `Student_iOS_new/IOS_DESIGN_LOG.md`（[Mac-demo-sprint] 写的），一眼看出需要补齐老师 Web 侧的一块功能：
- iOS App 内 **密码不能自改**（§3.7 明文规定）→ 必须老师后台代办
- 00 号 = リュウ イヒ demo seed / 01 起真实学生自增（§3.2、§3.4）
- 之前 §9.2 挂了「老师 Web 加学生账号管理」的账，标记 ⏳ pending 等下次会话

itsuki 直接："现在在 Round 3 里加一个"。跳过"等下次会话"的延迟，利用当下的上下文和流程一次做完。

**实装**：
- `components/accounts.jsx` 新建 AccountsPage + AccountDetailModal 2 tab
- `theme.jsx` 加 `window.ACCOUNTS` seed 24 人（00 = リュウ イヒ + 01-23 = 其他学生，含 email / phone / lastLoginAt / locked / failedLoginCount）
- Shell 左 nav 加「学生アカウント管理」
- 功能：列表（番号 / 氏名 / 部屋 / メール / 電話 / 最終ログイン / 状態）+ 4 stat card + 搜索 + 4 filter · 详情 modal：プロフィール tab（邮箱/电话/部屋可改 + パスワード初期化 + ロック解除）+ アクティビティ tab（点呼/申请/扣分/体調/登录失败时间线）
- iOS 锁定升级 5 阶段规则嵌在详情 modal info box 里

---

## §18:45 · 点呼機 代替モード · 银行卡 + iPhone 快捷指令 🌟

**标签**：`#demo4-28 #hardware-pivot #engineering #AC候选🌟`  
**对应 AC 核心问题**：#2 問題意識 · #3 問題解決 · #4 自己認識（制约驱动设计）

### 契机

itsuki：「点呼機没时间买了，用银行卡代替」

距离 4-28 demo 还 6 天，Pi 3A+ 已经下单但今天 4-22 送达时间不定。而 itsuki 意识到一个更干净的方案：**不买专用硬件**，直接用她已有的：
- iPhone 17 Pro（iOS 26，支持 NFC Background Automation）
- 日常银行卡（FeliCa 芯片）

### 设计

```
① itsuki 拿银行卡碰 iPhone
       ↓ iPhone 快捷指令（NFC UID trigger）
② POST http://<Mac局域网IP>:8787/checkin?no=00
       ↓
③ demo_server.py 记录事件（in-memory · seq 自增）
       ↓
④ LiveRollCall useEffect 每 1s fetch /events/latest
       ↓ seq 变化 → 查 ACCOUNTS[no=00] → リュウ イヒ
⑤ 座位变绿 + `SpeechSynthesisUtterance('リュウイヒ', lang='ja-JP')` 读出
```

### 实装代码

1. **`demo_server.py`**（Python · 扩展 `http.server.SimpleHTTPRequestHandler`）
   - POST `/checkin?no=XX` → 更新 latest_event + print 「✔ 点呼」
   - GET `/events/latest` → JSON {no, name, timestamp, seq}
   - CORS `*` 允许局域网任意设备 POST
   - 启动时打印 Mac 的 局域网 IP（供 iPhone 快捷指令 URL 用）

2. **`live-roll-call.jsx`**
   - 新 useEffect：`setInterval(poll, 1000)` + initialSeq 去重首次 baseline
   - 收到新 seq → 从 `window.ACCOUNTS` 查学生 → `setStudents` 更新 status/checkinAt → SpeechSynthesisUtterance 日语 TTS
   - 新 `NfcIndicator` 顶部徽章 3 态：📡 NFC 待機中 / ✓ 受信 OK / ⚠ 不明な番号

3. **`NFC_DEMO_SETUP.md`** · 教程文档
   - Mac 侧：双击 `开发模式跑.command`
   - iPhone 侧：ショートカット App → オートメーション → NFC → 扫银行卡 → URL (POST) 到 Mac IP → 关「実行前に尋ねる」
   - 故障排查 5 条（IP 变 / 防火墙 / 浏览器 TTS / 番号误 / 多卡冲突）
   - 演示台本（1 分钟版）
   - AC 面试话术候选

4. **`开发模式跑.command`** → 改调 `demo_server.py` 而非 `http.server`

### 为什么这是 AC 候选 🌟

**1. 约束驱动的创造性解决**
- 约束：硬件来不及 + 时间不足
- 不是「放弃 demo 效果」或「演砸了忽悠过去」，而是用**已有资源重新实现**同样体验
- Tomoshibi 的核心设计思想是「thin client / thick server」→ 学生端只是一个 NFC UID 发射器，判定逻辑全在服务器
- 所以「银行卡（发射 UID）+ iPhone（传递 UID）+ Mac（判定）」跟「NFC 卡 + Pi 3A+」在系统层面**完全等价**
- 这不是"作弊"或"代替品"，而是**验证了架构设计的正确性** —— 因为 thin client 抽象得足够好，任何能发射 UID 的东西都能替代点呼機

**2. iOS 26 快捷指令 + NFC Automation 的技术洞察**
- 一般人用 iPhone 只用 App
- itsuki 知道 iOS 的 NFC Auto 能接任意 URL trigger，且能后台静默运行（不用解锁也不会弹通知）
- 这是**对平台能力的深度理解** → AC 评审会看重

**3. Speech Synthesis Web API 的选用**
- 不用额外安装 TTS 软件、不用调云端 API
- 直接用浏览器自带 `SpeechSynthesisUtterance` + `lang='ja-JP'` → 零依赖 + 零成本 + 跨平台
- AC 面试可以讲："为了让演示有声音反馈，我不用麦克风录音也不装软件，用 Web 标准 API 就实现了。这种『用标准组件替代专业工具』的思维是软件工程的基本功"

**AC 话术候选**：
> 「点呼機が間に合わないと分かったとき、一度は諦めかけました。でも Tomoshibi のシステム設計を見直すと、学生側の端末はただ UID を発射するだけで、判定は全部サーバー側で行っています。だったら UID を発射できるものなら何でもいい — iPhone の NFC オートメーション機能を使って銀行カードをタップすれば、POST リクエストでサーバーに『番号 00 が点呼した』と伝えられる。10 分で実装できました。制約は創造を生む、という経験でした」

**另一个角度**（Tomoshibi 设计哲学）：
> 「これは偶然ではなく、最初から『thin client / thick server』で設計したからこそ、ハードを入れ替えても動きます。設計の良さは制約の下で初めて証明される、ということを学びました」

---

## §20:30 · 砍 Pi 文档层落地 + 向管理员提问清单 + Wi-Fi 测试手册 🌟

**标签**：`#demo4-28 #stakeholder #AC候选🌟`
**对应 AC 核心问题**：#2 問題意識 · #3 問題解決 · #4 自己認識

### 契机

itsuki 在 §18:45 拍板"砍 Pi / 用银行卡 + Shortcuts 代替"之后，立刻给 [Mac-demo-sprint] 追加要求：

> "我还需要跟你讨论，确定现在还有什么不确定的问题**必须要问老师才知道**。比如说宿舍的 Wi-Fi 架构（这个我可以自己去做测试，但是要老师的允许还需要你教我怎么做）。还有后端服务器的预算，点呼机的预算等等。并且要更新一下 demo 要做的东西的记录文件，然后这次要问的问题和别的东西，全部都单独放在一个地方，免得我找不到。"

核心信号：itsuki 意识到 **demo 只是交付的一半，另一半是从 stakeholder 那里"取回"信息**。

### [Mac-demo-sprint] 的挖掘过程

CC 没有只按照 itsuki 给的 3 件事（Wi-Fi / 服务器预算 / 点呼机预算）写一个短清单，而是先挖了 5 个她还没想到的洞：

1. **语音喊名原创设计** — itsuki 砍 Pi 没想到这切掉了她"灯火防代签"的 AC 核心叙事 → CC 推荐 Mac `say` / iPad Safari Web Speech 代替（方案 B）
2. **已下单的 Pi 怎么办** — itsuki 澄清"**还没下单**"，于是问题消失
3. **银行卡 NFC 静态 UID 漏洞** — 跟 4-20 ST25DV 决策时发现的漏洞同构，demo 演示"简化方案"要讲清楚
4. **银行卡能被 iOS Shortcuts 识别吗** — EMV 协议可能让 iOS 拒识别，itsuki 需要今晚自测（Shortcuts → NFC → 扫一下试）
5. **demo 从"端到端真跑"降级为"概念演示"** — 对管理员说服力打击，补救靠"上线版本完整架构图 A4 打印"

然后把 itsuki 给的 3 件事扩展到 **7 大类 35 个问题**，分 Tier A 必问（8）/ B 追问（10）/ C 书面（11）：

| 类 | 数量 | 代表问题 |
|---|---|---|
| Wi-Fi 基础设施 | 6 | Wi-Fi 架构 / VLAN 隔离 / 客户端隔离 / 实地测试许可 |
| 预算 | 6 | 服务器 / 点呼机 / NFC 卡 / 维护 / 学生支援金 / 毕业归属 |
| 现有流程 | 7 | 时间 / 迟到规则 / 扣分数字 / 申请方式 / 处分执行 / 人数 / 时长 |
| 角色权限 | 4 | 谁用 Web / 权限层级 / 管理员设备 / 家长 |
| 合规 | 5 | VPS 合规 / IT 审批 / 家长同意书 / 保留期 / 毕业交接 |
| 上线 | 4 | 时间 / 试运行 / 学生培训 / 回滚 |
| 期待 | 4 | 最头疼 / 最大痛点 / 额外功能 / 评估方式 |

### 产出（今晚 CC 落盘 11 文件）

**新建 2**：
- `00_admin/demo_4-28/questions_for_admin.md` — 35 问题分 Tier + 每题话术 + 答复模板 + §10 Demo Day 使用说明
- `00_admin/demo_4-28/wifi_survey_howto.md` — Wi-Fi 测试手册（许可话术 + 5 步测法 + 零基础术语速查 + 降级方案 + AC 叙事点）

**修订 9**：
- `scope_tier.md` 加 §0.1 砍硬件决策段 + §1.5 语音喊名改 iPad/Mac + §1.4 硬件替代升级 + §4 代码 agent 入口删 device
- `demo_script.md` §0 准备清单简化 + §1 开场 pitch 加"现场 vs 上线版"叙事 + §3.2 语音喊名改 iPad TTS + §10 收尾加 Tier A 8 问 + §11 Q4 点呼机话术 + Q4.5 新增 + Q8 成本细化
- `sprint.md` §0.5 硬件砍到 0 采购 + §3 时间表重写（D2 砍 Pi 决策 + 文档层）+ §4 架构图更新 + §5 风险清单改（砍硬件相关风险）
- `ST25DV_fallback.md` 改名为"NFC 替代方案：iPhone Shortcuts + itsuki 自有 NFC 卡" + §2 硬件 0 采购 + §3 自测步骤 + §5 话术更新 + §7 checklist 简化
- `hardware_design_v0.1.md` §0 状态表 + §4.1 Demo 全砍（¥12380 → ¥0）+ §4 策略段重写
- `CLAUDE.md` §项目信息 4-28 Demo 段 + 采购策略段更新
- `README.md` 档案目录加 2 新档案指针
- `raw/2026-04-22.md` 加本段
- `WIP.md` 进行中任务加今日记录

### 为什么这是 AC 候选 🌟

**1. "问题意识"进化的证据链**

itsuki 从 "做 demo 取悦老师" 进化到 "demo 是双向的 — 我演示系统 + 我从 stakeholder 那里取回信息"。这是从 **独行侠开发者** 进化到 **对业务环境有感知的产品负责人** 的关键转变。

她主动说"**必须要问老师才知道**" — 这句话里藏着"承认认知边界 + 主动闭合" 的 AC 式 problem awareness。

**2. CC 的 "主动挖洞" 框架（feedback_find_5_holes_after_any_proposal.md）的实际应用**

itsuki 说 3 件事，CC 挖到 5 个洞。不是补充细节，是推翻隐藏前提（"你砍 Pi 但没想到原创设计也砍了 / 银行卡可能不被识别 / 叙事打击怎么补救"）。这种**主动找洞 + 不谄媚**的 AI 协作模式是 CLAUDE.md 明确要求的，今天是纯度最高的一次应用。

**3. Stakeholder 前置准备的结构化**

从"演完再想问什么"→ "演前结构化问什么"，从 3 题 → 35 题 → 8 必问，从零散 → Tier A/B/C 分层。这种**从任务到流程的结构化升级**是零基础留学生最难学的，今天 CC 的输出让 itsuki 可以照搬 / 修改，不用从头设计。

**4. 合规意识（Wi-Fi 测试许可话术）**

CC 没直接教 itsuki "用 Mac 跑 airport 命令扫 Wi-Fi" —— 先提醒 **必须先问老师许可**，然后明确区分"被动测量"和"主动入侵"的界限，给出"被拒绝怎么降级"的 fallback 方案。这是"合规意识"的具体操练，和 AC 面试问"你怎么处理 sensitive 数据 / 学生隐私"类题直接挂钩。

**AC 话术候选**：

> 「デモ準備を進めるうちに気づいたのは、デモは私から相手への一方的なプレゼンではなく、**相手から情報を取り戻す機会でもある**ということです。寮の Wi-Fi 構造、予算体系、既存の点呼運用ルール、法的コンプライアンス — これらは寮長と話さなければ分からない情報ばかりで、仮定で進めたら上線時に必ず破綻します。デモの 12 分を使って『質問を 8 つ必ず聞く』と決めておいて、答えを持ち帰って設計を直す。これが『実装できる人』から『環境を持って実装する人』への一歩だと感じました」

---

## §19:00 · IP 自动检测 + URL 一键复制 card

**标签**：`#demo4-28 #web #ux`  
**对应 AC 核心问题**：#3 問題解決

itsuki 第一次去 iPhone 设置 NFC 快捷指令就遇到"怎么查 Mac 的 LAN IP"。解法是让 Mac 自己告诉自己：
- `demo_server.py` 加 `/api/server-info` 端点，用 **UDP connect 8.8.8.8** trick 拿 outbound IP（无论 Wi-Fi 还是热点都准）
- `roll-call-landing.jsx` 加 `NfcQuickUrlCard`：fetch 该端点 → 番号 select 组合成完整 URL → コピー button
- 以后 IP 变了（换网络 / 换热点）自动更新，不用手动查

小细节但**省掉一个 stakeholder 当场出错的时刻** = 高价值 demo 投资。

---

## §19:10 · 外泊申请提交期限规则实装

**标签**：`#demo4-28 #web #business-rule`  
**对应 AC 核心问题**：#1 問題発見 · #3 問題解決

itsuki 说："外宿的申请要当周的周三或者提前两天交，到了时间就不允许交了，必须要跟老师去谈"。

这是**真实业务规则**：宿舎の現行紙申請の運用をデジタル化する時にそのまま取り込むべきルール。

**实装**：
- `outstayDeadline(departStr)` helper：`min(出発日の属する週の水曜 23:59, 出発 48h 前)`（两条件取较早 = 更严格）
- `DeadlineBadge` 列加到 ApplicationsPage：⚠ 期限後 / ✓ 期限内
- OutstayDetailModal 加 `§ 提出期限` section：期限 / 実提出 / 遅れ時間 / 面談必須 red banner（期限後のみ）
- 顶部 banner 说明规则（可折叠）
- Seed 调成 2 late / 2 on-time 展示两种状态
- iOS 侧同步：`IOS_DESIGN_LOG` 19:10 追记 iOS App 要在申请 flow 加 deadline check + 期限後送信 block + 寮監室導線

**AC 角度**：这是"把纸の運用をデジタル化する时坚持既有规则"的例子。很多学生会用"系统不检查就可以偷偷交"的心态；itsuki 选择**让系统执行规则 + 给不合规的人一条正规救济路径（跟老师谈）**。这个设计思想比"功能能用"更深一层。

---

## §19:40 · 简化 UI + CLI 启动脚本

**标签**：`#demo4-28 #polish #tooling`  
**对应 AC 核心问题**：#3 問題解決 · #4 自己認識

itsuki 看 NfcQuickUrlCard 第一版（带 banner / DEMO 专用 / 使い方+前提+テスト 3 段说明 / テスト送信 button / 折叠按钮等 5 个元素）直接评："一大堆东西看起来太蠢了，写网页就好了"。

**精简**：170 行大 card → 20 行 **1 行 3 元素**（番号 select + URL 框 + コピー button）。砍掉 5 个次要 UI + 所有说明文字。

**另加 CLI 启动器**：itsuki："写一个sh脚本，这样看起来比较专业"。做 `./tomoshibi` bash CLI：
- ANSI 彩色 banner + 🔥 emoji
- 7 个 subcommand：`start / stop / status / ip / rebuild / pack / help`
- 替代原来双击 `.command` 的方式

demo 当天打开 terminal → `./tomoshibi` → banner + IP + 启动日志滚动 → 观众的"开发者感"比双击图标强很多。

**为什么值得记**：
- itsuki 的**美学判断**很准 — 「太蠢了」是直觉但准确。AI 默认生成的 UI 倾向于"把所有功能都摆出来"，真人的"克制"是 AI 很难模仿的品质
- "sh 脚本看起来更专业"也是设计判断 — 不是功能需要，是**演出需要**。demo 本质是表演，专业感影响 stakeholder 信任

---

## §19:55 · badge 换行 bug + 日语自然化第二轮

**标签**：`#demo4-28 #japanese #debugging`  
**对应 AC 核心问题**：#3 問題解決 · #4 自己認識

itsuki 指出 2 个问题：
1. **bug**：「期限超過」badge 文字折成两行（「期限超」/「過」）→ 列宽 90px + 无 nowrap → 修：列宽 110px + `whiteSpace: nowrap`
2. **「哪有这个日语啊，日语不自然」** → 「期限超過」是中文语感，日语校規文書不用

**修**：「期限超過」→「期限後」（和「期限内」对称、短、natural、校規用语）
一起改：
- 「期限より X 遅れて提出」→「期限の X 後に提出」
- 「期限まで残り X で提出」→「期限の X 前に提出」
- 警告 banner 缩短："本来なら..." → "本来は iOS App から送信できない申請です..."

**为什么值得记**：
- itsuki 的 native QA 第二次发现 AI 生成的日语不自然 → 验证 §18:00 的模式（AI 对日语地道度判断有盲区）
- 「期限超過」不是错 — 字典查得到 — 但**在日本人眼里不 idiomatic**。这种判断只有 native speaker 能做
- 已经可以总结成 AC 素材的一个 pattern："AI 生成的日语一轮 review 不够，需要多轮 native 抽查"

---

**END** — raw dump 区，等 itsuki 挑升级。
