# DMSD 问题清单 + Codex 修复 SOP

> **作用**：CC 把 11 区域 codex 报告找到的问题整合成可执行清单。每个问题配 Codex 修复 prompt — codex 限额恢复后直接跑，或者 itsuki 醒来 review 后让 codex 跑。
>
> **限额管理**：
> - CC 撞 Anthropic 限额 → 等 02:21 / 7:10 刷新后重做
> - Codex 撞墙 → CC 接管（但 Claude Code classifier 拦批量动作 → CC 写脚本不直执行）
>
> **回滚**：万一改坏 → `cd ~/dev/DMSD && git reset --hard pre-cleanup-2026-05-11`

---

## 📊 11 区域健康度排名（按低到高，优先修最低）

| 区域 | 健康度 | 优先修 |
|---|---|---|
| 03_device | **45/100** | 🔴 P0 — 代码全是空骨架 |
| 03_ios | **55/100** | 🔴 P0 — 上架风险（注册门禁 / 签名 / baseURL） |
| 03_backend | **62/100** | 🔴 P0 — 12 个具体 bug |
| 00_admin | **62/100** | 🟡 P1 — 文档治理（CC 已识别） |
| 03_web | 中等偏低 | 🟡 P1 — NFC 编号演示会失败 |
| 03_android | 中等偏好 | 🟡 P1 — 3 个闭环 |
| 02_design | 中等偏好 | 🟢 P2 — §8 数据模型滞后 |
| 99_archive | 中等偏乱 | 🟢 P2 — 归档区像活代码 |
| 05_logs | 中上 | 🟢 P2 — 索引文件过期 |
| 01_specs | **72/100** | 🟢 P2 — 二进制冻结快照 |
| claude | — | ⚪ codex 没真分析（5K） |

---

## 🔴 P0 — 03_backend 12 个 bug（最严重）

### Bug B1: `rollcall.py:83-86` minute-5 整点 session 触发 ValueError

**问题**：seed 里 22:00 session，启动前 5 分判断时 `minute - 5` 变负数 → `ValueError`。

**Codex 执行 prompt**：
```
cd ~/dev/DMSD/03_dev/backend/v1 && codex exec -s workspace-write \
  --skip-git-repo-check \
  -c model_reasoning_effort=high \
  "读 app/routers/rollcall.py:80-95，修 minute - 5 bug。

要求：
1. 用 datetime.timedelta 替代裸 minute 算术
2. 测试 edge case：22:00 session 启动前 5 分（21:55）
3. 在 tests/test_smoke.py 加一个 case 覆盖整点 session
4. 跑 pytest 确认全绿
5. 报告 diff + 测试结果"
```

### Bug B2: `applications.py:151+254` route shadow

**问题**：`GET /{application_id}` 定义在 `/pending-for-me` 前面，FastAPI 路由匹配顺序导致 `GET /api/v1/applications/pending-for-me` 被 UUID 动态路由吞掉 → 422。

**Codex 执行 prompt**：
```
读 app/routers/applications.py:140-260，调整路由定义顺序：所有静态路径（如 /pending-for-me）必须定义在动态路径（{application_id}）前面。

要求：
1. 用 git diff 显示 reorder 后的代码
2. 加测试 case 覆盖 /pending-for-me 真返回 200 不是 422
3. 检查其他 router 是否有类似 shadow（accounts / announcements / study / rollcall）
4. 修完跑 pytest
```

### Bug B3: `accounts.py` 缺 `DELETE /accounts/me` 实装

**问题**：文档承诺账号删除闭环，实装空。`models.py:83` status 约束也不允许 `deleted`。

**Codex 执行 prompt**：
```
按 BACKEND_DESIGN_LOG.md:642-673 spec，在 app/routers/accounts.py 加：
1. DELETE /accounts/me endpoint
2. 更新 models.py:83 status 约束加 'deleted'
3. 写 alembic migration 改约束
4. 加 audit_log 记录删除
5. 加测试覆盖（删除前 / 删除后查询 / 重复删除）
```

### Bug B4: `rollcall.py:145-153` NFC 防作弊核心未落地

**问题**：spec 要 `card_uid` 唯一索引 + 防作弊，实装没 `student.card_uid` 字段，没有 card 表，路径 A NFC 直接 `UNKNOWN_CARD`。

**严重性**：🔴 项目核心承诺没做（防作弊是 AC 叙事重点）。

**Codex 执行 prompt**：
```
按 RollCall_Spec.md spec 实装 NFC card_uid 链路：

1. models.py 加 cards 表（card_uid PK / student_id FK / issued_at / retired_at）或 students.card_uid 字段（看 spec）
2. alembic migration
3. seed.py 给现有学生发卡
4. rollcall.py:145-153 改成真按 card_uid 查 student
5. routers/admin_cards.py 加发卡 / 注销卡 endpoint
6. 测试 NFC 路径 A：知道 UID → 返回 student / 不知道 UID → UNKNOWN_CARD
```

**注意**：这是大改动，建议**先 review spec 再做** — itsuki 醒来拍板范围。

### Bug B5: idempotency_key / card_uid 唯一索引缺失

**问题**：spec 要唯一索引防止重复，实装只是普通 index。

**Codex 执行 prompt**：
```
按 spec 在 models.py 给 rollcall_events 表的 idempotency_key 和 card_uid 加 unique index：
1. models.py 改 Index → UniqueConstraint
2. alembic migration
3. rollcall.py 的幂等检查改用 ON CONFLICT DO NOTHING
4. 加测试覆盖重复提交
```

### Bug B6: WebSocket + Redis 缺失

**问题**：文档要 WS + Redis pub/sub，v1 没装。多 iPad 实时同步未做。

**Codex 执行 prompt**：
```
评估实装 WebSocket + Redis 的工作量。先不实装，写一份 spike 报告：
1. 当前 polling 接口在哪些地方？
2. 改 WS 需要改多少处？
3. Redis 必须吗？还是可以用进程内 pub/sub？
4. v1.0 上线必须装吗？还是 v1.1 再做？
```

**严重性**：⚠️ 大改 — 先 spike 不实装。

### Bug B7-B12（中级，合并一个 prompt）

```
codex exec -s workspace-write -c model_reasoning_effort=high "
修以下 6 个中级 bug：

B7 refresh_token rotation 没实装 (schemas.py:57-61 / security.py:39-72)
B8 注册码 refresh 没限流 (admin_registration_code.py:90-156)
B9 登录失败不增 failed_count/lock_level (auth.py:40-44)
B10 学習欠席届不发邮件只写 NotificationLog (study.py:527-541)
B11 Alembic 不读 .env 固定读 ini SQLite URL (alembic/env.py:65-69)
B12 测试覆盖不够（route shadow / 账号删除 / 锁定升级 / WS / rollcall 边界）

每个 bug 修完跑 pytest 全绿后再做下一个。报告 diff + 测试结果。
"
```

### B13: 文档大量过期内容

**8 条过期**（README.md / BACKEND_DESIGN_LOG.md / v1/README.md），文档说 "v1 未开始 / 未实装"但实际已 6 个 router 在跑。

**Codex 执行 prompt**：
```
codex exec -s workspace-write "
按当前真实代码状态，更新以下文档：
- README.md:10 改'v1 未开始'为真实状态
- BACKEND_DESIGN_LOG.md:17 / :45 / :577 / :969 / :1071 — 删 ⏳ 改 ✅
- v1/README.md:22 / :129 / :133 — 改'未实装'为'已实装'
- 加'最后更新 2026-05-12'时间戳

跑 git diff 给我看。不动代码，只改文档。
"
```

### B14: 5 个生成物在 repo 内（.venv / .db / .pytest_cache）

**问题**：`.gitignore` 已写但文件已 commit。

**Codex 执行 prompt**：
```
codex exec -s workspace-write "
1. git ls-files | grep -E '\\.(db|pyc|pytest_cache)|/__pycache__/|/.venv/' 看哪些已被 track
2. 用 git rm --cached 取消跟踪（保留本地文件）
3. 确认 .gitignore 规则覆盖
4. git diff --cached 给我看
"
```

---

## 🔴 P0 — 03_ios 4 个上架风险（55/100）

### iOS-1: 注册门禁 `000000` bypass

**Codex 执行 prompt**：
```
codex exec -s workspace-write "
找 03_dev/student_ios 里所有 '000000' 引用 + 注册码 bypass 逻辑。
1. grep -rn '000000' 03_dev/student_ios/v1/
2. 如果是 dev bypass：加 #if DEBUG 包起来或用环境变量控制
3. 确保 Release build 不会包含 bypass
4. 报告改了哪些文件
"
```

### iOS-2: `project.yml` 签名配置

**Codex 执行 prompt**：
```
读 03_dev/student_ios/v1/project.yml，确认：
1. CODE_SIGN_STYLE / DEVELOPMENT_TEAM / PROVISIONING_PROFILE
2. Release config 不能用 development cert
3. 给上架 prep 写一份 checklist
```

### iOS-3: 注册字段编码

**Codex 执行 prompt**：
```
找 iOS 注册 view 里所有字段编码问题（汉字 / 日文输入 → URL encode）。
确保 POST /accounts 时字段编码正确。
跑 build + 测试注册流程。
```

### iOS-4: API baseURL 硬编码

**Codex 执行 prompt**：
```
grep -rn 'http://localhost\\|http://127' 03_dev/student_ios/v1/
所有硬编码 baseURL 改用 Info.plist 配置 + dev/prod 两套。
```

---

## 🔴 P0 — 03_device 代码空骨架（45/100）

### Device-1: `requirements.txt` 全是注释

**问题**：依赖列表都是 `# Adafruit-PN532` 注释格式，pip install 不装任何东西。

**Codex 执行 prompt**：
```
codex exec -s workspace-write "
按 ROLLCALL_DEVICE_DESIGN_LOG.md §1.1 拍板的技术栈，把 requirements.txt 改成真实依赖（去注释）：
- adafruit-circuitpython-pn532
- httpx[http2]
- websockets
- pyttsx3
- gpiozero
- structlog
- pytest

每个依赖加版本 pin。报告。
"
```

### Device-2: D1-D6 待拍板项

**Codex 执行 prompt**（其实是 CC 写给 itsuki 看的）：
```
ROLLCALL_DEVICE_DESIGN_LOG.md §10 列了 6 个待拍板：
D1 NFC 库 / D2 ST25DV 驱动 / D3 TTS / D4 SPI vs I2C / D5 WebSocket vs HTTP / D6 设备认证

CC 给推荐方案（已在 §10 标 🟡），等 itsuki 拍板后写代码。
```

### Device-3: main.py + 4 模块空骨架

**Codex 执行 prompt**（D1-D6 拍板后）：
```
codex exec -s workspace-write "
按 ROLLCALL_DEVICE_DESIGN_LOG §3 主循环 + §4 模块设计：
1. 写 src/main.py 状态机（IDLE → SUBMITTING → SUCCESS / FAIL → IDLE）
2. 写 src/nfc/pn532.py（读 NTAG215 UID）
3. 写 src/api/client.py（HTTP POST /rollcall/checkin）
4. 写 src/led/led.py（GPIO 状态机）
5. 写 src/audio/player.py（预录音频播放）
6. 写 1 个 smoke test
跑测试确认无依赖错误。
"
```

---

## 🟡 P1 — 00_admin 文档治理（CC 已识别）

详见 `2026-05-12_深夜大整理_总结报告.md` Step 1-7。

Codex 执行 prompt（整理脚本）：
```
codex exec -s workspace-write "
按 ~/dev/DMSD/05_logs/raw/2026-05-12_深夜大整理_总结报告.md Step 1-6 执行：
1. mkdir 99_archive/2026-05-12_深夜大整理/
2. git mv 5 个 00_admin 过期文件
3. git mv 2 个 iOS 文件改名 _archived_
4. git mv 7 个管理文档到正确目录
5. git mv AC 文件到 05_logs/AC_叙事/
6. 归档 progress_overview_draft
每步单独 commit。
"
```

---

## 🟡 P1 — 03_web NFC 演示风险

### Web-1: NFC 编号 `00`→`060218`

**问题**：codex 报告说 demo NFC 编号变了，**演示会失败**。

**Codex 执行 prompt**：
```
grep -rn '"00"\\|nfc_id.*00\\|card.*060218' 03_dev/teacher_web/
找 NFC 编号硬编码 → 统一改用 spec 真值 060218。
跑 demo 验证 NFC tap 触发对的学生。
```

### Web-2: demo vs v1 100% 重复

**Codex 执行 prompt**：
```
按 WEB_DESIGN_LOG.md 把 v1/src/components/_legacy/*.jsx 14 个 100% 复制的旧组件归到 v1/src/components/archive_legacy_demo/，加 README 说明"只读归档"。
```

---

## 🟡 P1 — 03_android 3 个闭环

```
codex exec -s workspace-write "
按 ANDROID_DESIGN_LOG.md 修 3 个闭环：
1. NFC 防作弊接入（card_uid 一致 iOS / backend）
2. 留学生审批链（帰国 chain）
3. 申请截止时间窗逻辑

每个加测试。先写 review 再实装。
"
```

---

## 🟢 P2 — 03_web 整体补 Tier 1 页面

按 WEB_DESIGN_LOG / system_features §7：外泊 / 帰省 / 帰国 / records / search / 健康申报 / 请假流程。

**预计**：单独开会话，多个 commit。

---

## 🟢 P2 — 文档索引补

按 05_logs codex 报告：
- decision_log.md 追到 5-11
- project_evolution.md 改成 v0.8.0 视角
- learning_path.md 补 4-10 后新学

**Codex prompt**：itsuki 手写区，CC 起 draft 不直写。

---

## 🟢 P2 — 99_archive 分层

按 99_archive codex 报告：把归档分"证据材料"vs"废弃源码快照"两层。

**预计**：单独开会话 + itsuki review。

---

## 🟢 P2 — 01_specs 二进制清理

把 `.pages` / `.docx` 历史快照移出冻结区到 99_archive。

---

## ⚪ codex review 互查工作流

修完每个 P0 bug 后，用第二个 codex 审查（防单 codex 出 bug 没人 catch）：

```
codex review --uncommitted "
重点审查：
1. 修的 bug 是否真修了
2. 有没有引入新 bug
3. 测试覆盖够不够
4. 跟 spec 对齐
"
```

---

## 🎯 itsuki 醒来 5 分钟操作指引

```bash
# 1. 看本文件 + 总结报告
cd ~/dev/DMSD
less 05_logs/raw/2026-05-12_问题清单_codex修复SOP.md  # 本文件
less 05_logs/raw/2026-05-12_深夜大整理_总结报告.md

# 2. 看项目级 codex 报告（如果跑完了）
cat /tmp/codex_report_project_level.md

# 3. 决定怎么修
# 选项 A: 让 codex 跑全部 P0（自动）
bash /tmp/codex_run_p0_all.sh  # CC 待会儿写
# 选项 B: 手动挑 codex prompt 跑
# 选项 C: itsuki 自己改

# 4. 不满意回档
git reset --hard pre-cleanup-2026-05-11
```

---

## ⏰ Cron job 提醒

- **cef9565e** — 5-12 早 7:10 fire — 重置 Anthropic 5h 限额 + 提醒检查整理进度

**END**
