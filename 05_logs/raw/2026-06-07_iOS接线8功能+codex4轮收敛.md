# 2026-06-07 iOS 接线（8 功能两端对齐）+ codex 4 轮对抗复审收敛

> 本会话由 itsuki 压缩后用 Claude Code 的 `/goal` 命令自主跑完。完成条件 = 8 个 iOS 学生界面的生产分支接真后端 + 正式版/演示版双 scheme `** BUILD SUCCEEDED **` + 每功能各一 commit。施工图 = `00_admin/handoff/iOS接线_GOAL提示词.md`。

## 起因
后端「iOS 缺的 6+1 功能」上一会话已实装（commit `d815995`，353 测试过）。「两端对齐」还差 iOS 这半 —— 8 类界面的生产分支（`#else`）还在读假数据 `SEED.*` / 提交只弹假 toast。本会话把这半接上，演示分支（`#if DEMO`）假数据全保留。

## 做了什么 —— 8 个功能各一 commit

| # | 功能 | 界面 | 接的后端 | commit |
|---|---|---|---|---|
| ① | 扫除历史 | MyCleanView | GET /cleaning/me | 54e9245 |
| ② | 个人信息修改 | MyInfoEditView | PATCH /students/me（只传改动字段 + 422 日语提示） | b3f1459 |
| ③ | 体调/欠席/其他 | Health/Absence/OtherSheet | POST /rollcall/reports | d59101a |
| ④ | 点歌 | MusicView/New/Detail | POST+GET /songs（通報/投票 v1.1 生产隐藏） | 5fced44 |
| ⑤ | 遗失物 | LostView/New/Detail | /lost-found（含本人 PATCH resolve） | eb5cecb |
| ⑥ | 修繕/来訪/代理 | GenericApplyForm | POST /misc-requests（parcel→proxy_receipt） | ae2aca7 |
| ⑦ | 点呼历史 | MyRollcallView/Detail | GET /students/{id}/profile rollcall_events | a417b56 |
| ⑧ | 减点明细 | MyPointsView/Chart | 同 profile 接口 demerit_events（⑦⑧共用 loadMyProfile） | 08d2b46 |

新建 6 个 endpoint 文件（Cleaning/Songs/LostFound/MiscRequests/StudentProfile + RollCall 内加 reports）+ AuthAPI.updateMe + AppStore 各缓存/loadXxx/myStudentId + RollCallDisplay 等归一视图模型（双 init 把演示日语枚举 vs 后端英语枚举翻译成同一套展示字段）。Route 的 homeMusicDetail/homeLostDetail 的 id 由 Int 改 String 以容纳后端 UUID（承宅配 homePackageDetail 先例）。

## codex 对抗复审 —— 4 轮跑到收敛（gpt-5.5 / xhigh / 只读）

按 codex-review skill：codex 只挑刺、不改文件；每条发现 CC 独立核实真代码再裁决（不盲信）；修完 CC 自己 `xcodebuild` 双 scheme 验证（codex 沙箱编译不了 iOS）。

- **R1**：6 条（0 阻塞/4 重大/2 次要）。采纳 4（修）：owner 比对大小写、loadMyProfile 冷启动不重试、guest/parcel 必填、MusicDetail 空状态。驳回/缓办 2（major-1/3，见下）。→ commit 219fa1e
- **R2**：R1 的 4 修全确认到位；新报「9 处提交流缺令牌守卫」。我范围内 8 处全补（最严重 = saveAndLog await 后写共享 currentUser/SEED.user，切账号会污染新用户显示）。→ commit b6bdc00
- **R3**：8 处守卫确认；揪出 3 处 Community 是双 await（create 后还 await loadXxx），只守了第一个；且 resolve 的 guard 提前 return 跳过 `resolving=false`（按钮永久禁用）。各补第二守卫 + 改 defer。→ commit cb412f9
- **R4**：3 处全确认到位，本次范围内**0 新发现**。结论原话「本次 8 功能改动已收敛（改动范围内 0 blocker + 0 major）」。

## 验证
- 正式版 `TomoshibiApp` + 演示版 `TomoshibiAppDemo` 双 scheme `** BUILD SUCCEEDED **`（每轮修完都重编）。
- git log：8 功能各一 commit + 3 轮 codex 修复 commit，全部本地，**未 push**。

## 留给 itsuki 拍板的 4 项（codex 报了但我没连夜改 —— 都是早就存在/超本次 8 界面范围）
1. **major-1**：首页 `HomeStubs` 的音乐/遗失物**预览卡**生产版仍读 `SEED.songs`/`SEED.lost`（假数据泄漏到生产首页）。早就存在、不在本次 8 界面内。
2. **major-3**：`MyPage` landing「今月の点呼」**汇总卡**（约 line 386）生产读 `SEED.rollcall`。同上。
3. **submitOuting**（外出申请，GenericApplyForm 里、早就存在、非本次改动）同样缺令牌守卫。
4. **minor**：音乐/遗失物/点呼详情按 id 查表 `uuidString == id` 大小写敏感 —— 当前 app 内导航两边同源（都 Swift 大写）不触发，纯防御性（防未来深链/推送传后端小写 id）。

> 这 4 项都不影响本次 8 功能（codex R4 已确认本次范围收敛）。1/2 是「首页/landing 的预览卡也该一起接真后端」的范围扩张决定；3 是要不要顺手补早存在代码；4 是防御性加固。建议但需 itsuki 定。

## AC 价值
- 模式 2（多 AI 对抗复审）顶级：codex 4 轮、每轮都真挖出我没看到的问题（双 await 漏守卫、按钮卡死都是 R3 才暴露），且我每条都核实前提再裁决（owner 大小写是真 bug 采纳 / submitOuting 超范围缓办 / UUID minor 是不触发的假设缓办）。
- 教训复用：「派 agent 干活必须独立验证它的自报」「codex 编译不了 iOS、CC 必须自己 xcodebuild」「转述 AI 结论前审前提」全部在本会话实践。
- 「演示/生产双轨」工程模式（`#if DEMO` + 双 init 归一视图模型）跑通 8 次，是「给管理员演示用假数据讲叙事 + 真上线用真后端」一套代码两用的成熟范式。
