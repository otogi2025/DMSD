# 2026-06-03 演示版逐屏打磨 + 出租车预约 4 端功能（itsuki 逐屏审 + 过夜自主）

> by [Opus 4.8 1M]。本会话从「itsuki 实机逐屏看 iOS 演示版揪问题」一路推进到「出租车预约 4 端实装 + codex 5.5 xhigh 审查收敛」。itsuki 中途说要睡，授权 CC 全自主做完 + codex 审 + 收尾，期间不打断。

## 一、起因 + 全过程

itsuki 实机打开 iOS 演示版，一屏一屏看，揪出一批问题，逐条让 CC 改：

### 第一批：通知 / 主页演示假数据
- 通知页去 Amazon 品牌（「Amazon 荷物到着」→「荷物到着」）、寮管理員室→寮管室前、外泊承认去「田中先生」具名、活动「新入生歓迎会/食堂」→「誕生日会/カフェテリア」
- 主页宅配卡片 + 包裹确认页的 Amazon 也一起去掉
- 全在 `SEED.swift`（演示假数据）+ `HomeStubs.swift` + `ApplyStubs.swift`

### 第二批：页面切换黑屏
- 切页面一瞬黑屏。根因：`RootView.swift` 的 `.transition(.opacity)` + `.animation(value: router.current)` —— 淡入淡出中间帧两页都半透明，露出底层 `systemBackground`（深色=黑）。删掉两行改瞬时硬切，底部导航胶囊 morph 动画独立不受影响。

### 第三批：外出表单（先删字段，后被 itsuki 改回）
- itsuki 先说外出删交通手段 / 时间，CC 删了；后又改主意「外出可以选出行方式 + 预约出租车」→ CC 撤回交通手段删除。外出表单最终：删日期/帰寮時刻（按提交时刻算）+ 联系方式预填学生电话可改 + 行き先真必填 + 交通手段保留。

### 第四批：出寮届表单对齐两张真实纸质表（様式3-1 帰国許可願 / 様式3-2 外泊許可願）
itsuki 提供真实纸质表照片，逐条对：
- §3「出寮（寮を出る）」/§4「帰寮（寮に戻る）」删多余括号
- 「滞在先」→「宿泊先」（実物用宿泊先；iOS 漂移了）
- 交通方法从单一共用列表**拆成出寮/帰寮两串不同选项**（实物去程/回程班次不同）+ 删「飛行機」（走帰国飞机段单独填）+「教員送迎」→「教員」+ 加「寮生特別運行」
- 删早帰（return）/その他（other）申请类型 + 删 4 处界面假 ID 显示 + 删 2 条矛盾假数据（早帰点进去显示成帰国届）
- 帰国隐藏「行先（都市名）」只填宿泊先住所

### 第五批：出租车预约 4 端新功能（itsuki 改主意「上线预约出租车」）
- 后端：`applications.taxi_reservation_time`（Time/nullable）三种出寮届+外出共通 + migration + schema + router + _to_application_out 映射 + 2 测试
- iOS：`StayForm` 加开关+时刻 state + UI section + 三种提交带 taxi + 详情显示；外出 UI 桩
- 老师网页：预留的「タクシー」tab 从骨架实装成真列表（OutstayList 复用 + badge 防漏看）+ 详情字段
- Android：判断记 TODO（骨架未接后端 + 无法 gradle 验证，不做无法验证的代码）

## 二、codex 5.5 xhigh 审查 → CC 独立核实 → 修复

itsuki 要求做完派 codex 审查 + 修问题。codex 审出 1 阻塞 + 3 建议，CC 逐条独立核实（不盲信）：

- 🔴 **migration revision 撞车**（真）：CC 瞎编的迁移编号 `e3f4a5b6c7d8` 正好撞既有 `add_events_and_bus_routes`。换 `a7b8c9d0e1f2` + 重命名文件 + `alembic heads` 验证单 head。**这是本会话最实的一个 bug**。
- 🟡 **帰国教师详情误显行先都市**（真）：teacher_web 帰国详情 fallback `app.city`（=宿泊先）会把宿泊先误显示成行先都市 → 帰国隐藏该行 + 去 fallback。
- 🟡 **修改届 taxi 名义支持**（真）：Swift nil 省略 + 后端 exclude_none 丢 null + StayEditForm 无 UI → 改成明确 create-only（移除 update body taxi 字段）+ 记 TODO 三态待办。
- 🟢 **taxi tab 共享 sub filter**：切 tab 重置 sub。

CC 也踩了自己的坑并自我纠正：中途一次「编译成功」其实是假的 —— `cd` 命令把工作目录挪走，编译没跑成，CC 误把管道末尾 grep 的退出码当编译结果（被 itsuki 教训过的同类陷阱又踩）。用绝对项目路径重编才真 BUILD SUCCEEDED。

## 三、最终验证（全绿）
- 后端：223 测试通过（含 2 个新 taxi 测试：带预约回显 / 不带默认 null）
- iOS：演示 + 正式双 scheme BUILD SUCCEEDED
- 老师网页：check_jsx 16 块 0 错误
- alembic：单 head `a7b8c9d0e1f2`

## 四、AC 素材

- **模式 2（AI 方案崩→核验→真做）**：codex 审出 migration 撞车 + 帰国误显示，CC 独立核实全属实再修；CC 自己编译陷阱自我纠正。
- **模式 5（认知/学习）**：itsuki 用真实纸质凭证（様式3-1/3-2）驱动表单设计 —— 需求工程「真实凭证 > 凭空设计」；出寮/帰寮班次去程回程不同这种业务细节只有看实物才知道。
- **模式 6（取舍）**：① CC 判断「后端不用改」（交通方法是自由文本字段不是枚举）—— 没为迎合 itsuki「前端改后端要懂」瞎改后端，用代码证据说明。② Android 判断记 TODO 不做无法验证的代码。③ 出租车 create-only vs 三态的范围取舍。
- **itsuki 主体性**：逐屏实机审揪出一堆自相矛盾的假数据（早帰点进去是帰国届、宿泊先写晚点呼），对「不符合宿舍真实情况」的细节零容忍。
- **需求演化**：外出交通手段「删→改回」+ 出租车从「先记 TODO」→「上线实装」，itsuki 边看边调。
- **联动意识**：itsuki 主动提醒「前端改了后端也要懂，那个联动 skill/hook 你没看到吗」—— CC 之前光改 UI 没处理联动提醒，被点出后走 file-linkage skill 补全。

## 五、教训
- migration 编号不能瞎编（要 grep 现有 + alembic heads 验证），CC 这次瞎编正好撞车，codex 逮到。
- 管道 exit code 陷阱（`xcodebuild | grep` 的退出码是 grep 的）+ `cd` 挪工作目录导致编译没真跑 —— 编译验证必须读真实 BUILD SUCCEEDED 输出 + 用绝对路径。
