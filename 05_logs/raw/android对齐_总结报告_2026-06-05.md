# Android 学生 App 对齐 iOS — 本会话总结报告

> **日期**：2026-06-05
> **任务**：把 Android 学生 App（`03_dev/student_android/v1/`）按 `00_admin/iOS_Android_对齐规格.md` 对齐到 iOS。
> **范围现实**：网络层=零、约 60 个功能屏（41 全缺 + 32 半成品），全套对齐是几十小时工程。本会话在 40 轮刹车内做**摸底 + 地基 + 高价值屏**，其余逐屏记 TODO。

---

## 一、本会话产出（全部 gradle 编译验证退出码 0）

### 1. 摸底（终态 #1）
- 产出 `05_logs/raw/android对齐_差异矩阵.md`：60 个功能屏逐个「有/半成品/缺」+ 关键差距。统计 **有 4 / 半成品 ~32 / 缺 ~41**。
- 三个地基级结构性缺口：网络层=零、共享组件库=零、自研导航栈=零。

### 2. 网络层字段对照（终态 #4）
- 产出 `05_logs/raw/android对齐_网络层字段对照.md`：30 个学生端端点路径 **100% 一致**、字段全 snake_case 一致。
- ⚠️ 发现 1 个真矛盾：**密码长度**后端 `min_length=8` vs iOS 本地校验「6〜128」→ 待 itsuki 决定是否统一成 8。
- 2 处仅响应类名别名不同（StudentMeOut=后端 StudentProfileBasic / EventOut=后端 DormEventOut），字段相同。

### 3. 地基（终态 #3 的一部分）
- 新建 `ui/theme/Dimens.kt`：`SuzuDim` 尺寸常量（圆角 xs/sm/md/lg + 间距 4-32），对齐 iOS `T.Radius`/`T.Space`。
- 新建 `ui/components/SuzuAtoms.kt`：16 个中央共享组件（Card/Pill/Avatar/SectionHeader/PrimaryButton/GhostButton/Field/TField/TArea/RadioCard/ChipGroup/EmptyState/TToggle/PageHeader），照规格 4.1–4.16 实装。各屏不再内联重写。

### 4. 逐屏对齐（终态 #2）
| 屏名 | 截图 | 状态 | 改了什么 |
|---|---|---|---|
| ホーム主页 | 01/06 | ✅ 已对齐 | 问候行改「おかえり、X さん」+ JST 当天日期 +铃铛未读 badge；5 卡文案对齐(次のバス便/宅配便·N件未受取/今週の活動·N件/リクエスト曲·N件/遺失物·最新)；图标改浅底彩色 |
| 申し込み列表 | 08 | ✅ 已对齐 | tab 选中改青底白字(原黑底)；FAB 改圆角方 primary(原圆)；卡改「图标块+类型名+徽章/摘要/细线+日期」；空态「申請はありません」；徽章「差戻」 |
| 新規申請宫格 | 09/10 | ✅ 已对齐 | 12 类型(原 10)，英文 key + Material 图标 + 日语副标题逐字照抄 iOS APPLY_TYPES |
| 宅配一覧 | 03/04 | ✅ 已对齐 | PageHeader「宅配」+ 待領/領済 segmented tab + 📦 卡(发货方/日期·追跡番号) + 待領「受取」按钮 + 空态「なし」；加 PackageItem 模型 + DEFAULT_PACKAGES 假数据 |

---

## 二、收尾数字

- **本会话新对齐**：1 地基层（16 组件 + 尺寸常量）+ **4 屏**（主页 / 申し込み列表 / 新規申請宫格 / 宅配一覧）
- **之前已基本对齐**（摸底标「有」）：减点 amber 卡 / 遺失物卡 / RollCall 履历 ≈ 3 屏
- **记 TODO**：约 **53 屏**（含 41 个全缺 + 部分半成品未及）
- **总**：约 **60 个真实功能屏**

> 即：累计基本对齐 ≈ 7 屏 / 记 TODO ≈ 53 屏 / 总 ≈ 60 屏。本会话推进了地基（最大杠杆，后续屏靠它拼装）。

---

## 三、记 TODO（按优先级，下次施工接力）

### 全局地基（最高优先，挡住所有屏）
- **N-AND-NET 网络层从零建**：HTTP 客户端 + 错误处理(ApiError sealed class) + 日期适配器 + token 存储(EncryptedSharedPreferences) + 10 组端点 + ~40 数据类。照网络层字段对照报告 + 规格第 3 章。**几乎所有半成品屏的根因。**
- **N-AND-NAV 自研导航栈**：Routes 22→~60 + displayName/分支判断属性；RouterStore(go/back/replace/jump) + 全局浮层(sheet/面包屑/toast) + 用 PageHeader 统一所有子页头部(现 Home/申し込み仍用 26sp 大标题，应改 17sp PageHeader)。
- **玻璃组件降级**：GlassCard/GlassSheet/GlassBackdrop。

### 半成品屏补全
- 主页：amber 减点卡右上白色径向装饰圆斑；学年更新「待更新」横幅(needsRenewal)；巴士卡「挑下一班」JST 实时逻辑。
- 申し込み：新規申請改独立全屏页(现底部弹层)；列表接 `GET /applications/mine`；状态枚举补 draft/approved_partial/withdrawn。
- 宅配：宅配詳細页(加路由 homePackageDetail)。
- 登录/注册：Login 番号/邮箱双 tab + 登录 API + 401 锁定；Register Step5 認証コード(全缺)；Lockout 锁定页(全缺)。
- 杂项：BusList 日别分组+筛选+空港 banner；リクエスト曲 hint banner + 通報 4 radio；行事予定月历网格；通知 Pill tone 映射；お知らせ公告(接后端)。
- マイページ：11 个 L2 子页全缺(個人情報/連絡先編集/点呼履歴/点呼詳細/減点グラフ/処分履歴/体調報告/掃除提出/荷物受取/学習履歴/Tomoshibi について)；着陸页 3 状态卡。
- 减点：入口卡 PointsStatusCard；減点グラフ 12 月折线图。
- 点呼/NFC：TopRollBar 四态条(现是减点卡)；RollcallSheet scanning/success/fail 三步动画；StudyCheckinSheet；FeedbackSheet 三选一；中央 FAB 震动反馈。

### 防作弊核心（后端未写，遇到只记 TODO 不动）
- 点呼真实 NFC 签到链路依赖 nonce / ECDSA 签名 / 设备注册 / 卡→学生映射 —— 后端一行没写。Android 侧点呼屏只做 UI + 动画。

### 待 itsuki 决策
- 密码长度：后端 8 vs iOS 6，是否统一成 8（三端 + 后端）。

---

## 四、验证方式说明
- 每改一批真跑 `./gradlew assembleDebug`，退出码 0（非语法解析）。
- 每屏对照规格逐字 + 对照 itsuki 提供的 14 张 iOS 实机截图肉眼核布局/配色/文案。
- 中文铁律：新增代码注释 100% 中文，UI 字符串保持日语，引用 UI 文字用「」括起来（hook 实时拦截 + 已修正）。
