# 2026-06-04 raw 日志

## iOS 学生端两处改动 + codex 审查闭环

### 起因
itsuki 实机逐屏审 iOS 演示，逮到两个问题：
1. 外泊申请表单里出租车预约是底部一个独立开关框，要先打开开关才出时刻选择器 —— 想改成「出寮方法选了タクシー就当场出时刻」。
2. 登录页账号密码空着点登录，弹的提示是「通信エラー（通信错误请检查信号）」，跟「我根本没填」对不上、误导。

### 做了什么
- 出租车：删独立开关框，改成出寮方法选「タクシー」时在出寮卡片内联露出时刻选择器；只管出寮不管帰寮（后端只有一个 taxi_reservation_time 字段）。
- 登录：加空字段校验，空着就提示「请输入账号密码」、不再发请求。
- 4 个文档同步（iOS/Android 设计日志 + 共用真值表）。

### AC 价值点 — AI 给建议，我评估后部分采纳部分改造
派 codex（gpt-5.5 最高强度）审我的改动，它挑出 2 个问题：
- 账号密码空检查 trim 了空格判空，但发给后端的是没 trim 的原值 → 带空格的账号会被后端拒。codex 建议「账号密码都 trim 后发」。
- 「タクシー」字符串散在 3 处，将来改文案易漏改。

**我没全照 codex 说的做**：
- 账号确实该 trim（6 桁学号、空格永远非法）→ 采纳。
- 但**密码我坚持不 trim、原样发** —— 密码里用户故意打的空格也算密码内容，砍掉可能砍掉真实密码的一部分。我先去读了后端 schemas.py 确认密码只校验长度 6–128、不 strip，佐证了我的判断。
- 把这个分歧 + 我的理由喂回给 codex 复审，codex 复审同意我的取舍。

「タクシー」抽成常量这条采纳了 —— 这是「机制兜底优于自律」，与其靠以后记得三处一起改，不如用一个常量从结构上杜绝漂移。

### 反思
codex 的建议不能照单全收。它对「trim」的建议是对称的（账号密码一视同仁），但安全语义上账号和密码不一样。判断的依据不是「codex 说了算」，而是回到一手来源（后端 schemas.py 的实际校验规则）去验证。

### 验证
正式版 + 演示版双 scheme `BUILD SUCCEEDED`；codex 复审 0 未解决真问题。

### 顺带发现
- 演示数据消失不是 bug：itsuki 选了正式版 scheme（字段故意全空），换演示版 scheme 即恢复。
- 工作区同时有多个并行会话的改动混在一起（巴士时刻表会话 / backend / Android / teacher_web），共享文件互相交错 —— git commit 没法干净地只提交单一会话的活。

---

## 遗失物场所修正 + 匿名建議五端删除 + CC 编造功能名翻车（另一会话，主体非出租车那轮）

### 任务1 遗失物场所
itsuki 实机看 iOS 遗失物界面，指出「食堂」「ロビー(大堂)」不该当宿舍遗失物地点（宿舍只管宿舍内遗失物）。拍板：食堂→玄関、ロビー 全删（宿舍没大堂，连掃除区域固定列表 + 行事「寮ロビー集合」一起清）。5 端 14 处，iOS xcodebuild 过 + 后端 223 测试过。teacher 后台输入位置功能其实早就有（FrontDeskItem.location 自由文本），只清了示例文案。

### 任务2 匿名建議五端删除
itsuki 看到我提「匿名建議」愣住「我哪里有这功能」。查实：匿名建議 4-29 就拍板砍了，文档记了但 iOS/Android 代码一直没删 —— 跟宿舍墙「文档砍了代码漂一个月」同一个病第二次犯。拍板彻底删：五端代码+路由+假数据+类型+文档全清（iOS 6 文件 + Android 5 文件含整屏 FeedbackScreen + 2 设计日志）。

### ⚠️ CC 翻车：把桩代码/demo 当成 itsuki 真有的功能（连环 2 次）
这次最该记的不是删除，是我连环翻车：
1. 我说「意见箱里的食堂分类」→ itsuki「我哪里有意见箱」。我看到 CommunityStubs.swift 一行 cats=["食堂",...] 没读它属于哪个功能，脑补了「意见箱」这个名字。
2. 我改口说「匿名建議」→ itsuki 更怒「我他妈哪里又有匿名建议」。我又是看到代码里 PageHeader(title:"匿名建議") 就当成 itsuki 拥有/规划的功能汇报。
根因：把「代码文件里存在某段」当成「itsuki 确认要做的功能」来陈述。CommunityStubs.swift 文件名带 Stubs（占位/演示桩代码），里面一堆早期堆的、itsuki 没规划的演示界面。
itsuki 治理：连续两次直接怼到我承认认知边界 —— 逼我区分「代码里有」≠「用户的功能」。

### codex 审查闭环（itsuki 要求：审→修→复审→收敛，且不盲信 codex）
codex(gpt-5.5 xhigh) 审两任务，找 4 问题：
- ①(中) Android 删 AppState.feedback 字段后，旧本地存档严格解码会失败回退、丢本地数据。
- ②(低) iOS 设计日志数字 18→13。
- ③④(低) Routes.kt / NavGraph.kt 文件头注释「23」漏改。
我裁决：①③④ 修（① 加 Json{ignoreUnknownKeys=true} 共享编解码器，删字段不再触发回退）；②不改 —— 那是 §7 v2 设计页面清单口径，跟当前代码 view 数不是一个体系，增量 15-2=13 自洽。codex 复审：①③④ CORRECT、②AGREE DO NOT FIX、无新问题。
不盲信点：② 我顶住 codex 的「应是 12」，给口径理由，复审它认了。

### 验证缺口
Android 本机没装 SDK，跑不了完整 gradle 编译。靠 codex 把关 Kotlin（删干净 + appJson 正确 + when 穷尽）+ 静态结构配平。完整编译待有 Android 环境补。

### git
工作区多会话叠加，混合文件（backend models.py/schemas.py、teacher_web index.html）没法干净拆单会话提交 → 没擅自 commit，留 itsuki 统一处理。

---

# 会话：オンライン学習 契約書文件上传实装（[Mac-Opus 4.8] 另一会话）

## 起因
itsuki 启动后看 iOS「オンライン学習申請」界面（截图），说「オンライン学習要可以上传图片，上传合同」。追问确认：生产版（非 demo）/ 照片 + PDF 都要 / 后端前端一起改 / 选择时底部弹窗选「照片 or 文件」（「很多 iosapp 都带这个」）/ 老师要能在学生个人页查看历史合同。

## 做了什么（三端）
- **后端**：`StudyOnlineRequest` 加 4 列（合同路径/原名/类型/大小）+ migration `c9d0e1f2a3b4`；新上传端点 `POST /study/online-requests/{id}/contract`（仅本人 + 仅 pending）+ 下载端点（学生本人 OR 老师受 R4 寮边界）；文件存磁盘 `upload_dir/contracts/<申请id>.<ext>`、DB 只存元数据、存盘名用申请 id 防路径穿越；类型白名单 + 10MB 限。学生档案接口加在线学习+合同子块。
- **iOS**：新 `ContractFilePicker.swift` 底部弹窗（拍照 UIImagePickerController / 相册 photosPicker / 选文件 fileImporter）+ 图片统一转 JPEG（HEIC 浏览器不兼容）；`APIClient` 加手搓 multipart 上传；表单两步提交。
- **teacher_web**：学生档案弹窗加「オンライン学習」tab + 「契約書を見る」按钮（fetch 带 token → blob → 开新窗口）。

## codex 5.5 xhigh 审查（itsuki 要求「不要 codex 说什么就是什么，你也要有自己的思考」）
codex 报 2 阻塞 + 5 重要 + 3 建议。CC 自己核实后分档：
- **当场修**（安全/正确性、改动小且稳）：① 老师列表端点补 R4 寮过滤——CC 加的文件名字段让跨寮老师能从列表看到别寮学生合同文件名（含姓名），真回归，加了寮边界测试锁住 ② 写文件→commit→删旧文件 顺序改原子 ③ 文件名 `_safe_filename` 去控制字符+限长防 header 注入 ④ iOS multipart 文件名去 CR/LF/引号 ⑤ 提交按钮 isSubmitting 防连点 ⑥ 相机 isSourceTypeAvailable 检查防模拟器崩 ⑦ teacher_web 弹窗先同步开窗防拦截。
- **记 TODO**（产品决策 / 大改）：契約書是否强制（codex 当阻塞，CC 判断这是 itsuki 的产品决策、原字段就是「任意」、不擅自改必填）/ 第二步失败补传入口 / 学生看自己合同预览 / 大文件内存放大 v1.1 / magic bytes v1.1 / Android 端。

## 验证
后端 pytest 64 passed（含新列表寮边界测试）；iOS 正式版+演示版双 scheme `generic/platform=iOS Simulator` BUILD SUCCEEDED；teacher_web check_jsx 16 块 0 错误。

⚠️ 中途翻车一次：第一次 iOS 编译用了不存在的模拟器名「iPhone 16」，xcodebuild 没真跑只吐设备列表，但 bash 管道末尾 tail 的退出码 0 把我骗了（pytest-exitcode 同类陷阱）。改用 `generic/platform=iOS Simulator` 真编译才确认绿。

## git（跟上面别会话同样的多会话叠加问题，但这次拆开了）
工作区混着并发会话的巴士功能（BusRouteOut/BusAPI）+ 清扫/忘れ物改动。本会话用 `git apply --cached` 按 hunk 过滤，把 models.py/schemas.py/NetworkModels.swift/index.html 里只属于合同功能的改动块挑出来入暂存，干净文件整文件加，最后核对暂存区无巴士/清扫关键词混入。pbxproj 被巴士污染没提交（靠 project.yml + 下次 xcodegen 重生成）。

---

## 项目协作机制讨论 — 启动 skill vs hook / 心智模型与 WIP 职责 / 建 codex-review skill by [Opus 4.8 1M]

### 起因
itsuki 主动反思自己搭的项目协作机制，连问三件事：启动要不要从 skill 改 hook、WIP 和心智模型是不是冗余、以及把高频「派 codex 审查」固定成 skill。

### 经过 + 关键判断

**① 启动：skill vs hook（模式 6 取舍 + 模式 5 认知）**
itsuki 先问「是不是该从 skill 改 hook，强制每次会话开始就启动，正常对话不被影响」。CC 解释 hook=机器自动跑、skill=靠 CC 自觉，并指出 hook 替不了「读懂+判断+报告」只能注入数据。讨论到一半 itsuki 自己想通了：

> "如果我每次开新的会话就说启动的话，那其实根本就不需要 Hook 了，Skill 就可以代替 Hook 全部的功能了。"

CC 确认这个判断对——差别只剩「靠你记得说启动」vs「机器强制」。itsuki 拍板维持 skill 现状不上 hook。**AC 点**：他把「机制兜底优于自律」这条自己定的铁律的适用边界想清楚了——铁律针对的是「CC 反复忘」，触发权收回自己手里后就不适用。

**② 心智模型 vs WIP 职责（模式 5 认知改变，顶级）**
itsuki 觉得两个文件功能「完全一样」想融合。CC 不附和，指出只有「5 端现状」一块重叠，其余不重叠，且变化速度不同（心智模型=慢变量/架构骨架，WIP=快变量/流水账），用「把变化频率不同的东西分开放」的工程原则解释为什么不该整文件合并。itsuki 追问：

> "但是说实话，当前焦点这个不应该交给 To do 去做吗？我还是不太了解 WIP 到底是干什么的。"

CC 用时间轴讲清三文件：心智模型=现在长什么样（无时间）/ WIP=最近做了啥+当下（朝后看）/ TODO=将来要做啥（朝前看）；WIP 本质是「会话之间的交接棒」。itsuki 拍板：当前焦点留 WIP，砍掉心智模型 §4 跟 WIP 的重叠。**AC 点**：itsuki 自己发现机制冗余 → CC 诊断真因（重叠只在一处+快慢变量混放）→ 落地（§4 只标成熟度档位、细节归 WIP，连带治好「§4 老过期漂移」）。

**③ 建 codex-review skill（模式 2 不盲信 AI 制度化 + 模式 6）**
itsuki 给一段提示词要把「派 codex gpt-5.5 xhigh 审我做的所有事→我修→复审→收敛」做成 skill，并主动问「你看这提示词还有什么不足？」。CC 评审补 5 个洞（审查范围要明确/收敛判据+死循环/codex 只读修由 CC/修完 CC 自己验证/逐条裁决落地），查实 codex 配置默认就是 gpt-5.5+xhigh、按 itsuki 定的规矩问触发词不自己编。itsuki 拍板：范围=本会话改动、不设轮次上限跑到收敛、不带「codex」字样不触发。

### itsuki 原话 ⭐
> "我最初搭建 WIP 是希望新的会话知道之前的会话做了什么。然后我又创建心智模型，是因为想让 AI 新的会话马上就可以理解我整个项目是什么……简单地说，快速地消耗少量的 token 就理解我整个项目。但实际上想一想，这两个功能完全一样的。我觉得可以融合，你怎么看？"

### AC 价值 ⭐
- 对应核心问题：#5 自己认识（元认知：主动审视自己搭的协作机制是否冗余/最优）、#2 技术判断
- 模式 5（认知改变，顶级）×2：想通 skill 能代替 hook、想通两文件职责边界
- 模式 6（取舍）×3：不上 hook / 不整文件合并 / codex skill 各项规则拍板
- 模式 2（不盲信 AI）：把「不盲信 codex」做成制度化 skill，且主动要 CC 挑提示词的刺
- 展示：itsuki 不只是用工具，而是反过来设计和优化「AI 怎么跟我协作」的元机制——这是他独有的「通过 AI 协作成长」主线的高阶形态

### 工程产出
- 改 `00_admin/项目心智模型.md` §4（四列→两列，只标成熟度+一句话定位，防作弊大缺口单独强调）+ 顶部更新 + §7 维护说明
- 改 `.claude/skills/session-wrap/SKILL.md` 项 12（对齐：只在跳档时更新 §4）
- 新建 `.claude/skills/codex-review/SKILL.md`（7 步流程+5 红线）+ CLAUDE.md skill 表 + project-overview 清单登记

#AC候选 #认知改变 #技术判断 #方法论 #设计决策

---

## AC 信号 (20:07) [外部 AI 评估 / 面试预演]

**原话/概要**：itsuki 跟外部 AI（措辞像 Gemini）讨论服务器自主组装时，AI 反过来给出两段「DMSD 项目 AC 竞争力评估 + 面试预警」（① 自主组装为何在答辩有压倒性优势 ② 三个现实依据 + 残酷前提 + 3 个教授拷问）。itsuki 觉得有价值（原话「这个格局瞬间就打开了」），主动要求记录。

**上下文**：这不是 itsuki 主体性素材，是外部 AI 对项目的评价。CC 审前提后标了 5 处不能照单全收：SFC（庆应湘南藤泽校区）≠ 筑波被混着夸 / 「大概率摸过 Nginx-Apache」是 AI 推测非事实 / 服务器 ECC 配置还在砍非定案 / 量化交易数学深度别夸大 / AI 自点「代码要你自己敲 + 扛得住拷问」前提最该正视。最有价值的是末尾 3 个教授拷问（白板画服务器架构图 / 高并发数据库死锁 / 点呼机对比成熟打卡系统优越在哪）= 面试模拟题。

**归属候选**：AI 评价我项目的 AC 竞争力，我主动收藏要求记录；CC 审了前提、标出不能直接抄进文书的地方，并把 3 个教授拷问拎出来当面试题库。深度记录见 AC 高权重区 `_priority_itsuki主动提报/2026-06-04_外部AI项目AC评估+面试拷问.md`

---

## 工程操作 (20:30) — 统一两个 AC 升学根目录

### 起因
itsuki 要「整理 AC 素材 + 完善升学文件夹结构」（已进 TODO 顶部高优先段）。让 CC 先列真实结构再给方案。

### 诊断
iCloud `升学/` 下有两个独立真目录：`大学入試/`（主力，219 文件，全部 AC 工作 + 雷达收集箱 + 流水线 + 产出）vs `AC/`（残缺空壳，只有 19 个会话存档）。根因：`daily-archive-cp.sh`（每天把会话 jsonl 归档到 iCloud 的脚本）第 14 行 + `ac-tsukuba-session-start.sh`（会话启动 hook）第 19 行，根路径写死 `AC/`，跟雷达收集箱那套用的 `大学入試/` 根分了家、各写各的越走越叉。

### itsuki 拍板的约束 ⭐
「文件只能移动归档但是不准删」—— 移动可逆、删不可逆，他守住数据安全底线，宁可留空壳也不执行删除。

### 做了什么（选方案 b：统一到主力根 大学入試/）
1. 移 19 个文件（会话总结 8 + 对话原文 11）`AC/` → `大学入試/.../03_素材_候选/` 同名子目录，用移动 mv 不用删除 rm，逐个查重名防覆盖，0 冲突。
2. 改 2 个脚本根路径 `AC/` → `大学入試/`（第 14 行 / 第 19 行）。
3. 对齐 2 个活文档旧路径：memory `user_ac_profile.md` 第 62 行（合格者分析 html 路径错指 `AC/`，实际在主力侧）+ `04_ops/MAC_MINI_SETUP.md` 第 302 行健康检查命令。
4. `AC/` 空壳保留（itsuki 不准删，里面已空）。
5. 历史快照（jsonl / file-history / backups / 旧 raw / INSTALL.md 一次性安装命令）不改 —— 只读历史，篡改无意义。

### 验证
全局 `grep 升学/AC/`：活动脚本 / 配置 0 命中，剩下全是历史快照。两个脚本第 14 / 19 行确认已指向 `大学入試/`。

### AC 价值
itsuki 主动发现自己搭的文件体系有结构债（同一项目裂成两个根）+ 拍板「只移不删」的数据安全原则。#项目演化 #元认知 #数据安全

---

## 工程操作 (晚续) — 升学素材库「两校共用池」大重构

### 起因
itsuki 连环追问暴露旧 AC 素材体系两个设计缺陷：
1. 「radar inbox 是不是跟素材候选冲突了？我从来没往素材候选里手动放过素材」→ 发现旧四级流水线（06_radar_inbox→03_素材_候选→04_素材_成品→05_产出）的「人工精选」闸门从没运转过。两个自动脚本（ac-radar 写 06、daily-archive 写 03）从两端往里灌，06 和 03 职责重叠。
2. 「庆应和筑波都要申请，原始素材应该是通用的吧」→ 要同时申请筑波 AC + 庆应 SFC AO，旧结构两校各一套独立流水线，原始素材要各存一份、重复漂移。

### itsuki 的核心洞察 ⭐⭐
「原始素材两校通用，分叉发生在产出端」—— 正是软件工程「单一数据源 + 多视图」思想。同一段经历（如自己组装服务器），筑波按"数理深度"提炼、庆应按"解决实体问题"提炼，原始一份、提炼两角度。他自己悟出来的。

### 做了什么（7 步，全程只移不删，旧物归档不删）
1. 建 iCloud 根统一归档区 `99_归档/` + 共用池骨架 `00_原始素材池_两校共用/`
2. 迁 75 个原始素材进共用池（信号碎片 18 + 主动提报 6 + 会话存档 38 + 素材卡片 13）
3. 盘点筑波 04_素材_成品 = `_v2_归档` 31 个早期素材 + 5 空目录 → 整体归档
4. 筑波侧瘦身重组（新建 03_按筑波口味提炼 / 05_产出→04_产出 / 证据截图→99_证据截图 / 旧壳归档）
5. 庆应侧空架子按新结构重建
6. 改 6 个脚本指向共用池（append_to_scratchpad.py / daily-archive-cp.sh / ac-tsukuba-session-start.sh 重写 / startup_check.py / ac-tsukuba-pre-write-protected.sh 重写保护新 04_产出 区 / ac-radar SKILL.md 流水线图+红线）
7. 旧空壳 + 99_archive_父级 全归档 + 写 2 个 README（归档区 + 共用池）+ 全局验证

### 验证
find_ac_root 锚点在 / ac-radar + daily-archive 写入目标存在 / startup_check 无报错 / 改过脚本 0 残留旧路径。

### AC 价值 ⭐⭐
itsuki 主动诊断自建系统的结构债（人工闸门空转 + 两校重复）→ 提出「单一数据源多视图」架构 → CC 执行落地。元认知 + 系统架构设计能力，顶级素材。#项目演化 #元认知 #系统设计 #单一数据源

---

## 个人页面精致化 + 外出申請を単一先生確認に（iOS 演示版） — 另一会话

### 起因（itsuki 实机审）
itsuki 实机看 iOS 学生端演示版，提两点：① 个人页面（マイページ）太丑、「行事予定」（日程）做太小；② 外出申請不需要审查，一个老师确认即可。

### 改了什么
- **マイページ**（`MyPageStubs.swift`）：顶部新增「行事予定」日程卡（列今日及以后最近 3 条活动 + 跳全部日程）；6 宫格图标从 emoji 换成 SF Symbols 系统图标 + 淡 teal 圆角底块；删底部设置里「行事予定」那行小字；删不再用的 EmojiIcon struct。
- **外出申請**（`ApplyStubs.swift`）：进度从「提出→審査→完了・承認」3 步改成「提出→先生確認」2 步；确认后显示确认老师名（演示版写死「松本 先生」，真正按登录账号自动记 = 后端待做）；StepMeta 加 activeNote 字段。
- **设计文档**：`system_features.md §7.2.7`（外出単一先生確認规则 + 与出寮届区别表 + 确认者自动记录）；`IOS_DESIGN_LOG §14.20`；`TODO.md` 外出后端待办 4 条。

### 验证 + codex 审查
- Demo + Release 双 scheme 都 BUILD SUCCEEDED。
- 跑 codex-review skill：codex（gpt-5.5 / xhigh）只读审查 2 轮收敛。
  - 第 1 轮提 2 条（均非 blocker/major）：① 外出状态机 `active: !(status=="approved")` 接后端后会误显示非 approved 状态 → CC 核实演示版假数据只有 1 条 approved 外出、撤回不改状态、新建不加记录，**驳回本轮改代码 + 记 TODO**；② monthDay 非法日期显示「0月0」→ **采纳改 guard let**。
  - 第 2 轮：codex 核实两条处理都成立，CONVERGED（0 blocker / 0 major）。
- commit `8563d05`（明确文件名提交 5 文件，避开别会话的 backend/README 改动）。

### AC 价值 ⭐
「多 AI 对抗复审」模式实操：CC 不盲信 codex，逐条独立核实代码再裁决（驳回 1 采纳 1），驳回有依据（查 SEED 假数据 + 撤回/新建逻辑）。#模式2_多AI对抗 #代码审查 #独立验证

## AC 信号 (22:12) [方法论/元认知]

**原话/概要**：我发现素材按『类型』分（信号碎片/会话存档/素材卡片）是分错维度——这些都是素材，该按『重要程度』分。重定义成三层：1自动产出(Claude堆的，权重最低)/2我挑的(权重高)/3最重要(我拍板，权重最高)，金句单独一类。还问『各种金句现在怎么全混一块了』

**上下文**：我担心素材丢了，让 CC 查素材都存哪。查的过程中发现旧结构按类型分顶层，越看越乱、分不清

**归属候选**：我指出分类维度错了（该按重要度不按类型），拍板新三层结构，CC 执行落地

> 由 ac-radar 实时打 tag 写入。深度 dump 由 session-wrap §5.5.1 在收尾时另开段写。

---

## [元认知 / 人机协作] 素材库四层落地 + 不信任张力下读完 8.6 万字（接早段二次重构）

### 起因
早段二次重构把两校共用池建好后，itsuki 看到「自动捕获」最早只到 5-10，担心之前的素材丢了。CC 查清没丢（ac-radar 5-10 才上线，更早的素材在素材卡片 + iCloud 归档区）。itsuki 接着说素材按「类型」分（信号碎片 / 会话存档 / 素材卡片）是分错维度——「这都是素材」——要改成按「重要程度」分。重定义四层：1 自动产出（Claude 堆的，权重最低）/ 2 我挑的 / 3 最重要 / 4 金句单独。

### 做了什么
- 素材库二次重构落地：建四层文件夹 + 迁现有文件（自动捕获 / 对话原文 / 会话总结 / 素材卡片 → 1 自动产出；itsuki 主动提报 → 2 我挑的）+ 改 4 脚本指向新路径（append_to_scratchpad / daily-archive / startup_check / session-start hook）+ 旧空壳归档。
- itsuki 要从完整会话记录里找金句和最重要的 → CC 全读 8.6 万字 30 条发言 → 写 3 文件归位：1 自动产出完整总结（按 itsuki 原话指令分「厉害的地方 / 决策 / 发现的问题 / 解决的问题」）+ 3 最重要（itsuki 亲标四条）+ 4 金句。

### AC 价值点 — 人机信任的真实张力（这段最值钱）
itsuki 明确说「我不信任你」，坚持要自己把会话记录文件发给 CC、不让 CC 自己抽取。结果文件是机器格式（jsonl）他打不开、拿不出内容，被迫让 CC 读。CC 没有要求他「相信我」，而是用可验证的方式回应不信任：把他的全部发言提取成单独文件**存到桌面让他自己核对**，原话照抄、标注时间。

这件事是 itsuki 对 AI 一贯态度的活样本——**要可验证、要掌控、不盲信**。他志望理由书的方法论骨架是「AI 是我获取知识的通道，真正的决策中心是我的大脑」；这次不信任 → 坚持自己发 → 被迫靠 AI → 要求可核对，证明他不是嘴上说说，而是在每个协作细节上都要攥住掌控权和验证权。这种张力本身就是 AI 时代「人怎么用 AI 而不被 AI 代替」的第一手素材。

### CC 读完的发现
8.6 万字里，itsuki 自己 06-03 就已经一条条挑好了金句和最高权重素材、还亲手标注「这条放最高权重」「能进自我推荐书」。CC 要做的不是替他判断，只是把他自己的判断从记录里拎出来归位。这反过来印证了主体性——他从不把「哪条素材值钱」的判断外包给 AI。

### 标签
#元认知 #人机协作 #主体性 #信息架构 #AI时代学习方法
