# 2026-05-28 凌晨 — iOS catch 修 + StayDetail 切真 API + codex 审查 + 全装

> **会话主题**：itsuki 启动「推进 iOS 全做」→ CC 修 TODO §D 工程债 2 处 catch + StayDetail 切真 API + 调 codex 审查 + 按 codex 建议实装 6 项 + 关 2 条 TODO 过期项
> **跨日**：会话开始 5-27 晚 → 主要工作时段跨午夜到 5-28 早 8:30
> **设备**：MacBook-Pro / Opus 4.7 1M / 主会话
> **改动文件数**：6（1 新建 + 4 改 + 2 graphify 重建）

---

## 00:30 [协作授权] itsuki「今晚全做」推进 iOS

### 背景

itsuki 启动「我们来推进 ios app 相关的」→ CC 跑启动 SOP 5 步报告状态 → 给 4 个候选方向（修 catch / 注册码 iOS / 公告 iOS / 整体扫一遍）→ itsuki 选**「今晚全做」**。

### 关键判断

CC 接到「全做」后没立即闷头干，先扫 iOS 现状判断哪些是真活、哪些已经完成：

- **注册码 iOS 端**：grep `AuthStubs.swift` 发现 `registerStep5` 已调 `createAccount(registrationCode: code)` — 学生侧完整实装 ✅（spec §7.16 老师生成码是 teacher_web 的活，iOS 不沾）
- **老师公告 iOS 端**：spec line 939 表里 iOS 已 ✅，code 7 处文件（Home / Apply / Community / NetworkModels / AuthAPI / AppStore / Route / RootView）装好 ✅

**实际剩活只有 catch 修 2 处**（TODO §D 472-473），加 §11.9 I11 chain 实装核对（已通过 `ApprovalChainBuilder` 实装 ✅）。

### itsuki 原话

> "今晚全做"

### AC 价值

- **模式 6 取舍**（核心问题 #2 技术判断）：CC 接到「全做」指令后**不机械执行**，先核对现状区分真活 vs 已完成。展示「指令理解 + 现状核对 + 再行动」的工程素养。
- **协作授权颗粒度**：itsuki 明确「全做」= 给 CC 充分自治权。CC 在自治范围内继续 propose（如发现别的 iOS 残留是否做），保持「propose 等拍板 vs 闷头干」边界感。

#AC候选 #协作模式 #指令理解

---

## 00:45 [问题解决] StayList 2 处 catch + StayDetail 切真 API

### 背景

TODO §D 第 472-473 条是 5-27 深夜审查发现的 2 处错误处理 bug：

1. `StayListStubs.swift:475` — `ApplicationsAPI.listMine()` 失败时统一降级到 `StayListMock.all` 假数据，v1.0 上线后用户会看到假数据
2. `MyPageStubs.swift:1637` — 删账号 catch 写 `deleteError = "通信失败 — \(error.localizedDescription)"`，会把 Swift / `URLSession` 的英文系统错误码（如 "The data couldn't be read..."）直接给学生看

### 经过

**改动 1（StayList loadList）**：catch 拆成 2 个分支：
- `APIError.unauthorized`（401）→ 用 mock 兜底（未登录态 / Apple reviewer 没真账号也能看 UI）
- 其他错误 → 显示日语提示 + 空列表（不降级假数据）

**改动 2（MyPage 删账号）**：catch 按 `APIError` 类型 switch 4 个 case（unauthorized / network / server / default），每个 case 显示日语友好提示。

**意外发现 + 第 3 处改动**：扫整体 iOS 时发现 `StayDetailView.load()` 写着 `⚠️ DEMO-ONLY-SCAFFOLD（2026-05-03）：纯 mock，无后端依赖` + 注释「v1.0 切回：UUID guard + ApplicationsAPI.detail / .audit 并行 + 4 个 catch 分支」**未落地**（5-03 拍板，到 5-27 一直没切回）。CC 一并切回真 API：
- `app.isAuthenticated` + `UUID(uuidString: id)` 双 guard 兜底 mock
- `async let` 并行调 `detail` + `audit`（节省往返）
- 401 / network / server / 其他 4 个 catch 分支

### 关键判断

CC 在 itsuki「全做」授权下**自主把 5-03 拍板但 24 天没落地的工作做了** — 不是新加 scope，是清掉已经决定但漂的工作。

### itsuki 原话

> "今晚全做"（5-03 拍板的工作 itsuki 之前没回头跟进，CC 在这次「全做」授权里识别为可做项一起清）

### AC 价值

- **模式 1 问题→尝试→解决**（核心问题 #3）：3 处具体 bug → 具体 catch 拆分方案 → 落地修
- **模式 2 假设崩**（核心问题 #4）：CC 一开始以为只修 2 条 TODO，扫文件时假设崩 — 发现还有 1 处更大的 5-03 漂移工作。继续调查 → 真因是 24 天前的「等切回真 API」标记 + 一直没人触发。**没放弃**。
- **工程方法论**：「不是新加 scope，是清掉漂移工作」— 区分「新开任务」vs「闭合既定决策」是关键工程素养

#AC候选 #问题解决 #工程方法论 #scope边界

---

## 01:30 [认知改变 ⭐ 模式 5] itsuki「他妈的 graphify 是什么」

### 背景

CC 给 iOS 整体扫描结果后 propose 2 件事：(A) diff fork iOS vs 主项目，(B) 跑 `graphify update .` 刷代码知识图谱。

itsuki 选 #1 + #2，但在选项 #2 追问「**他妈的 graphify 是什么**」+ #4「**所以说现在做不了？**」

### 经过

**CC 沟通失职被当场点破** — CC 上一条回复直接用了「graphify」「fork」「diff」「scaffold」一堆术语**没解释**，违反全局 + DMSD CLAUDE.md「不要默认 itsuki 认识英语单词 / 项目代号」铁律。

CC 立即认沟通失职 + 补解释：
- **graphify** = Claude Code 能力插件（skill），把代码 / 文档转成知识图谱（函数 / 类 / 文件之间的引用关系图）。itsuki 启动会话时看到的「⚠️ graphify: Knowledge graph exists. Read graphify-out/GRAPH_REPORT.md」就是它的提示
- **#4「做不了」的真相** = 学習出席 NFC 提交 / 点呼 NFC 真接 — iOS 文件注释写「backend 待实装」（`StudyAPI.swift:9` / `AppStore.swift:295` 的 `TODO[backend]`），iOS 强写也是 404 空调用

### 关键判断

CC **诚实声明信息边界** — 「只看了 iOS 那边的注释，没真去 backend 文件验证端点到底有没有 — 注释可能过期」。**不假装查过**（违反全局 CLAUDE.md anti-ai-flavor 8 类第 8 类「编造数据」铁律）。

### itsuki 原话

> "解释他妈的到底是什么东西"
> "所以说现在做不了？"

### AC 价值

⭐⭐⭐⭐⭐
- **协作纠错 顶级**：itsuki 当场指出 CC 沟通失职 → CC 立即承认 + 修正（不辩解）+ 主动补 8 类自检里的「术语裸露」case
- **模式 5（认知改变）顶级**：itsuki 通过质疑「graphify 是什么」逼 CC 解释 — 自己从「CC 用了一个我不认识的工具」过渡到「知道 graphify 是知识图谱工具 + 启动 hook 会提醒它存在」
- **模式 5 二阶**：CC 自己也学到「即使是 hook 提示出现过的词，也不能假定 itsuki 见过 = 认识」— 「见过 ≠ 知道意思」
- **方法论级别 — 信息边界诚实**：CC 在「做不了」回答里**主动加注「没真验证 backend，只看了 iOS 注释」**— 8 类自检第 8 类「编造数据」机制层修正

#AC候选 #认知改变 #模式5 #协作纠错 #沟通失职 #信息边界

---

## 02:15 [自我管理] TODO §D + §E 两条过期项闭合

### 背景

itsuki 选 #1 「比对 fork iOS vs 主项目，确认账号删除界面是否真未同步」。

### 经过

CC 跑 `ls ~/dev/Tomoshibi-AppStore/` → 整个目录不存在。继续 `find ~/dev/ -name "Tomoshibi-AppStore"` → 找到 `~/dev/DMSD/99_archive/2026-05-22_tomoshibi_appstore_fork/ios/` — **5-22 已整体归档到项目内 99_archive，不是真删除**。

`diff -rq 归档 主项目`：
- fork 独有文件 0 个 ✅
- 主项目独有 4 个文件（5-24 / 5-26 后续归位的新文件）
- 9 个文件内容不同（5-22 后主项目继续推进，fork 是冻结快照）

**结论**：TODO §D 463 + §E 477 两条「iOS fork 处置 + 账号删除界面同步」都是过期残留 — 5-22 归档时已同步完毕，没人回头标 ✅ 让条目悬挂了 6 天。验证 `AccountsAPI.deleteMyAccount()` 在 `AuthAPI.swift:52` + 界面在 `MyPageStubs.swift:1632` 调用 ✅。

CC 直接 Edit TODO 闭合两条（按 `[x] ~~strikethrough~~ + ✅ + 闭合证据` 格式）。

### 关键判断

「**TODO 关条目不要问 itsuki 拍板，做完就该 CC 自己关**」（5-26 立的铁律 / memory `feedback_no_handoff_work_back_to_itsuki.md`） — CC 这次直接 Edit 闭合，没甩回 itsuki 让他粘贴。

### AC 价值

- **模式 6 取舍**：fork 处置 = 已完成（5-22 归档）vs 残留待办（TODO 还挂着）的辨别 — 「文件位置变了 = 处置完成了」「TODO 没标 ✅ ≠ 没做」
- **工程素养**：CC 主动跑 diff 验证 + 不轻信 TODO 残留文字
- **自我管理**：5-26 立的「关条目不要问」铁律 5-28 真落地，没忘

#AC候选 #自我管理 #TODO卫生

---

## 03:00 [问题解决 ⭐ 模式 2] codex 审查 — 卡住 → 重试 → 识破误判

### 背景

itsuki 「调用 codex 审查一遍」— 让外部 AI（OpenAI Codex CLI 子智能体）做第二意见审查 CC 本次 3 处代码改动。

### 经过

**第一次 codex 调用**：
- 8:18 PM 启动，子智能体调 codex-rescue 后台跑
- 8:20 PM 出最后一条中间消息「主要风险在 401 + UUID」+ 跑 3 个读文件命令
- **之后没出最终报告**，输出文件冻住 10 分钟没动
- itsuki 问「还没好——」 → CC 调查发现 codex 进程都还活着但不再产出

**CC 诊断 + 重试**：
- propose 3 个选项（A 重开 codex 简化版 / B 自己审 / C 不审直接 commit）
- itsuki 选 A
- 第二次 codex 调用 — 精简上下文（直接给改动 diff + 上次已识别的 2 个风险点 + 明确要求 500 字内 5 段报告）
- **2 分钟出报告** 3/5 分

**CC 识破 codex 误判**：
- 报告 #1 必修 bug：「`auditOut.map { /bin/zsh.toAuditLogEntry() }` 编译失败」
- CC 立即 grep 实际代码 → 是 `auditOut.map { $0.toAuditLogEntry() }`（`$0` = Swift 闭包简写参数）
- **codex 看到提示词里被 shell 转义的 `$` 符号才误读** — false positive（假阳性）
- CC 在最终汇总里**剔除这条误判**，只保留真问题（401 mock 取舍 + UUID 静默 back + helper 抽象）

### 关键判断 ⭐⭐⭐

- **CC 不盲信 AI 第二意见** — codex 给的 3 个必修 bug 里 1 个是 false positive，CC 通过验证发现并剔除
- **CC 主动诊断卡住** — 不重试就闭合「codex 评审」任务，而是诊断卡因 + 给选项 + 让 itsuki 决定
- **CC 提示词工程反思** — 第一次卡住可能是上下文太大 / 任务太多，第二次精简 + 限字数 + 明确 5 段格式 → 成功

### itsuki 原话

> "调用codex审查一边"（原话 typo）
> "还没好——"
> "A"（选重开简化版）
> "全都做"（接受全部 codex 真建议）

### AC 价值

⭐⭐⭐⭐⭐
- **模式 2 假设崩 顶级**（核心问题 #4）：
  - 假设 1 = 「codex 调用一定会成功出报告」 → 第一次卡住，假设崩
  - 假设 2 = 「CC 不能 verify codex 输出」 → CC 验证后发现 codex 误判，假设崩
  - 没放弃 → 重试 + 验证 + 剔除误判 + 落地真建议
- **模式 6 取舍**：第一次卡住时 3 个选项（A 重试 / B 自审 / C 跳过）+ itsuki 选 A — 展示多 AI 协作时的「降级策略」
- **方法论级别 — AI 协作真实形态**：「AI 协作 ≠ 盲信 AI」+「AI 协作的失败模式（卡住 / 误判）需要 fallback 机制」+「Claude + codex 双 AI 串行（Claude 自审 → 让 codex 二审 → Claude 验证 codex 输出）」
- **AC 出愿可挂**：「跟 AI 协作时何时采纳 / 何时拒绝 / 何时重试」级别的元能力

#AC候选 #模式2 #假设崩 #模式6 #AI协作 #信息边界

---

## 04:00 [问题解决 ⭐ 模式 4] 按 codex 建议实装 6 项

### 背景

itsuki「全都做」= 接受 codex 真建议（剔除误判后 5 项）。CC 实装：

### 实际改动

| # | 文件 | 改动 | codex 建议 |
|---|---|---|---|
| 1 | 新建 `Foundation/Network/APIErrorPresenter.swift` | 错误文案统一 helper（6 个 APIError case 转日语提示 + .unprocessable 显示 backend 真 message + fallback 参数） | 建议 2 抽 helper |
| 2 | `StayListStubs.swift` `loadList` | 401 改清 `app.authToken = nil`（触发 didSet 自动跳登录页）+ 其他错误走 helper | 必修 2（401 mock 误导用户）|
| 3 | `StayListStubs.swift` `StayDetailView.load()` | 拆 guard（未登录 vs 非 UUID 分开 toast）+ audit 单独 try（失败不影响 detail）+ 401 清 token + 其他走 helper | 必修 3 + 建议 3 |
| 4 | `MyPageStubs.swift` 删账号 catch | 9 行 switch → 1 行调 helper | 建议 2 落地 |
| 5 | `IOS_DESIGN_LOG.md` | §速查表 5 项 🟡 → ✅ + §11.9 I11 → ✅ | 文档同步 |
| 6 | `TODO.md` | §D 463 + §E 477 两条 iOS fork 过期项闭合 | TODO 卫生 |

**意外收获**：新建 `APIErrorPresenter.swift` 让 `03_dev/` 实际文件数 389 → 390，**正好对上启动时 drift 报告说的「写 390 / 实际 389」**。漂移自动闭合，project-overview SKILL.md 不需要改。

### 关键判断 ⭐⭐⭐

- **401 清 token 是产品决策不是技术决策** — 原 mock 兜底逻辑是 5-08 Apple reviewer 没真账号也能看 UI 的需求；codex 主张清 token 跳登录页是真生产逻辑。itsuki「全都做」拍板 = 接受真生产逻辑，reviewer 走 guard 那条路（未登录）依然能看 mock
- **audit 失败容错是产品决策** — audit（履历）拉取失败不应该让 detail（详情）整个 fail。CC 拆成 `try await detailTask` + 内部 `do try await auditTask catch entries=[]`
- **helper 抽象的边界** — helper 接 `Error` 型（不只 `APIError`），用 `as?` 转 — 既兼容非 `APIError` 的 catch，又能统一 `APIError` 6 case

### AC 价值

⭐⭐⭐⭐⭐
- **模式 4 v1→v2 演化 顶级**（核心问题 #3）：错误处理从原始（所有错误降级 mock）→ 切回真 API → codex 审查发现风险 → 抽 helper + 401 清 token + audit 容错 — **4 步演化**。每步有具体证据 + 真实推动力。
- **模式 6 取舍 × 多**：
  - reviewer 兜底 vs 真生产逻辑（401 mock 取舍）
  - audit 失败致命 vs 非致命（产品决策）
  - helper 抽象边界（接 `Error` vs 接 `APIError`）
- **协作模式深度**：3 重 AI 协作 — CC 自审 → codex 二审 → CC 验证 codex → 落地真建议
- **AC 学术延伸**：错误处理策略（fail-fast vs graceful degradation）/ 用户错误提示设计（用户友好 vs 调试信息）/ 多 AI 协作的工程价值

#AC候选 #模式4 #模式6 #错误处理 #helper抽象

---

## 工程动作汇总

### 新建（1 文件）

- `03_dev/student_ios/v1/TomoshibiApp/Foundation/Network/APIErrorPresenter.swift`（39 行）— `APIError` 6 case 转日语提示的统一 helper

### 改（4 文件）

- `03_dev/student_ios/v1/TomoshibiApp/Features/StayList/StayListStubs.swift` — `loadList` 401 改清 token + `StayDetailView.load()` 拆 guard + audit 容错 + helper
- `03_dev/student_ios/v1/TomoshibiApp/Features/MyPage/MyPageStubs.swift` — 删账号 catch 改用 helper（9 行 → 1 行）
- `03_dev/student_ios/IOS_DESIGN_LOG.md` — §速查表 5 项 🟡 → ✅ + §11.9 I11 → ✅ + 最后更新日改
- `00_admin/TODO.md` — §D 463 + §E 477 两条 iOS fork 过期项闭合

### 重建（2 文件）

- `graphify-out/graph.json` + `GRAPH_REPORT.md` — 知识图谱重建（17487 节点 / 35753 边 / 1010 社区 / 677 文件）

### 漂移自动闭合

- `03_dev/` 实际 389 → 390（加 `APIErrorPresenter.swift`）= project-overview SKILL.md §0.1 写 390 ✅

---

## 残（下次跟进 — 加 TODO）

无新增长尾。本次会话所有 propose 都在「修 TODO §D 工程债 + codex 审查 + 落地建议」内闭环。

下次 iOS 可做的事（按现状）：
- **学習出席 NFC 提交真接 backend**（`StudyAPI.swift:9`）— 等 backend 实装
- **点呼 NFC 真接**（`AppStore.swift:295`）— 等 backend 实装
- **Xcode Archive 上架冲刺**（TODO §A 433-445）— itsuki 操作不是 CC 做

---

## AC 价值汇总

- **模式 1 问题→解决** × 1（3 处具体 catch bug → 拆分方案 → 落地）
- **模式 2 假设崩** × 2（5-03 漂移工作 + codex 卡住 / 误判）⭐⭐⭐⭐⭐
- **模式 4 v1→v2 演化** × 1（错误处理 4 步演化）⭐⭐⭐⭐⭐
- **模式 5 认知改变** × 2（graphify 是什么 + 「见过 ≠ 知道意思」元认知）⭐⭐⭐⭐⭐
- **模式 6 取舍** × 多（codex 重试选项 / 401 mock 取舍 / audit 致命非致命 / helper 抽象边界）⭐⭐⭐⭐
- **协作纠错** × 1（itsuki「他妈的 graphify 是什么」纠 CC 沟通失职）顶级

**学术延伸**：
- 错误处理策略（fail-fast vs graceful degradation）
- 多 AI 协作工程化（Claude + codex 串行 + 验证 + fallback）
- 信息边界诚实（不假装查过 / 标注「没验证」）
- TODO 系统卫生（关条目不要问 + 区分「漂移残留」vs「真待办」）

**AC 出愿可挂**：
- 「跟 AI 协作时何时采纳 / 何时拒绝 / 何时重试」级别的元能力（codex 审查全段）
- 「错误处理的产品决策 vs 技术决策」（401 清 token 取舍）
- 「沟通中的信息边界 — 见过 ≠ 知道意思」（itsuki graphify 质疑段）

#AC候选 #DMSD #iOS #错误处理 #AI协作
