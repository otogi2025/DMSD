# 工程简报 2026-06-09

> 纯工程流水账（改了什么 / 修了什么 / 怎么验证）。AC 素材（怎么想的 / 学到什么）在 `raw/`，两条线分开。

## 会话：iOS 上线缺口 11 功能实装 + codex 4 轮复审 [Opus 4.8 1M · ultracode · /goal]

起因：施工图 `00_admin/handoff/iOS上线缺口_GOAL提示词.md`（itsuki 6-08 让 6 维度子代理审 iOS 2.2 万行 + CC 自验后列的缺口）里「代码能做 + 能编译验证」的 11 个功能。`/goal` 因正文超 4000 字符没挂上，itsuki 说「继续」直接按施工图跑。

### 改了什么（13 commit，`bc86c09..HEAD`，全本地未 push）

| commit | 功能 |
|---|---|
| `53ab362` | ⑪ 通知设定页加「接通后生效」说明文字 |
| `008abe8` | ⑨ 删暗色模式死控件开关（拨了无反应的假开关）|
| `3f4df93` | ⑧ PrivacyInfo.xcprivacy 据实补 6 类数据收集声明 |
| `70a7d46` | ③⑩ project.yml 补 NFC 用途说明 / 加密合规标志 / entitlements 接线（+ pbxproj）|
| `1b3bea8` | ⑦a 删 BusView 死页 + .homeBus 路由（CommunityStubs/RootView/Route 三文件）|
| `39e77fb` | ⑦b 我的页行事卡接 EventsAPI 真后端 |
| `fd186ef` | ⑤ RootView 加 onChange(authToken) 全局令牌守卫 + StayList 三注释纠正 |
| `0399d36` | ④ 6 列表加载失败显三态（AppStore ListLoadState 枚举 + 4 状态字段 + 5 界面）|
| `2800402` | ⑥ displayUser 生产不回退假人 + profileIsPlaceholder + HomeStubs 4 处显「—」|
| `4d4b5b5` | ① 新建 ST25DVWriter.swift（CoreNFC 写 ST25DV Mailbox）+ 两 simulate 分演示/生产轨 + RollCallAPI 注释纠正 |
| `0841ad7` | codex 一轮修 7 条（2 阻塞 + 4 重大 + 1 次要）|
| `0193b89` | codex 二轮修 M-1 残留 |
| `c2e052f` | codex 三轮修 M-1 最深层（ST25DVWriter 原子取消守护）|

### ② 图标 — 不改代码
保留 itsuki 6-07 做的 Xcode 26 `.icon` 玻璃火苗（Icon Composer 格式）。施工图原让建老式 `appiconset` 是过时方案（勘察发现已用 `.icon` 解决）。① 的编译已验证 `.icon` 在 iOS 16 部署目标下能出图标。

### 验证
- 每个功能 / 每轮 codex 修复都 CC 自己 xcodebuild 正式版(`TomoshibiApp`)+演示版(`TomoshibiAppDemo`)双 scheme，全 `BUILD SUCCEEDED`。
- 实测确认 CoreNFC 在 iOS 模拟器 SDK 可编译（不用 `#if canImport` 包）。
- Swift 6 并发坑：async Task 闭包当 'sending' parameter 报 data race → 改 completion handler 版。
- 踩坑：裸 `xcodebuild` 依赖 Bash 工作目录、有次漂到仓库根编译失败 → 全改 `-project` 绝对路径。

### codex 4 轮复审（gpt-5 + high，非预期的 gpt-5.5 + xhigh）
逐层挖 `ST25DVWriter` 的 NFC 取消竞态：一轮 2 阻塞（session 被 ARC 提前释放 / continuation 跨线程双重 resume）+ 4 重大 + 1 次要 → 二轮 M-1 残留 → 三轮 M-1 最深层（创建 session 前的取消空窗）→ 四轮核实死锁判断 + 0/0 收敛。每条 CC 核实真代码再裁决、修完自己 xcodebuild 验。AC 素材见 raw。

### 没做完（留真机联调，不在本次范围）
- ① ST25DVWriter 的 ISO15693 写 Mailbox 命令字节是占位 `// TODO[硬件]`，待 ST25DV16K datasheet + 点呼机读取侧 `st25dv.py` 对齐。
- 真机 NFC 联调（模拟器跑不了 NFC，`readingAvailable` 恒 false）。
- ③ entitlements 的推送 + NFC 受限能力真生效需 itsuki 苹果开发者账号 Team ID（现在 DEVELOPMENT_TEAM 空、关签名）。
