# dev_log 2026-06-11

> 工程简报（纯事实，AC 叙事在 raw/）。格式见 `_README_工程简报格式.md`。

## 会话：三份索引/总览文档对账校准（by Opus 4.8 1M）

**起因**：itsuki 一句「project overview / project guide / 我的环境.md 全都更新」。这三份都是索引类文档，最易跟真实状态漂移；启动漂移检测脚本一上来就报多目录不符。

### 改了什么

**1. `.claude/skills/project-overview/SKILL.md`**（1450 文件的逐个字典）— commit `cc29127`
- §0.1 体量表全行重算：总数 1399→1450（committed，`git ls-files` 口径）。99_archive 639→650 / 03_dev 527→541 / 05_logs 149→174 / 00_admin 25→28 / 01_specs 14→13
- §3 后端 104→112：§3.4 加 3 路由 `lost_found.py`/`misc_requests.py`/`songs.py`；§3.6 加 2 测试 `test_demo_teacher.py`/`test_student_self_views.py`；cleaning 全删反映
- §5 iOS 78→84：§5.6 补 5 个 API endpoint（CleaningAPI/LostFoundAPI/MiscRequestsAPI/SongsAPI/StudentProfileAPI）+ `ST25DVWriter.swift`；Android 131→132；§4 teacher_web 177→176
- §6 raw 108→109 + dev_log 16，§6.2 补 6-06~6-10 关键日；§7 归档 639→650
- §0.1 脚注加：startup 漂移脚本会把未提交的 .bak/icon/handoff 算进「实际」是噪音，本表只记 committed；05_logs 脚本偶报 173 是中文文件名 NFC/NFD quirk，以 ls-files 174 为准

**2. `PROJECT_GUIDE.md`**（新人/外部 AI 导览）— commit `5d4ad3d`
- §5.1「响应格式」原把 `{ok,data}` 包络当事实写 → 标注「2026-06-10 全量审查证实与代码不符」（后端 29 路由直接返回数据模型、iOS 直接解码、包络无实现），对齐心智模型 §3/§6
- `decision_log.md` 路径 4 处全改 `decisions/decision_log.md`（6-10 移入子目录）
- 文件字典数 1396→1450

**3. `~/.claude/我的环境.md`**（全局工具/skill/hook 清单，不在 DMSD git 仓库）— 已存盘
- anti-ai-flavor 描述 6 类→8 类（5-27 加 G 自指失败 + H 编造数据一直没同步）+ 标 6-10 提醒 hook 停用
- DMSD skills 8→9（补 6-04 加的 `codex-review`）
- §8 删退役的 `progress_overview.md` + 加 `项目心智模型.md` / `PROJECT_GUIDE.md`

### 验证
- `bash bin/check_overview_drift.sh`：§0.1 committed 口径对齐（残留「实际更多」差 = 未提交的 itsuki 新 icon + .bak 备份噪音，不进表）
- `bash bin/sync-check.sh`：我改的 2 文件无联动漏；报的 Android 警告属别会话改动

### commit 清单
- `cc29127` docs(overview): project-overview 全目录对账 1399→1450
- `5d4ad3d` docs(guide): PROJECT_GUIDE §5.1 响应包络标与代码不符 + decision_log 路径

### 未做完 / 待拍板
- `我的环境.html`（美化版，session-start-env-diff 挂钩对账的是它）停在 6-06，现落后 `.md` → 记 TODO 待 itsuki 定要不要从 .md 重生成

### 本会话决策一览
- §0.1 用 `git ls-files` committed 标准口径（1450），不追漂移脚本「实际」里的未提交噪音 → 详见 decisions/decision_log_全部.md

---

## 会话 B（Opus 4.8 1M）— iOS 点呼显示链 R-1/R-2 接真后端

### 起因
itsuki 问「iOS 距离 v1.0 上线还有什么问题」→ CC 读 6-10 全量审查 TODO 盘出 R-1~R-5 五条重大。itsuki 拍板：v1.0 不支持手机签到（ST25DVWriter 代码别动）/ iOS 暂不给学生真用、只为上架好看 / R-1+R-2 走「动后端补接口」、本会话主做；R-3/4/5 写提示词交另一会话。

### 改了什么 + 验证（3 commit）
- `8cdff97` 后端 `GET /rollcall/me/today`（学生令牌）：今日本人寮场次 + 四时间窗 + 我的判定。schemas.py 加 `MyRollCallTodaySession` / rollcall.py 加 `my_today_rollcall`（dorm_unit 过滤 + event join）/ test_rollcall.py `TestMyTodayRollCall` 4 例。pytest 20 passed。
- `20776b6` iOS 接 me/today（R-1①②/R-2）：RollCallAPI `myToday()`+模型；AppStore `todaySessions`/`loadTodayRollcall`/`refreshRollStateFromSessions`（时间窗状态机）/`tickCountdown` 生产版真实重算；HomeStubs done hero 用 checkinKind、banner 按 rollState。双 scheme BUILD SUCCEEDED。
- `9f92d00` R-1③：profile 接口 join session 带窗口时刻；ProfileRollCallEntry 两端加 scheduled_window_start_at/on_time_end_at；MyPageStubs kvPairs 生产显真实窗口不写死 07:00/21:00。pytest 53 passed + 双 scheme BUILD SUCCEEDED。

### 新增文件
- `00_admin/handoff/iOS_R3R4R5提示词_2026-06-11.md`（R-3/4/5 施工提示词）

### 未做完 / 交接
- R-3（三上报弹窗防连点）/ R-4（登录锁定 iOS↔后端 423/403 对齐）/ R-5（体調報告履歴假病历接 GET /reports/mine）→ 提示词已写好，compact 后接着做
- 预存在 lint 债：rollcall.py `session_summary` 端点 F841（session/today 未用，非本会话引入）→ 记 TODO

### 本会话决策一览
- v1.0 不支持手机签到（方案 A）但 ST25DVWriter 代码保留 / iOS v1.0 暂不给学生用只为上架 / R-1/R-2 走方案 B 动后端 → 详见 decisions/decision_log_全部.md

---

## 会话 C（Opus 4.8 1M）— iOS R-3/R-4/R-5 三条上线缺口（接会话 B 的交接件）

### 起因
会话 B compact 后 itsuki 把 `handoff/iOS_R3R4R5提示词_2026-06-11.md` 发回，本会话照提示词把三条做完。

### 改了什么 + 验证（4 commit）
- `d3d0439` R-3：HomeStubs 三个上报 Sheet（体調報告/欠席申請/その他）照 RenewStudentNoSheet 范本各加 `@State submitting` —— 按钮 enabled 追加 `!submitting` + title 显「送信中…」+ 生产版 Task 前置 true、失败复位。防慢网连点重复 POST。
- `7841f5a` R-4：登录锁定以后端为真值。生产版 401 只提示凭证错不走本地假倒计时 LockoutView；新增 `catch APIError.server(423,_)` 显后端「残り約 X 分」锁定文案、`server(403,_)` 显账号停用；演示版 `#if DEMO` 保留本地锁定升级演出。核实链路：后端 auth.py 423/403 detail 是 `{code,message}` dict → iOS DetailError.extractMessage 已支持取 message。
- `e4078c5` R-5：体調報告履歴接真数据。RollCallAPI 加 `RollCallReportsAPI.listMine()` 接后端已有的 `GET /rollcall/reports/mine`（缺口在 iOS 侧没 list 方法）；MyHealthView 改双轨（演示 SEED.health / 生产 .task 拉 listMine 过滤 kind=health）+ 三态（转圈/失败可重试/真空），失败也不退回假病历。
- `44055ab` 交接件归档：`iOS_R3R4R5提示词` git mv 进 `99_archive/2026-06-11_handoff已完成/` + README 加归档记录。
- 验证：正式版 + 演示版双 scheme `xcodebuild` 均 BUILD SUCCEEDED。后端无改动（listMine/me/today/reports 端点都是已有）。

### 备注
- 至此 6-10 全量审查「🔴 重大已验证 5 组」R-1~R-5 全部清零，TODO 对应段标 ✅。
- 别会话正在改后端 deps.py/models.py/permissions.py + alembic（老师权限组）+ Android 多文件 —— 本会话只动 4 个 iOS 文件，显式路径提交，未碰别会话改动。

### 会话 C 续（同会话，compact 后）— 设计档同步 + v1.0 盘点 + 全端图标 + M-1 + 单测 + 签名

**1. R-1~R-5 设计档补同步**（itsuki 怒「改了必须同步设计档」）：`b4bd2ec` IOS_DESIGN_LOG §16 记 R-1~R-5；`442554f` BACKEND_DESIGN_LOG §5.5.7 `GET /rollcall/me/today` + §5.5.8 体调上报族 + system_features §7.4.2 点呼矩阵「学生侧 status」行落地为 me/today（注明 Android 同款链未接）。

**2. v1.0 上线盘点**（itsuki 问「距 v1.0 还有什么问题」）：核实后给账 — 头号阻塞 = 后端只跑骨架待 itsuki 本地对齐完再部署；iOS 真阻塞剩签名接线/图标/隐私问卷(外部)/App Store 后台元数据；M-1 首页活动卡核实仍真显假数据。

**3. 全端图标换 Tomoshibi-icon-1**：`31a40fc` iOS AppIcon.icon 换青绿渐变 + AppIcon-1024.png；`4201a2f` 老师网页 4 处 import 的 logo png。Android 自适应图标需合成 logo+渐变层 + 本机无 ImageMagick + 别会话在改 Android → itsuki 拍板「先别管」。

**4. M-1 首页活动卡 + 巴士卡接真**：`fc57edb` LifeTab 加 loadedEvents/loadedBusRoutes 两 @State，.task 生产拉 EventsAPI/BusAPI，双轨 + 失败不退回假数据。`5f1f71a` 设计档 §17 + TODO 标 ✅。

**5. iOS 首批单元测试**（接 handoff `iOS点呼状态机单元测试_交接`）：`8f237cf` 抽 `decideRollState(sessions:now:)` 纯函数（注入 now 不读真实时钟）；`99a2999` project.yml 加 TomoshibiAppTests 目标（bundle.unit-test）+ xcodegen + 7 条 Swift Testing（无场次/窗口前/窗口内未签/迟到段倒计时夹0/准时签/迟到签/过迟到截止欠席），decideRollState 标 nonisolated 让测试同步调。`xcodebuild test 7/7 passed`。`877a063` 归档交接件。**iOS 首次有逻辑层测试**。

**6. 签名接线**（itsuki 给 Team ID `DCQ2KT5ZA9`）：`63f9cfc` project.yml DEVELOPMENT_TEAM 填上 + CODE_SIGNING_ALLOWED/REQUIRED 改 YES；IOS_DESIGN_LOG §19 建「上线签名/构建配置登记」表（itsuki 怒「Team id 放设计档案不是只放 memory」）。双 scheme BUILD SUCCEEDED + 单测 7/7（模拟器不受签名开关影响）。仍需 itsuki Xcode 登账号才能真 Archive。

**全程验证**：每条 iOS 改动后双 scheme `xcodebuild` BUILD SUCCEEDED；单测 `xcodebuild test` 7/7；老师网页 `npm run build` 过。版本号由别会话 bump 到 v0.23.0（CHANGELOG 已把图标/M-1/单测算入），本会话不抢版本号。

### 本会话决策一览（C 续）
- 签名开关从全关改 YES + Team ID 入仓库 / Android 图标缓办 / 配置类外部值放设计档案不只放 memory / 版本号让并行会话管不抢 → 详见 decisions/decision_log_全部.md

---

## 2026-06-11 5 端命令实测验证 + CLAUDE.md 瘦身（harness 评估会话）

### 起因
itsuki 想装第三方插件 Hyperbrowser /harness（让 agent 试跑项目命令、把真实失败写成 CLAUDE.md 规则）。CC 安全核验后判明：skill 文件本身无危险命令，但 ① 产出会直接覆盖项目根 CLAUDE.md ② 按单技术栈设计（根目录找 package.json），对 5 端 monorepo 失灵。itsuki 拍板不装，CC 照其方法论手工跑 DMSD 定制版。

### 5 端命令实测结果（全部当场真跑）
- 后端：`.venv/bin/python -m pytest` → **367 passed**（180 秒）；`ruff check app` → 9 个 E402（记 TODO 未修）；`from app.main import app` 加载 147 路由
- iOS：`TomoshibiApp` + `TomoshibiAppDemo` 双 scheme xcodebuild → 双 BUILD SUCCEEDED
- Android：`./gradlew assembleDebug` → BUILD SUCCESSFUL 17 秒（Gradle 9.4.1）
- 老师网页：`npm run build`（tsc --noEmit + vite build）→ 705ms 全过；无 test/lint 脚本
- 点呼机：骨架阶段（main.py 16 行 + 4 空模块），仅 `py_compile` 语法检查通过

### 产出
- CLAUDE.md 加「各端构建/测试防坑」4 条（itsuki 从 8 条草稿里拍板 1/2/5/6）— commit `92012a0`
- 3 条顺带发现入 TODO §B（ruff E402 / BUILD.md 演示版说明过时 / 测试数 373→367 待查）
- 同会话后续：两份 CLAUDE.md 瘦身 + 4 处漂移修复 + anti-ai-flavor 整套退役归档 — commit `8f73449`（详见 WIP 最近会话）

---

## 会话 D — 版本迭代记录优化（版本史插空细分 + 防漂移机制）

### 功能 / 文档增改
- 版本标签 67→143：插空补打 70 补丁（v0.3.3~v0.14.8 — fix 一个一个补丁号，标签日期回填 commit 当天，已 push 旧标签与 commit 零改动）+ 新段 v0.20.0 / v0.21.0 / v0.22.0 / v0.22.1~3
- CHANGELOG：「2026-06-11 插空细分说明」banner + 76 个新版本段 + 3 处死区注记（13 个无空号 fix 逐条列出）+ 全版本一览 24 行新增 + 头部最后更新
- 版本演变一览：补 6-09 欠的 v0.15.1~v0.19.3 四个 minor 详细段（铁律欠账清零）+ 插空说明段 + v0.20~v0.22 叙事段 + 总表同步
- version-bump §4 联动清单 7→8 处（补三端客户端版本号，含具体文件+字段名）+ sync-rules Rule 23 文案 + file-linkage 表同步
- pre-commit 新增「版本演变一览拦截」（阻塞型：含 CHANGELOG 的提交，其顶部版本号在一览查不到就拒绝）+ hooks README 改 5 段检查清单
- 三端客户端版本号 0.15.0→0.22.3 / WIP 头部 v0.19.3→v0.22.3（当天稍后并行会话接力到 0.23.0，新联动当天生效）
- 新交接件 `00_admin/handoff/push准备_交接提示词_2026-06-11.md`（push 前内容盘点 + 推 143 标签 + CI 首跑观察）+ README 清单登记
- AC 叙事 CC 起草版写 iCloud 共用池 `2_我挑的/2026-06-11_v0.22.3_版本史插空细分_AC叙事_CC起草.md`（itsuki 主动委托）

### 验证
- 143 标签计数 + 版本排序抽查 + 回填日期抽查（v0.11.15=05-31 / v0.22.3=06-11）
- pre-commit 拦截正反向逻辑测试（0.22.3 命中放行 / 假版本 9.9.9 触发拦截）+ bash -n 语法检查过
- `git ls-remote` 实测推翻旧情报「远程停在 v0.8.0」——main + 全部 67 旧标签其实已 push（memory 已修正）

### commit 清单（本会话）
- `893bc4d` fix(linkage): 版本号联动链补第 8 处
- `6d85dc0` release(v0.20.0~v0.22.3 + 插空v0.3.3~v0.14.8): 版本史插空细分三文档
- `d15d62c` chore(release): 三端客户端版本号 0.22.3
- `8fbb8bb` docs(skills): 注记去误写日期
- `25c4345` feat(hooks): pre-commit 一览拦截
- 收尾批 commit 见 git log 本段之后

### 决策一览（详见 decisions/ 同日条目）
- 版本史细分选方案 A 插空补打——证据链优先【重大，正本有完整条目】
- 一览铁律「提醒→拦截」升级 / 联动清单补第 8 处 / AC 叙事 CC 起草放 iCloud 2_我挑的
