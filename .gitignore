# ── macOS ─────────────────────────────────────────
.DS_Store
**/.DS_Store
**/._*

# ── Xcode user/local files ────────────────────────
**/xcuserdata/
**/*.xcuserstate
DerivedData/

# ── SwiftPM user state ────────────────────────────
.swiftpm/xcode/package.xcworkspace/xcuserdata/

# ── Google Drive conflict artifacts (if any) ──────
*conflicted copy*

# ── Python (后端 FastAPI + Pi 点呼机) ────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.venv/
venv/
env/
ENV/
.env
.env.local
.env.*.local
pip-log.txt
pip-delete-this-directory.txt

# ── Python 测试 / 覆盖率 ──────────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# ── Node (teacher_web 如果用 Node 栈) ─────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.next/
out/
dist/
build/

# ── Android (Phase 2 Android App) ─────────────────
*.apk
*.aab
*.ap_
*.dex
local.properties
.gradle/
captures/
# keystore 一律不进 git（memory: 备份方案见 TODO）
*.jks
*.keystore

# ── IDE / 编辑器 ──────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── 日志 ──────────────────────────────────────────
*.log
logs/

# ── 操作系统临时 ──────────────────────────────────
Thumbs.db
Desktop.ini

# ── Claude Code 本地设置（含机器路径，不入 repo） ─
.claude/settings.local.json

# ── session-coord skill 会话状态（心跳 / 锁 / inbox 不进 git） ─
.claude/sessions/

# ── graphify 知识图谱产物（82M+ 临时文件，不入 repo）─
graphify-out/

# ── graphify hook install 残留目录（hook 已 copy 到 00_admin/hooks/，详见 hooks/README.md ⚠️ 段）─
.beads/

# ── Matt Pocock skills `to-prd` 中间产物（PRD 详细文档，进 TODO.md 后即可弃） ─
.scratch/

# ── 数据库本地文件 ────────────────────────────────
*.sqlite
*.sqlite3
*.db
*.db.bak
*.bak

# ── 秘密 / 证书（2026-05-22 加 — Codex FC-035） ────
*.pem
*.key
*.p12
*.mobileprovision
secrets/

# ── Claude Code worktree（git submodule 残留，不入 git） ─
.claude/worktrees/
.claude/scheduled_tasks.lock

# ── TypeScript 增量编译产物 ─────────────────────────
*.tsbuildinfo

# ── 带说明后缀的备份文件（xxx.bak_before_yyy / xxx.bak3_zzz 这类，*.bak 挡不住）─
*.bak_*
*.bak[0-9]*

# ── 含服务器 IP/SSH 用户名的运维文档，itsuki 拍板不公开，只留本地（2026-06-12）─
04_ops/个人网站_pj部署运维.md
