# ════════════════════════════════════════════════════════════════
# 白名单模式（2026-06-13）
# 公开仓库 otogi2025/DMSD 只保留「DMSD 软件本体」，其余默认不公开。
# 机制：先忽略一切（/*），再用 ! 放行项目本体。
# 新加的任何文件 / 文件夹默认不公开 —— 零维护。
# 详见 admin/handoff/push大整理_执行计划_2026-06-13.md
# ────────────────────────────────────────────────────────────────
# 放行（= 会出现在公开 GitHub 上的）只有「项目本体」：
/*
!/specs/
!/design/
!/dev/
!/ops/
!/.github/
!/.gitignore
!/README.md
!/LICENSE
!/CHANGELOG.md

# ════════════════════════════════════════════════════════════════
# 以下 pattern：放行目录（尤其 dev/ ops/）内部仍必须忽略的
# 垃圾 / 构建产物 / 密钥 / 数据库 / 备份。
# 白名单 !/dev/ 放行了整个目录，这些 pattern 负责重新挡住目录里
# 不该公开的东西（node_modules、.venv、.env、密钥等）。
# ════════════════════════════════════════════════════════════════

# ── macOS ─────────────────────────────────────────
.DS_Store
**/.DS_Store
**/._*

# ── Xcode user/local files ────────────────────────
**/xcuserdata/
**/*.xcuserstate
DerivedData/

# ── SwiftPM user state ────────────────────────────
.swiftpm/xcode/package.xcworkspace/xcuserdata/

# ── Google Drive conflict artifacts (if any) ──────
*conflicted copy*

# ── Python (后端 FastAPI + Pi 点呼机) ────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.venv/
venv/
env/
ENV/
.env
.env.local
.env.*.local
pip-log.txt
pip-delete-this-directory.txt

# ── Python 测试 / 覆盖率 ──────────────────────────
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# ── Node (teacher_web 如果用 Node 栈) ─────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.next/
out/
dist/
build/

# ── Android (Phase 2 Android App) ─────────────────
*.apk
*.aab
*.ap_
*.dex
local.properties
.gradle/
captures/
# keystore 一律不进 git（memory: 备份方案见 TODO）
*.jks
*.keystore

# ── IDE / 编辑器 ──────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── 日志 ──────────────────────────────────────────
*.log
logs/

# ── 操作系统临时 ──────────────────────────────────
Thumbs.db
Desktop.ini

# ── Claude Code 本地设置（含机器路径，不入 repo） ─
.claude/settings.local.json

# ── session-coord skill 会话状态（心跳 / 锁 / inbox 不进 git） ─
.claude/sessions/

# ── graphify 知识图谱产物（82M+ 临时文件，不入 repo）─
graphify-out/

# ── graphify hook install 残留目录（hook 已 copy 到 admin/hooks/，详见 hooks/README.md ⚠️ 段）─
.beads/

# ── Matt Pocock skills `to-prd` 中间产物（PRD 详细文档，进 TODO.md 后即可弃） ─
.scratch/

# ── 数据库本地文件 ────────────────────────────────
*.sqlite
*.sqlite3
*.db
*.db.bak
*.bak

# ── 秘密 / 证书（2026-05-22 加 — Codex FC-035） ────
*.pem
*.key
*.p12
*.mobileprovision
secrets/

# ── Claude Code worktree（git submodule 残留，不入 git） ─
.claude/worktrees/
.claude/scheduled_tasks.lock

# ── TypeScript 增量编译产物 ─────────────────────────
*.tsbuildinfo

# ── 带说明后缀的备份文件（xxx.bak_before_yyy / xxx.bak3_zzz 这类，*.bak 挡不住）─
*.bak_*
*.bak[0-9]*

# ── 含服务器 IP/SSH 用户名的运维文档，itsuki 拍板不公开，只留本地（2026-06-12）─
ops/个人网站_pj部署运维.md
# ── 个人机器迁移/恢复指南（含 iCloud 私人路径 + Claude memory 恢复流程），不公开（2026-06-13）─
ops/MAC_MINI_SETUP.md
# ── 点呼机采购截图（采购记录/开发日记，非软件本体，不公开；2026-06-30 全量审查发现 24 张在公开范围）─
dev/rollcall_device/采购截图/
# ── 点呼机到货实物照片（清点证据；其中納品書一张含收货住址/电话/姓名，绝不公开）─
dev/rollcall_device/到货实物_2026-07-26/
