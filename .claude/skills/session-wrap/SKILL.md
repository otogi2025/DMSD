---
name: session-wrap
description: DMSD 会话收尾完整流程 — AC 素材 dump + 文件联动 + git 状态 + 设计同步 + 跨 repo 同步 + memory 维护 + WIP/TODO 刷新 + commit 起草。⭐ 主触发 = itsuki 说「收尾」/「总结今天」/「整理一下今天」/「记一下今天发生的事」时 CC 全量扫描会话上下文。次触发 = 关键词或事件（拍板/反思/学到/纠正/代码改/版本 bump/CC 主动发现问题）。原 ac-record skill 2026-05-04 改名升级 — 收尾不只是 AC，是项目级总收尾。
when_to_use: ⭐ 主触发 — itsuki 说「收尾」/「总结今天」/「整理一下今天」/「记一下今天发生的事」/「今天的事 dump 一下」 → CC 主动加载并全量扫描会话上下文执行 8 类收尾动作（AC dump / 文件联动 / git 状态 / 设计同步 / 跨 repo / memory / WIP+TODO / commit 起草）。次触发 — 关键词命中（启动/记一下/dump/留痕/拍板/反思/学到/纠正/推翻 / 以前我/之前以为/原来这样啊）或事件（代码改了/架构改了/版本号 bump/CC 主动发现的问题）→ 当下片段立刻 dump 不等收尾。
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

# Session Wrap Skill — DMSD 会话收尾完整流程

> **2026-05-04 改名背景**：本 skill 原名 `ac-record`，但 §5.5 已经包含 9 节收尾动作（不只是 AC dump，还有 git / WIP / TODO / commit 起草）。itsuki 反问后拍板：收尾 skill 不只是 AC 相关，还有文件联动 / 设计同步 / memory 维护等。改名 `session-wrap` 反映实际作用。
>
> **核心理念**：收尾 = 让下次会话能干净接上 + 当下决定不漏到风里。具体分两层：
> 1. **AC 素材层**：宁多勿少，最后再筛（任何 1% 可能用到的素材都记录，月度回顾时再筛选）
> 2. **工程层**：文件联动 / git / 设计同步 / 跨 repo / memory / WIP / TODO 全过一遍
>
> **⭐ 主要工作模式（2026-05-04 itsuki 拍板）**：itsuki 说「**收尾**」/「**总结一下今天**」/「**记一下今天发生的事**」 → CC 立刻执行 §5.5.0 全量扫描算法，**从会话第一条消息开始扫到最后**，找出所有候选素材 dump。**不要等关键词，不要只看最后一段**。详见 §0.4。
>
> **来源**：基于合格者报告分析（小松潜水无人机 / 间瀬校内 LAN / 匿名 B 未踏→Alice_Maker）+ DMSD 实战提炼，2026-05-04 由 v3 升级到 v4 做成 skill / 同日改名 session-wrap 扩展工程层。

---

## §0 三条根本性原则

### 0.1 叙事策略：raw 阶段记真实，月度筛选时归功

**raw/ 阶段格式（dump 时 CC 写）**：

> "AI 提出方案 X。我评估后[采纳 / 拒绝 / 改造为 X']，理由是 Y。"

2 种判断归属情况：

| 情况 | raw 写法 |
|---|---|
| AI 提方案，itsuki 采纳 | "AI 提出 X。我评估后采纳，因为 Y" |
| AI 提方案，itsuki 改造后采纳 | "AI 提出 X。我改成 X' 后采纳，因为 Y" |

**为什么这样**：
- AC 评的是判断力 / 学习能力，AI 推荐 → itsuki 评估采纳本身就是判断
- raw 会被 itsuki 整理素材时回读，写「AI 提的我评估采纳」既诚实又强调判断
- 月度筛选时（写自我推荐书 / AC 叙事文档），可以改写为更突出 itsuki 判断的版本（"私はXXの理由でYYを採用した"）

**绝对不做（2026-05-04 itsuki 拍板）**：
- ❌ raw 不写「我未完全理解就采纳」类自我贬低标记 — raw 是日后整理志望理由书的底稿，这种标记**本身就变成翻车证据**。itsuki 的逻辑：自己面试前会评估懂不懂，不需要 dump 系统帮他"标记"，真不懂会主动问 / 学。
- ❌ raw 不写「我设计了 X」「我决定 X」抹掉 AI 提案痕迹 — 失去判断过程的证据

### 0.2 最大化捕捉

每次会话结束 CC 必须 dump 所有候选素材到 `<raw池>/`，**包括看起来"没什么大事"的日常工作**。

理由：
1. AC 评委要看的是"持续在动手"的轨迹，碎片累积起来就是叙事
2. 月度回顾时筛选，比当场判断"这条值不值得记"更准确
3. **当时觉得没用的，后来可能成为关键** — 比如「今天学会 git push」配合「3 月之前我都用文件覆盖管理代码」back story → 「我对版本管理认知的根本转变」

如果一次会话真的纯执行类工作（rm/mv/重启），明确标"今日仅执行类工作" — 这本身也是记录，方便月度回顾时看出活跃度。

### 0.3 CC 主动发现的也算 itsuki 的素材

CC 看到 itsuki 不知道的问题（技术 / 流程 / AC / 学习方法 / 自我管理）→ 不只当场告诉他，**还要主动 dump 到 raw/ 当成素材**。

理由：即使是 CC 发现的，过程也算 itsuki 的（他有了"通过工具发现问题"的能力 + 他做了"采纳/拒绝/修改"的判断）。

特别重要：**"原本不知道，通过 CC 才知道"** 这类素材 — 这是 itsuki 独有的成长路径（详见 §3 模式 5）。

### 0.4 ⭐ 主要工作模式 = 收尾全量扫描（2026-05-04 itsuki 拍板）

> itsuki 现实情况：**自然对话里几乎不说"拍板/反思/学到/纠正"这类词**。所以「关键词触发」对他不是主要工作模式，**「收尾时 CC 全量扫描整个会话上下文」才是主要工作模式**。

**两种工作模式分工**：

| 模式 | 触发 | CC 行为 |
|---|---|---|
| **⭐ 主模式 — 收尾全量扫描** | itsuki 说「收尾」/「总结今天」/「整理一下今天」/「dump 一下今天」 | CC **从会话第一条消息开始按时间顺序过一遍**，对照 §2 5 级清单 + §1 模式 5 + §4 6 经典模式，找出所有候选素材，按 §5.5.0 全量扫描算法 dump |
| **次模式 — 对话中即时 dump** | 关键词命中 / CC 主动发现重要素材 | 当下片段立刻 dump 那一段，**不必等收尾**（避免错过细节） |
| **⭐ 今日总结模式（2026-05-21 itsuki 立项加）** | itsuki 说「今日总结」/「今天总结」/「写日记」 | CC **先**跑主模式全流程产出当前会话总结，**再**读今天所有会话总结的 md 合并写「今日总结」md。详见 §5.5.14.3 |

**铁律**：
- ❌ CC **不能**只 dump 最后一段对话就交差 — 收尾的本质是**对整个会话负责**
- ✅ CC 收尾时**必须从会话第一条用户消息扫到最后一条**，全量识别候选素材
- ✅ itsuki 不说关键词不等于没素材 — CC 自己识别（详见 §1 模式 5 触发词 + §4 6 经典模式）

---

## §1 itsuki 的素材 DNA — 模式 5（认知改变型）

> **这是 itsuki 最强的、独有的素材类型 — 必须重点挖掘**

### 1.1 为什么这是 itsuki 独有

合格者报告里，大部分学生是「我自己一步步学到的」。**itsuki 是「我通过 AI 协作学到的」** — 这个独特路径需要专门叙事。

模式 5 的力量：把"我以前不会"变成"我现在的方法论" — 不是炫耀终点知识，而是展示**通过工具实现自我升级**的能力。

### 1.2 触发关键词（CC 听到立刻挖深）

itsuki 嘴里出现以下词 → CC **必须**当场追问 + dump：

- 「以前我...」「之前我都是...」「我原本是...」
- 「我之前以为...」「我以前不知道...」
- 「原来这样啊」「居然是这样」「现在才知道」
- 「这个我没想到」「这个角度没考虑过」

### 1.3 追问 4 句话（挖完整素材）

听到上面任一关键词 → CC 必须问完这 4 句：

1. **「之前你具体怎么做的？」** — 还原错误/低效做法
2. **「什么时候 / 通过什么发现是错的？」** — 转折契机
3. **「正确的做法是什么？」** — 新方法论
4. **「现在为什么这样做更好？」** — 深层理解

### 1.4 经典示例（合格者级别）

> 之前我用「复制文件夹+改名」的方式管理代码版本，每改一次新建一个文件夹叫 "DMSD_v2_新版本_最终版_真的最终版"。结果几个文件夹之间的 diff 记不清，文件夹大小爆炸。
>
> 通过 AI 了解到 Git 后，我开始用 commit 记录每次修改，用 tag 标记版本，用 branch 做实验性改动。这让我意识到，软件开发的"版本管理"不是单纯的文件备份，而是一套关于"如何让代码状态可追溯、可回滚、可分支"的方法论。
>
> 现在我已经习惯用「小 commit + 清晰 commit message」的方式工作，并把这个习惯也用到了项目文档管理上。

这一段就是合格者级别：
- ✅ 具体的"之前的错误做法"（教授知道你来自哪里）
- ✅ 清晰的认知转变契机（AI）
- ✅ 从具体做法上升到方法论的层次
- ✅ 有迁移应用（用到了文档管理）

### 1.5 itsuki 的元认知金句（2026-05-04 拍板）

> **「这种事情没有体验，别人提醒我也不会知道」**

含义：他不仅不知道 X，他**不知道存在 X 这个东西要去问** → CC 必须主动扫描 + 主动提（详见 memory `feedback_proactive_diagnose_unknown_unknowns.md`）。

每次 CC 主动扫描发现 itsuki unknown unknowns → 立刻提 + dump 到 raw → 模式 5 又多一条。

---

## §2 什么值得记录 — 5 级清单

### 2.1 1 级 — 必须记录（核心 AC 素材，每条都不能漏）

#### 2.1.1 任何"发现问题→解决问题"循环

完整循环 4 要素：
- 注意到了什么不对劲（症状）
- 分析了什么原因（哪怕错了）
- 试了什么方法
- 结果如何（成功/失败都要）

例：「实装时发现签到接口在并发下会写入重复数据」/「跑回测时 PF 高得不正常，查了发现是前视偏差」

#### 2.1.2 任何技术判断的"为什么选 A 不选 B"

合格者报告里出现频率最高的写法。每次技术选择记下：
- 候选方案有哪些
- 各自优缺点
- 为什么最后选这个

例：「为什么 FastAPI 不 Flask」「为什么 NFC 不 QR」「为什么 PostgreSQL 不 MySQL」

哪怕你的"判断"是"AI 推荐了所以用了"，也要记 — 但要按 §0.1 第 1 / 2 / 3 种格式区分清楚。

#### 2.1.3 任何失败 / 卡点 / 推翻重做

合格者报告中失败素材占比 **>50%**。失败素材必须按 4 要素记：
1. 失败的事实（具体到什么程度）
2. 具体技术原因（不是"我能力不够"，而是"OOP 没掌握导致代码意大利面化"）
3. 当时尝试过的解决方法
4. 教训 / 下一步怎么改

#### 2.1.4 "原本不知道，通过 AI / 外部才知道"的认知改变

→ **见 §1 模式 5 详细规则**

#### 2.1.5 和 AI 意见不一致或纠正 AI

itsuki 反驳 / 修正 / 坚持不同方案 → 必须记。

理由：直接证明 itsuki 不是被 AI 牵着走，而是有自己的判断。

例：「AI 提议方案 A，itsuki 坚持 B 并说出了理由」/「AI 写了段代码，itsuki 指出 bug」/「AI 说应该这样，itsuki 查了资料发现 AI 错了」

#### 2.1.6 关键设计决策

任何影响系统架构的决策。哪怕事后改了，也要记下当时为什么这么决定。

例：数据库表结构变更 / API 设计选型 / 认证方式选择 / 前后端分离策略

### 2.2 2 级 — 强烈推荐（次核心）

- **学到的新概念 / 新工具 / 新语法** — 哪怕基础知识，配合 back story 变成成长叙事
- **工程方法论的转变** — 不是技术，是"做事方式"的改变（"先写代码再改" → "先写文档定义清楚再写代码"）
- **与他人的技术性对话** — 老师 / 同学 / 网友 / 社区任何技术讨论
- **自我管理 / 学习方法的改进** — 元层面"我怎么学习/工作"
- **推翻自己之前的判断** — "我之前以为 A 是对的，现在发现 B 才对"

### 2.3 3 级 — 可以记录（边缘素材，1% 可能用到）

- 阅读 / 视频学习记录
- 项目周边活动（CTF / 技术交流会 / 开源讨论）
- 灵感 / 未来想法（标 `[未来]`）
- 资源工具的发现和评价

### 2.4 4 级 — 看起来没用但要记（潜在素材）

- 日常工作的小决定（变量命名 / 文件位置 / 错误信息文案）
- 困惑和纠结（没解决的问题、想不清楚的点）
- 时间管理 / 状态记录（工作量证据）

### 2.5 5 级 — 不需要记录

- 寒暄 / 闲聊
- 和项目完全无关的话题
- 纯执行命令（rm / mv / 重启），除非执行后出问题

---

## §3 怎么记 — 格式

### 3.1 文件位置

**`<raw池>` 定义**（本文档所有 `<raw池>/` 都指这里）：

```
~/Library/Mobile Documents/com~apple~CloudDocs/02_学习与知识/升学/大学入試/00_原始素材池_两校共用/1_自动产出/raw详细叙事/
```

raw 不放 DMSD 仓库（itsuki 2026-06-11 拍板）：仓库 public，raw 含未过滤原始语气不适合公开；硬时间戳证据由同池 `对话原文/` 的 jsonl 承担。旧文件 107 个已整体迁入，仓库 `05_logs/raw/` 只剩指路牌 README。

```
<raw池>/
├── 2026-05-04.md          ← 一天一会话 / 一个主题
└── 2026-05-04_治理.md     ← 一天多会话时加主题词
```

### 3.2 raw 命名规则

| 命名 | 何时用 | 例 |
|---|---|---|
| **日期版** `YYYY-MM-DD.md` | 一天一会话 / 一主题 | `2026-04-17.md` |
| **主题版** `YYYY-MM-DD_主题.md` | 一天多会话 / 需要区分 | `2026-04-20_v0.3.1发布执行.md` |

**判断规则**：
1. 开场默认用日期版
2. 发现同日已有 raw → 当前会话和那个是**同一会话延续**吗？
   - 是 → **append**
   - 否 → 开主题版
3. 主题词选**本会话核心产出**（如 `v0.3.1发布执行` / `NFC架构讨论`）

### 3.3 单条素材推荐字段（必填 2 项 + 推荐字段）

> **模板原则**：核心叙述 + **必填 2 项**（itsuki 原话 + AC 价值），其他字段按需。长会话允许按 § 自由叙述，不强制每条都套模板。

**最简版（短素材）**：

```markdown
## HH:MM [标签] 一句话主题

（自由叙述背景 / 经过 / 关键判断）

**itsuki 原话**：
> "..."

**AC 价值**：（这条对应核心问题 #X，展示了什么能力）

#AC候选 #标签
```

**完整版（重要素材 / 长篇 dump）**：

```markdown
## HH:MM [标签] 一句话主题

### 背景 / 触发
（讨论是怎么起来的，之前的 context）

### 经过
（具体发生了什么，按时间顺序）

### 关键判断 / 学到的东西
（核心内容 — 为什么这样选 / 学了什么 / 推翻了什么）

### itsuki 原话 ⭐ 必填
> "..."（不润色，原话引用，可多句）

### AC 价值 ⭐ 必填
- 对应核心问题：#X #Y
- 展示了什么：（具体能力 / 特质）
- 可能用在自我推荐书的哪个部分

### 后续 / 未解决
（如果有未完成的部分，留待后续补充）

#AC候选 #标签1 #标签2
```

### 3.4 标签体系

**主类标签**（每条至少一个）：

`[问题解决]` `[技术判断]` `[失败]` `[认知改变]` `[分歧]` `[设计决策]` `[学习]` `[方法论]` `[访谈]` `[自我管理]` `[推翻]` `[资源]` `[周边]` `[未来]` `[小决定]` `[困惑]` `[状态]`

**辅助标签**：

- `#AC候选` — CC 默认给所有 1-3 级素材打这个
- `#AC強候補` — itsuki 月度回顾时手动升级
- `#待补充` — 需要 itsuki 后续补完整理解
- `#交易系统` `#DMSD` 等项目标签

### 3.5 5 个 AC 核心问题（打标签用）

合格者报告分析得出的 6 个核心评价维度（itsuki 项目相关 5 个）：

1. **問題発見** — 我注意到了什么没人解决的问题
2. **技術判断** — 我做了什么选择，为什么不选其他方案
3. **問題解決** — 我具体做了什么、遇到什么障碍、怎么破的
4. **失敗と修正** — 哪里失败了、原因、怎么改
5. **自己認識** — 学到什么、能力边界在哪、认知改变

每条素材标对应核心问题号（可多个）。

### 3.6 详细度 — 按"思考层数"分级

| 层数 | 触发条件 | 关键词识别 | 目标篇幅 |
|---|---|---|---|
| 1 层（事实）| 做了 X | "做了"「commit 了」 | 100 字 |
| 2 层（理由）| 做了 X，因为 Y | "因为"「为了」 | 300 字 |
| 3 层（取舍）| 做了 X 而非 Z，因为 Y | "选 X 不选 Z"「权衡了」 | 500-800 字 |
| 4 层（反悔）| 原想做 X，发现 W，改做 Y | "原本想"「发现」「改成」 | 1000-1500 字 |
| 5 层（元思考）| 这次让我意识到我之前对 XX 理解不对 | "我意识到"「整个理解都不对」「重新想」 | 1500-2500 字 |

**判断标准是思考深度，不是事件大小**。一个看起来小的事件如果引发 5 层思考（"通过 Git 我意识到自己之前对版本管理的整个认知都是错的"），就该写 1500 字+。

### 3.7 itsuki 原话的处理

**绝对不润色**。原话再不通顺、再口语化、再有语病，都保留原貌。

理由：
- 原话是 itsuki 思维的真实 snapshot，月度回顾时能看出认知变化
- 写自我推荐书时直接拿原话改写，比拿润色过的"假 itsuki"语言更真
- 教授读自我推荐书会感受到「真人在思考」vs「AI 润色出来的标准答案」

**可以**：在原话旁加 `[CC 注：上下文是 XX]` 补充背景。

**不可以**：把"啊那个我觉得就是说"改成"私が考えるに"。

### 3.8 推测的处理

CC 观察到但不 100% 确定的部分用 `[推测]` 标记，让 itsuki 事后校正。

例：`itsuki 採用了方案 A。[推测：可能是因为他之前提到不熟悉方案 B 所需的库，但他没明说]`

**零推测 < 标记推测** — CC 完全不猜让 itsuki 自己补，最后会出现大段空白。CC 猜错了 itsuki 能校正 — 比 CC 不猜好。

---

## §4 经典素材模式（按合格者报告整理）

合格者报告里反复出现、教授高度认可的写法模式。CC 看到符合的内容要特别详细记。

### 模式 1：问题→尝试→解决（最基础）

合格者小松潜水无人机：
> 玻璃打孔会碎 → 学了边浇水边打孔的技术 → 解决  
> 打孔后渗水 → 树脂固定 + CAD 仿真 → 解决

**记录要点**：每个具体技术障碍单独成段，不混着写。

### 模式 2：假设崩了→继续调查→真因（最高级）

合格者間瀬校内 LAN 调查：
> 现象（网慢）→ 别人结论（带宽不够）→ 自己假设（中间链路慢）→ 调查（全 1Gbps，假设崩）→ 没放弃 → 真因（监控软件 UDP 碎片化）

**记录要点**：把「假设崩了但没放弃」的转折单独标出来 — **这是教授最爱的瞬间**。

### 模式 3：失败→分析→下个项目吸取教训（跨项目）

合格者匿名 B 的未踏 → Alice_Maker：
> 未踏二次落选 → 具体原因（没专注一个方向）→ 下个项目 Alice_Maker 聚焦"理想 AI 通话伙伴"

**记录要点**：失败和下个决定要明确画出因果链。

### 模式 4：v1→v2 版本演化（同一个东西迭代）

itsuki 的交易系统 V5→V8 / DMSD 的 RollCall spec v0.1→v0.2→v0.3 就是这个模式。

**记录要点**：每个版本之间的"为什么要升级"必须明确。

### 模式 5：原本不知道→通过 XX 知道→现在的做法

→ **详见 §1 全文**。这是 itsuki 主线模式，必须重点挖。

### 模式 6：技术判断的"取舍三角"

每次"为什么选 A 不选 B"时，记完整的判断结构：
- 备选方案的优缺点列表
- 决定性的判断标准（性能 / 成本 / 学习曲线 / 团队规模等）
- 最终选择和理由

合格者小松：「为了安価地长距离传输数据，选择了 LPWA 的一种 LoRaWAN」 — 一句话里包含了选择标准（安価 + 长距离）和具体选项。

---

## §5 CC 操作手册

### 5.1 会话开始

1. 检查 `<raw池>/YYYY-MM-DD.md` 是否存在，**不存在则创建空文件**
2. **不主动问** itsuki「上次有没有没记下来的事」— 按 CLAUDE.md「不主动催进度」铁律
3. 如果 itsuki 自己想补，他会主动说

### 5.2 对话中触发捕捉（次要模式 — 主要模式见 §5.5.0 全量扫描）

> ⚠️ 重要前提：itsuki 自然对话很少出现关键词，所以「对话中即时 dump」是**辅助机制**，不是主要工作流。**真正的素材识别在 §5.5.0 收尾全量扫描时做**。

任何符合 §2 中 1-4 级的内容出现 → CC 直接 dump 到当日 raw 文件，**默认不问同意**。

收尾时（§5.5.0）给 itsuki 列「今天 dump 了哪 N 条」清单，让他事后否决 / 删除 / 改写。

**例外 — 高敏感场景必须当场问**：
- 涉及 itsuki 个人隐私 / 他人评价 / 学校敏感事件 → 当场问
- 涉及 iCloud `03_素材_候选/` `04_素材_成品/` 写入 → 必须当场授权（按 §6 权限速查）

**为什么这个模式是辅助**：
- itsuki 不会主动说「我刚才拍板了 X」 — 他只会说「就这样吧」
- itsuki 不会说「我学到了 Y」 — 他可能说「原来这样啊」（这种间接表达）
- → 关键词匹配漏率高，**必须靠收尾时 CC 全量扫描兜底**

### 5.3 主动追问触发清单

挖掘"itsuki 没意识到自己说了什么有价值"的素材。CC 听到以下 → 主动追问：

| itsuki 说 | CC 立刻追问 | 挖什么 |
|---|---|---|
| "以前我..." | 「以前你具体怎么做的？什么时候改的？为什么改？」 | 模式 5 金矿 |
| "我之前以为..." | 「之前的理解是怎么形成的？什么让你改变看法？」 | 推翻判断素材 |
| "原来这样啊" | 「之前怎么理解的？现在的理解差在哪？」 | 学习素材 |
| 选了某技术没说理由 | 「为什么选这个？还考虑过别的吗？」 | 技术判断素材 |
| 调试 bug | 事后追问：「最后怎么定位的？试了哪些方向？」 | 问题解决素材 |

### 5.4 写代码时的强制说明

CC 写出以下代码时**必须停下来告诉 itsuki**：
- itsuki 没接触过的库 / API
- 非平凡模式（装饰器 / async / 元类 / 并发原语 / 复杂类型 / 设计模式）
- 跨多文件改动

话术：
> 「这段用到了 X。你熟悉这个吗？  
> 不熟的话我先讲一遍。  
> 等你能用自己的话解释了，我们记一笔到当日日志。  
> （标签：[学习] 或 [认知改变]）」

记录时让 itsuki 口述一遍自己的理解，CC 转写到正文。

### 5.5 会话结束 — 详细动作（§5.5.0 全量扫描 2026-05-04 加，§5.5.14 daily-archive 2026-05-21 加，§5.5.15 decision-draft 2026-05-22 加，§5.5.16 dev-log 工程简报 2026-06-08 加）

#### 5.5.0 ⭐⭐⭐ 全量扫描算法（每次收尾必跑，比 5.5.1 dump 更前一步）

**触发**：itsuki 说「收尾」/「总结今天」/「整理一下今天」/「记一下今天发生的事」/「今天的事 dump 一下」 → CC 立刻执行下面 7 步。

**步骤**：

```
Step 1: 把整个会话上下文从第一条用户消息到当前最后一条，按时间顺序过一遍
        （CC 收尾时本来就持有完整上下文，只是要主动全量扫描，不只看最后一段）

Step 2: 对每段对话/事件，分类判断 — 这是几级素材？
        a. 1 级（必须记）：发现问题→解决 / 技术判断 A 不 B / 失败 / 模式 5 / 纠正 CC / 关键设计决策
        b. 2 级（强推）：学新概念 / 工程方法论转变 / 技术对话 / 自我管理改进 / 推翻自己
        c. 3 级（可记）：阅读学习 / 周边活动 / 灵感 / 工具发现
        d. 4 级（看似无用要记）：日常小决定 / 困惑纠结 / 时间状态
        e. 5 级（不记）：寒暄 / 无关 / 纯执行
        → 1-4 级**全部进候选清单**，5 级丢弃

Step 3: 对每个候选素材，识别属于哪个模式（§4 六模式）：
        - 模式 1: 问题→尝试→解决
        - 模式 2: 假设崩了→继续→真因（最高级）
        - 模式 3: 失败→吸取教训→下个项目
        - 模式 4: v1→v2 版本演化
        - 模式 5: 原本不知道→通过 X 知道→现在的做法（itsuki 主线）
        - 模式 6: 技术判断的取舍三角
        → 标在素材头部，决定篇幅按 §3.6 思考层数分级

Step 4: 检查 CC 主动发现的问题（§0.3）— 有没有 CC 看出 itsuki 不知道的事？
        → 这种也是素材，按 0.3 主动 dump

Step 5: 按 §3.2 决定 raw 文件名（日期版 / 主题版）
        → 默认 YYYY-MM-DD.md，今天已有同会话延续的 raw → append；新主题 → 主题版

Step 6: 按时间顺序 dump 全部候选素材到 raw（§3.3 推荐字段格式）
        → 必填 2 项：itsuki 原话 + AC 价值
        → 长素材按 §3.6 思考层数决定篇幅
        → §3.7 原话不润色

Step 7: 给 itsuki 列「我从今天对话扫到 N 条素材」清单：
        ┌────────────────────────────────────────┐
        │ 今天 dump 了 X 条到 raw/2026-MM-DD.md：│
        │ 1. §A 标题 [模式X] [思考层数Y]          │
        │ 2. §B 标题 [模式X] [思考层数Y]          │
        │ ...                                    │
        │                                        │
        │ itsuki 看一眼，要否决 / 删除 / 改写哪条直接说│
        └────────────────────────────────────────┘
```

**铁律**：
- ❌ CC **绝不能**只 dump 最后一段对话就跳到 §5.5.1 — 那叫"敷衍收尾"
- ❌ CC **绝不能**因为某段对话"看起来无聊"就跳过 — §0.2 「宁多勿少」+ 4 级素材规则
- ✅ 全量扫描完成后，再走 §5.5.1 ~ §5.5.7 剩下的收尾动作
- ✅ 扫描发现重大遗漏（之前没 dump 但应该 dump 的）→ 补 dump，不假装"已经记过了"

#### 5.5.1 ⭐⭐⭐ AC 素材 dump 写法（§5.5.0 Step 6 的具体执行规范）

> 本节是 §5.5.0 Step 6 的具体写作规范。§5.5.0 决定**扫到什么**，本节决定**怎么写**和**写到哪**。

##### 5.5.1.A AC 素材去向表 — 哪些话该写到哪些文件（2026-05-27 早段-3 itsuki 拍板加 — 防中枢档案污染）

**起因**：5-27 早段-3 itsuki 发现 CC 把 AC 模式分析（「模式 5 顶级 / AC 价值 ⭐⭐⭐⭐⭐」）写到了中枢 `项目档案/DMSD.md` 末尾「更新日志」段 → 排查发现 Tango.md + QTS.md 同类污染 + 5-27 三段会话 AC 素材**只写 DMSD raw 没写筑波中央 inbox**（漏一处）。

| AC 内容种类 | ✅ 该写到哪 | ❌ 不能写到哪 |
|---|---|---|
| 学习过程详细叙事（itsuki 不会 → 问 → 学会循环） | `<raw池>/YYYY-MM-DD.md` | 中枢档案 / WIP 长篇 |
| AC 模式归类（模式 1-7 + 顶级 / 中级 / 弱） | DMSD raw + 筑波 `06_radar_inbox/ac_scratchpad_<日期>.md` | 中枢 `项目档案/*.md` / decision_log 主体 |
| AC 价值评分（⭐⭐⭐⭐⭐ 等） | 同上 | 同上 |
| AC 叙事策略 / 关联度 / 风险 | iCloud `02_学习与知识 / 升学 / 大学入試 / 筑波大学 AC入試 準備 / 03_素材_候选/` | 中枢档案 / DMSD repo |
| 跨项目协同状态（现状一句话 / 工程事实） | 中枢 `项目档案/<项目>.md` | — |
| 决策脉络（方向反转 / 架构变化 / 选型变化等 6 类） | `DMSD/05_logs/decisions/decision_log.md`（详见 §5.5.15） | — |

**例外注脚**：`decision_log.md` 条目末尾写一行「相关：raw/...md（深度 AC 素材 + 模式 X 顶级）」算合法**指针注脚**（指向原始日志），不算把 AC 评分写在 decision_log 主体里 — 不要清。

##### 5.5.1.B 强制双写 — DMSD raw + 筑波中央 inbox（2026-05-27 早段-3 立铁律）

收尾 AC dump **必须同时写两个地方**：

1. **DMSD raw 日志**（详细叙事 — 学习过程为核心）：

   写到 `<raw池>/YYYY-MM-DD.md`（按 §3.2 命名规则 — 单日多会话用 `YYYY-MM-DD_<主题>.md` 命名）。

2. **筑波 AC 中央 inbox**（按信号 tag 格式 — 6 字段：起因 / CC 实际产出 / AC 价值 / 评分 / 学术延伸 / 出处指针）：

   写到 `~/Library/Mobile Documents/com~apple~CloudDocs/02_学习与知识/升学/大学入試/00_原始素材池_两校共用/1_自动产出/自动捕获/ac_scratchpad_YYYY-MM-DD.md`（2026-06-04 素材池重构后的新路径 — 跟 ac-radar 实时写的是**同一个当天文件**）。

   写法参考目录里已有文件。

**为什么必须双写**：
- raw 池 = 详细叙事（已在 iCloud，2026-06-11 迁出仓库），itsuki 整理两校素材时的底稿
- 筑波 06_radar_inbox = 跨项目 AC 素材汇总池，给 itsuki 后期挑精华做志望理由书 / 自我推荐书时看
- 只写 raw 不写 inbox = AC 素材落不进 itsuki 整理素材时的工作目录（5-27 早段-3 就漏了一次）

##### 5.5.1.C 学习过程为核心（详细叙事写法）

每个「itsuki 不会 → 问 CC → CC 教了 → itsuki 学会了」循环都详细记：
- 他发现了什么问题（症状 / 报错 / 不对劲的现象）
- 他不会怎么解决（卡在哪一步、为什么卡）
- 他问 CC 什么
- CC 教了什么概念 / 步骤（写完整解释，不写"教了 X"一句话）
- itsuki 学会后能怎么自己复述（用他自己的话）
- 这个学习能用在 AC 哪个核心问题

**详细记，宁长勿短**。

##### 5.5.1.D 反模式（写到错地方）

- ❌ 把 AC 模式归类 / AC 价值评分写到中枢 `项目档案/<项目>.md`（5-27 早段-3 真实翻车案例 — DMSD.md / Tango.md / QTS.md 三档案都中招）
- ❌ 把 AC 叙事策略 / 关联度评估 / 风险分析写到中枢档案（5-27 早段-3 QTS.md 整段污染案例）
- ❌ 只写 DMSD raw 不写筑波 06_radar_inbox（5-27 三段会话漏一次，早段-3 补写）
- ❌ 把项目工程事实写到筑波 06_radar_inbox（inbox 是 AC 素材，不是项目状态汇报）

#### 5.5.2 简单中文总结（给 itsuki 看的 — 短）

总结**只能用大白话中文**：
- ❌ 禁用「落档」「实装」「prewarm」等术语
- ❌ 不大段日语 / 英语
- ✅ 「我这次写了 X 文档 / 改了 Y 代码 / 你 ⌘R 后能看到 Z」
- ✅ 必须出现专业词时第一次就用中文解释

总结分 3 段，每段 3-5 行：
1. **做了什么**（中文人话讲改了哪些 / 你能看到什么）
2. **AC 素材记了什么**（哪几条 dump 到 raw + 一句话 hook）
3. **下次要做什么**（未完成 backlog）

**「做了什么」段的产法**：不重读会话上下文 — 直接把刚写好的 `05_logs/dev_log/` 当日工程简报（§5.5.16，先写它再产总结，顺序不能反）压缩成大白话。理由：① 收尾时上下文最满，重新回忆整个会话容易漏 / 错；② dev_log 是刚从 git 抓的客观事实，照它总结不会编。每条带一句「这是干嘛的」，零专业术语。

#### 5.5.3 「今天 dump 了哪 N 条」清单（事后否决机制）

→ **执行：见 §5.5.0 Step 7**（全量扫描算法已经在 Step 7 完成清单展示）。

本节保留只是为了让"事后否决机制"在 §5.5 7 节流程里有显式名字。具体格式 / 触发 / itsuki 否决渠道见 §5.5.0 Step 7。

#### 5.5.4 文件联动检查（双重保险 — itsuki 2026-06-11 拍板）

两条腿都要走，缺一不可：

1. **脚本扫**：跑 `bash bin/sync-check.sh`（背后是 sync-rules.sh 的全部规则，机械正则匹配）
2. **CC 自查**：Read `.claude/skills/file-linkage/SKILL.md`，把本会话改过的文件逐个对照规则表**人工过一遍**——脚本只会正则匹配路径，CC 负责语义判断（比如改动虽然没碰规则里的路径、但实质影响了某个联动对象）

为什么要双重：脚本快但死板（路径没匹配上就静默放行），CC 懂语义但会忘。两边各补对方的盲区。

触发的同步文件全列出来 → 当场改 / 加 TODO。

#### 5.5.5 规范文档同步检查

跑 `bash bin/sync-check.sh` → 看有没有联动漏改。

#### 5.5.6 WIP / 一致性检查 / 未完成 backlog

- 更新 `00_admin/WIP.md`「最近会话」加本次 1-2 行总结（短小为美，最近 5 条）
- 新 backlog → `00_admin/TODO.md`（不写 WIP）
- 多会话占用 → 登记 / 移除

#### 5.5.7 git commit（CC 除 push 外本地操作可自主，2026-05-28 itsuki 放宽）

> **2026-05-28 itsuki 拍板放宽**：git 只要不 push，就算出问题也在本地、可以回退修改，所以本地操作 CC 可自主做。原「bump / tag 要 itsuki 明示」规则推翻。

- ✅ CC 自动 commit 本地（按惯例 message + 不加 Co-Authored-By trailer）
- ✅ CC 可自主 bump 版本 + 发布 tag — 但仍走 [[version-bump]] skill 决策树判断「该不该 bump」（CC 有否决权那套不变），区别只是「bump / tag 这个动作」不再需要 itsuki 逐次明示
- ❌ CC 不 push（push 必须 itsuki 明示 — 唯一保留的明示动作）
- ⚠️ 破坏性命令仍要先确认：`git reset --hard`（丢未提交改动）/ `rm` 删文件 — 这俩恢复不了，不在「本地可回退」范围内

#### 5.5.8 月末最后会话

每月最后一次会话结束提醒：
```
这是 YYYY-MM 最后一次会话。月度回顾提醒：
- 挑本月 #AC候选 升级为 #AC強候補
- 在 iCloud 03_素材_候选/ 写本月 review
- 写 iCloud 04_素材_成品/monthly_review/YYYY-MM.md
```

#### 5.5.9 git 仓库状态收尾确认（2026-05-04 itsuki 拍板加，从 session-start skill 挪过来）

> **背景**：原本是 session-start skill 的内容（启动时跑），itsuki 拍板挪到结尾 — 启动时 CC 不需要这些信息，但**收尾时**正合适：确认 commit 干净 / 决定要不要 push / 看有没有别会话残留。

收尾时跑这 4 项 git 状态检查（已经做过 5.5.6 commit 的话部分项可以跳过）：

```bash
# A. 工作树状态
git status

# B. 未 push commit 数
git log --oneline origin/main..HEAD

# C. stash 是否挂着活
git stash list

# D. 残留垃圾文件嫌疑（按需跑）
git status --porcelain | grep -E '\.(bak|bak[0-9]|DS_Store)$|/File\.txt$'
```

报告格式：

```
📋 git 仓库状态收尾报告

- branch: main / 别 branch ⚠️
- 已修改未 staged: N 个 — [是不是别会话改的？要不要 stage]
- untracked: N 个 — [残留垃圾列出来 / 正常文件不列]
- staged 但未 commit: N 个 — [是不是忘 commit 了]
- 未 push commit: N 条（最新 [hash] [一句话]）— [按 commit/push 协作分工等 itsuki 拍板要不要 push]
- stash: N 条 — [是不是挂着的活忘了]

→ 疑似遗留 / 需确认：[列污染项]
→ push 决定：等 itsuki 指令
```

铁律（2026-05-28 itsuki 放宽本地操作后更新）：
- ✅ commit / add / bump / tag 等本地操作 CC 可自主（详见 §5.5.7）
- ❌ **不主动 push** — push 是唯一要 itsuki 明示的动作
- ⚠️ **不主动 `git reset --hard` / `rm` 删 untracked** — 可能是别会话的活 + 这俩恢复不了，要先确认
- 报告状态 + 列待决项（push 决定 + 破坏性命令决定等 itsuki）

#### 5.5.9.5 交接文件清理（2026-06-05 itsuki 拍板加 — 机制兜底，不靠自觉）

> **背景**：AI 会话间交接文件（GOAL 提示词 / 接手提示词 / 压缩点上下文）以前散丢在 `00_admin/` 根 + `05_logs/`，用完没人删越堆越多。`00_admin/` 根是放长期治理台账的（WIP/TODO/心智模型/同步点清单/hooks），不该混一次性交接件。已建专门文件夹 `00_admin/handoff/`（只放活的交接件）。

收尾时过这 3 条：

1. 本会话**接手并完成**了某个交接任务？ → 把对应 `00_admin/handoff/<交接文件>.md` **移到 `99_archive/YYYY-MM-DD_handoff已完成/`**（`git mv`，留痕不删 —— 多会话协作是 AC 入试素材）+ 更新 `00_admin/handoff/README.md` 末尾「当前活着的交接文件」清单。
2. 本会话**新写**了交接文件给别的会话？ → 放进 `00_admin/handoff/`，**别丢 `00_admin/` 根或 `05_logs/`**。
3. ⚠️ **别动别会话正在用的交接文件** —— 多会话共用工作区，untracked（`??`）的交接件多半某会话开着，搬走会让它丢文件。不确定就留着 / 问 itsuki。

完整规则见 `00_admin/handoff/README.md`。

#### 5.5.10 文件联动检查（2026-05-04 itsuki 拍板加）

跑一遍同步检查脚本，确认这次会话改的文件联动都改了：

```bash
bash bin/sync-check.sh
```

如有警告（红色 ⚠️）→ 当场修，不留到下次会话。

**脚本跑完不算完** — 还要按 §5.5.4 第 2 条 Read `.claude/skills/file-linkage/SKILL.md` 人工对照一遍（双重保险，itsuki 2026-06-11 拍板）。

**为什么不靠 commit 时 pre-commit 兜底**：会话中途没 commit 的累积改动可能多文件，commit 时全报会蒙。结尾跑一遍提早发现。

#### 5.5.11 设计文档同步检查（2026-05-04 itsuki 拍板加）

按这次会话改了哪一端，检查对应 DESIGN_LOG 章节是否同步更新：

| 这次改了什么 | 必查 DESIGN_LOG |
|---|---|
| iOS Swift view / Foundation 组件 | `03_dev/student_ios/IOS_DESIGN_LOG.md` 对应章节 |
| backend models / schemas / routers | `03_dev/backend/BACKEND_DESIGN_LOG.md` |
| teacher_web 任意 .vue / .ts | `03_dev/teacher_web/WEB_DESIGN_LOG.md` |
| ≥2 端共用功能 | `02_design/system_features.md` 章节 |

如果 DESIGN_LOG 对应章节没动 → 报告 itsuki 列差异 + 起草补充段等粘贴。

#### 5.5.12 跨 repo 同步检查（2026-05-04 itsuki 拍板加）

DMSD 是 iOS single source；`Tomoshibi-iOS` 独立 repo 是镜像。

```bash
# 看这次会话有没有改 iOS .swift 文件
git diff --name-only HEAD~5 HEAD -- '03_dev/student_ios/v1/**/*.swift' 2>/dev/null
```

如果改了 → 提醒 itsuki：

```
🔄 这次改了 N 个 iOS Swift 文件 → bash bin/sync-ios-refs.sh 同步到 Tomoshibi-iOS repo
```

CC 不主动跑（同步动作可能涉及推 commit 到独立 repo，按 commit/push 分工等 itsuki 拍板）。

**AC 状态快照刷新（itsuki 2026-06-11 拍板并入本项）**：

`筑波大学 AC入試 準備/状态快照.md`（iCloud）是给 **AC 侧会话**（写志望理由书 / 升学规划时的 Claude）看的「DMSD 走到哪了」快照。它死过一次（5-14 → 6-11 近一个月没更新，内容停在 v0.8.0），根因跟 learning_path 一样 — 不在核对表没人管。规则：

- 本会话有 **AC 侧视角值得知道的进展**（版本大跳 / 管理员交互 / 上架动作 / 重大架构决策 / 管理体系改版）→ 刷新它：更新顶部「最后更新」+ 重写「当前焦点」段 + 在「最近重大变化」最上面追加一段
- 没有这类进展 → 跳过，报告「⏸ 无 AC 侧重大进展」
- CC 写的行一律标 🤖（CC 起草待 itsuki 审），不动 itsuki 原创行 — 这是 AC 文件，归属规则照 §5.5.1.A

#### 5.5.13 Memory 维护扫描（2026-05-04 itsuki 拍板加）

会话中 itsuki 拍板过新规则吗？看这些信号：

- itsuki 说「以后这样」/「下次记得」/「不要再 X」 → feedback memory 候选
- itsuki 说「现在变成 Y」/「Z 项目状态变了」 → project memory 候选
- itsuki 透露新的个人信息（出身 / 学校 / 偏好） → user memory 候选
- itsuki 说「在 X 平台上看 Y」 → reference memory 候选

如有候选 → 走 `memory-write` skill SOP（4 类型判断 / 查重 / frontmatter / MEMORY.md 索引）。

**反模式**：会话中 itsuki 明确说了「以后这样」CC 当时只回「好的」却没 dump 到 memory → 下次会话 CC 不知道这条规则 → itsuki 又得纠正一次。

#### 5.5.14 daily-archive 联动 — jsonl 对话原文备份到 iCloud

> 每天的 CC 对话**原文全量备份**（jsonl）— 以后想找任何一段对话（itsuki 的话 / CC 的话 / 工具调用 / 写入的代码）都能全文搜索到。

**一件事**（DMSD 收尾时除 §5.5.0~§5.5.13 之外**额外**跑）:

##### 5.5.14.1 cp jsonl 原文到 iCloud（零 token）

调脚本 `~/.claude/scripts/daily-archive-cp.sh`：

```bash
bash ~/.claude/scripts/daily-archive-cp.sh --cwd "$PWD"
```

脚本自动算出当前会话 jsonl 路径（用 cwd 转码 + mtime fallback），复制到 iCloud 共用池 `00_原始素材池_两校共用/1_自动产出/对话原文/YYYY-MM-DD_设备名_会话ID短码.jsonl`。

完全脚本机械复制，**不消耗 token**。

##### 5.5.14.2 会话总结 / 今日总结 — 已退役

「详细日记式会话总结」「今日总结」两个产出已砍（内容与 raw 详细叙事 + dev_log 工程简报完全重叠，决策见 `05_logs/decisions/decision_log.md` 2026-06-10 条目）。触发词「今日总结 / 今天总结 / 写日记」不再产出 iCloud 日记，按 §5.5.0 正常收尾即可。

##### 5.5.14.3 跟 ac-radar 的并行关系（不冲突）

- ac-radar 继续往 iCloud 共用池 `1_自动产出/自动捕获/` 写实时信号条
- daily-archive 只写 `1_自动产出/对话原文/`（jsonl 机械备份）
- 两条线并行写，互不调用

##### 5.5.14.4 反模式

- ❌ 跳过 jsonl cp 步骤（"等下次再说" — 不行，下次 cwd 可能变了找不到）

#### 5.5.15 decision-draft — 决策日志草稿起草子流程（2026-05-22 itsuki 拍板加）

> **背景**：5-22 点呼机推进会话里撞到 5-12~16 中国海关查扣事件 → itsuki 拍板撤回中国海运渠道改日本本地买。CC 主动 propose 写 `05_logs/decisions/decision_log.md` 草稿，itsuki 反问「哪个 skill 提醒你写？」+ 拍板「decision_log 专门做一 skill — 我的目标是多方位收集素材」+ 反问「我不是有 ac-radar 了吗」+ 最终拍板「直接放收尾流程里作子节，不做独立 SKILL.md」。
> 本节 = 不重复 ac-radar（实时短 tag）/ §5.5.1 AC dump（深度叙事）已做的事，只补「按 `decision_log.md` 格式起草决策脉络条目」这一空缺。

**时机**：在 §5.5.7 git commit 之前跑（CC 直接 Edit 完 `decision_log.md`，跟其他改动一起 commit）。

**触发**：每次收尾必跑，不靠关键词。CC 复用 §5.5.0 全量扫描结果，识别本会话**所有决策（普通 + 重大都记，不只重大）**：

- **普通决策**（实施细节之上的取舍 / 拍板，但够不上下面 6 类重大标准）→ 写 3-5 行短条目进 `05_logs/decisions/decision_log_全部.md`（日期 / 决策一句话 / 为什么一句话）
- **重大决策**（命中下面 6 类）→ 短条目照写进 `decision_log_全部.md`，**另**按完整格式写进 `05_logs/decisions/decision_log.md`（重大决策正本）
- 本会话一条决策都没有 → 输出「本次无决策，§5.5.15 跳过」一行

**重大决策识别 6 类**（§5.5.0 扫描结果再过一遍）：

1. **方向反转 / 撤回前面拍板** — 例：5-22 中国海运 → 日本本地买
2. **架构变化** — 例：4-15 thin client / thick server 原则（点呼机只搬运不判断）
3. **选型变化** — 例：4-15 Pi 4B → Pi Zero 2 W 候选
4. **路线 / 阶段调整** — 例：4-19 G2 取消分阶段
5. **铁律 / 红线落地** — 例：iOS 早期代码扔
6. **跨端 / 跨项目 / 跨工具的取舍** — 例：5-06 退役独立 repo

**草稿格式**（按 `05_logs/decisions/decision_log.md` 顶部模板）：

```markdown
## YYYY-MM-DD — [决策简述]

**之前的决策**: ...
**新的决策**: ...
**为什么改**:
1. ...
2. ...
**这个改动影响了什么**: ...
**相关**: ...（raw 路径 / 别的 decision_log 条目 link）
**事后回看**(几个月后补填):
```

**铁律**（CLAUDE.md AC 默认底线 §3 + 本 SKILL §6.1）：

- CC **直接 Edit** `05_logs/decisions/decision_log.md` 顶部「## 决策记录(倒序)」段后，按文件顶部模板格式插入新决策
- 不让 itsuki 做粘贴的手活（itsuki 5-26 怒怼「他妈的别让我来做」拍板 — 详见 memory `feedback_no_handoff_work_back_to_itsuki.md`）
- itsuki 看完不满意会自己改 / 让 CC 改

**输出位置**：普通条目直接写进 `05_logs/decisions/decision_log_全部.md` 顶部，重大条目两边都写。收尾报告里只引用「## 2026-XX-XX 决策标题 → 文件名:行号」级别索引让 itsuki 知道写哪了。

**跟 ac-radar / §5.5.1 AC dump 分工**（同份会话上下文，3 种产出，不重叠）：

| 能力 | 产出位置 | 内容形式 |
|---|---|---|
| ac-radar（实时） | 中央收件箱 + 当天 raw 短 tag | 一句话标签 + attribution |
| §5.5.1 AC dump（收尾） | 当天 raw 完整段 | 叙事 + 模式分析 + AC 价值评分 |
| §5.5.15 decision-draft（收尾） | CC 直接 Edit `decision_log.md` 顶部 | 格式化的「决策脉络」条目 |

**例**（5-22 海关事件，本次会话实测产出）：

```markdown
## 2026-05-22 — 撤回中国海运渠道，改日本本地买点呼机配件

**之前的决策**(2026-05-08): 11 件配件淘宝集中下单 ¥381 RMB 海运到日本（含 Pi 3A+ / PN532 V3 红板 / ST25DV16K × 2 / NTAG215 × 50 / LED 5 色套装 / USB 小音响 / 面包板 / 杜邦线 / 电源 / 透明壳 + 风扇）
**新的决策**: 被海关查扣的不要了。以后所有点呼机配件改在日本本地买（Amazon.jp / 秋月電子 / スイッチサイエンス / 千石電商 / Yahoo Auction / メルカリ）
**为什么改**:
1. 5-12 到 5-16 之间这批配件走中国海运被海关查扣全没。原因是为省运费打成一个包裹寄出 → 不清楚是哪 1-2 件触发查扣，但所有件连带没收（打包 = 单点故障 / single point of failure）
2. 长期维护视角：日本本地买 = 本地有备件 + 退换货走日本邮政 = 以后某个配件坏了维护方便
3. 风险跟件数非线性 — 一件出事全没。下次拆寄 + 本地买 = 真省，不只是规避海关
**这个改动影响了什么**:
- 原 ¥381 RMB 淘宝清单 + 「下单」「收货清点」2 任务作废
- 预算重估：日本本地价 vs 中国海运价，预计贵 1.5~2 倍但消除海关风险 + 提速到货
- 硬件设计文档 `02_design/hardware_design.md` §2 全部型号 / 价格 / 渠道字段要重写（日本重新选型后）
- 点呼机设计文档 `03_dev/rollcall_device/ROLLCALL_DEVICE_DESIGN_LOG.md §1.2` 加海关事件 + 改日本买
- 未来从中国寄任何件都要拆成 2-3 包分批寄
**相关**: `<raw池>/2026-05-22.md`（深度 AC 素材 + 模式 1+2+6 三维度拆解）
**事后回看**(几个月后补填):
```

**反模式**：

- ❌ 拿当前会话所有「拍板 / 讨论」都塞 decision_log（不区分「决策」vs「实施细节」）
- ❌ 跟 §5.5.1 AC dump 内容大量重复（dump 是叙事，draft 是决策脉络，不一样）

#### 5.5.16 dev-log 工程简报 — 纯工程流水账（2026-06-08 itsuki 拍板复活）

> **背景**：itsuki 2026-06-08 反映「收尾光收集 AC 素材，实际做了什么都忘了」。排查发现 `05_logs/dev_log/`（工程日志目录）4-15 后停笔废弃，工程事实被 `raw/`（AC 素材）吸走。AC 素材写的是「怎么想的」（给教授看），不是「改了哪几个文件」（给 itsuki 自己看）。拍板复活 dev_log，**只记工程事实，跟 AC 素材物理分开**。

**时机**：每次收尾必跑，跟 §5.5.1 AC dump 平行（两条线分开写，互不重叠）。

**触发**：每次收尾必跑，不靠关键词。本会话有任何代码 / 文档改动 → 写工程简报；纯讨论零改动 → 输出「本会话无工程改动，§5.5.16 跳过」一行。

**写到哪**：`05_logs/dev_log/YYYY-MM-DD.md`（CC 直接写，见 §6.3）。同会话延续 append；新主题用 `YYYY-MM-DD_主题.md`。

**格式 + 内容规范 → 看 `05_logs/dev_log/_README_工程简报格式.md`**（含模板 + 数据来源 + 跟 AC 分开的铁律）。

**核心铁律（跟 §5.5.1 AC dump 的分工）**：

| | dev_log 工程简报（本节） | raw/ AC dump（§5.5.1） |
|---|---|---|
| 写给谁 | itsuki 自己 | 给教授看的素材池 |
| 内容 | 改了哪些文件 / 修了什么 bug / 跑了什么验证 / commit 清单 / 没做完 | 怎么想的 / 为什么选 / 学到什么 / 模式分析 / 原话引用 |
| 数据来源 | 尽量从 `git log` + `git diff --name-only` 自动抓 | CC 读会话上下文叙事 |

**反模式**：

- ❌ 工程简报里写「学到什么 / AC 价值 / 模式分析」（那是 raw/ 的活 —— 上次 dev_log 就因为混 AC 内容跟 raw 重叠然后废弃）
- ❌ CC 手写长篇叙事（上次废弃就因为手写太累 —— 尽量 git 自动抓，便宜到不会再死）
- ❌ 跳过工程简报只写 AC dump（itsuki 6-08 拍板原因就是「光收集 AC，实际做了什么忘了」）

---

## §6 AC 文件家族 — CC 权限速查 ⭐ 关键边界

> 完整文件清单 + 每个文件干嘛 + 状态 + AC 价值 → 看 `.claude/skills/project-overview/SKILL.md` §1.4 + §6。本节**只列 CC 权限边界**。

### 6.1 CC 权限分级

**A 类 — CC 直接 Edit 写（记录类，不让 itsuki 做粘贴手活，2026-05-26 itsuki 怒怼「他妈的别让我来做」拍板反转原「永不直写」规则）**：

- `05_logs/decisions/decision_log.md` — 重大决策正本，按文件顶部模板格式 Edit 到「## 决策记录(倒序)」段后
- `05_logs/decisions/decision_log_全部.md` — 所有决策流水（普通 + 重大短条目），Edit 到顶部
- `05_logs/project_evolution.md` — 按文件现有格式追加
- `05_logs/learning_path.md` — 按文件现有格式追加

**B 类 — CC 永远不直写正文（itsuki 主体性内容 / AC 叙事文档）**：

- `00_admin/AC_志望動機_素材.md`（itsuki 自填 8 个 Q）
- `00_admin/面试准备_索引.md`（题目清单 OK，回答永不写）
- `vX.Y.Z_AC叙事.md`（itsuki 自写；历史 v0.3-v0.8 的 6 个 CC 起草版保持不动）
- iCloud `05_产出/`（**永不写**）

### 6.2 CC 改前先告知 itsuki

- `00_admin/原创设计_语音播报防作弊.md`（重要 AC 素材）
- `05_logs/problem_solving/`（itsuki 自整理）
- iCloud `状态快照.md`

### 6.3 CC 自由直写

- `<raw池>/YYYY-MM-DD.md`（每日 AC 素材 dump）
- `05_logs/dev_log/YYYY-MM-DD.md`（每日工程简报，2026-06-08 复活，CC 收尾自动生成，itsuki 不用碰，见 §5.5.16）。注：`dev_log/` 4-15 前的旧文件是死历史，冻着没人动
- `00_admin/Batch3_itsuki手笔素材指引.md`（CC 起草过的 draft，等 itsuki 粘贴）

### 6.4 iCloud 当场授权才写

- iCloud `03_素材_候选/`（第 2 层）
- iCloud `04_素材_成品/`（第 3 层）
- iCloud `99_archive/` 新子目录（归档用途）

### 6.5 素材流向

```
<raw池>/（CC 写）→ 月末挑 #AC候选 → iCloud 03_候选 → 04_成品 → 05_产出（CC 永不）

Batch3 / CC draft → itsuki 粘贴 → 05_logs/decision_log / project_evolution / learning_path

vX.Y.Z_AC叙事.md ← 版本 bump 后 itsuki 自己写
```

---

## §7 禁止事项（CC 永远不做）

- ❌ 润色 itsuki 原话（§3.7）
- ❌ 替 itsuki 判断"这条不该记" — 除非是 5 级（明显无关）
- ❌ 主动写自我推荐书 / 志望理由书内容（这些归 itsuki）
- ❌ 把 AI 推荐的方案直接写成"itsuki 设计的 XX" — 必须按 §0.1 区分
- ❌ 在当日日志里加"事后才知道的信息"（保持时间线纯粹）
- ❌ 用术语堆砌让总结看起来专业（§5.5.2 总结只能用大白话）
- ❌ 看到"日常 bug fix"就跳过 — 日常工作也是素材（§0.2）
- ❌ 写 §6.1 / §6.4 列的文件正文 — 这是 CC 永久禁区

---

## §7.5 收尾完成强制自查清单（2026-05-13 凌晨 itsuki 拍板加）

> **背景**：5-12 跨夜延续到 5-13 凌晨，CC 跑完 §5.5 8 节后报告"收尾完成"，但 itsuki 反问"流程都做了吗" → CC 自查发现 4 项漏（file-linkage 漂移没修 / 第 4-5 项默默跳过没说明 / 第 6 项做了但没复述 / 第 7 项 WIP 漏 5-13 段）。
>
> **根本原因** = CC 判断"哪些 N/A 可跳过"而不是机械逐条核对。
>
> 跟 cc-comm-rules v0.4.0 方向调整同根原因 — 不让 CC 判断，**强制机械执行**。

### §7.5.1 收尾报告必须包含的 14 项逐条核对表

CC 跑完 §5.5 后，**最终收尾报告必须**包含以下 14 项逐条状态，**不允许默默跳过任何一项**：

| # | 项 | 状态字段（3 选 1） |
|---|---|---|
| 1 | **AC 素材**（详细叙事写 `<raw池>/当天.md` + 信号条写 iCloud 共用池自动捕获）| ✅ 写了 + 路径 / ⏸ 本会话无素材 + 显式说明 / ❌ 漏 |
| 2 | **dev_log 工程简报**（写 `05_logs/dev_log/当天.md`：新增/修改了什么功能、新增/删除了哪些文件、修了什么 bug、跑了什么验证、commit 清单、本会话新增决策一览（一句话 + 指向决策日志），详见 §5.5.16）| ✅ 写了 + 路径 / ⏸ 无工程改动 + 显式说明 / ❌ 漏 |
| 3 | **决策日志**（本会话**所有决策**都记，不只重大：普通决策 3-5 行短条目进 `05_logs/decisions/decision_log_全部.md`；命中 6 类**重大**标准的，短条目照写、**另**写完整条目进 `05_logs/decisions/decision_log.md`，详见 §5.5.15）| ✅ 写了 + 条数 / ⏸ 本会话无决策 + 显式说明 / ❌ 漏 |
| 4 | **learning_path / project_evolution**（学了任何新东西 → 追加 `05_logs/learning_path.md`；项目有任何演化（功能加/砍/改、方向变）→ 追加 `05_logs/project_evolution.md`。**不设重大门槛，有就写**）| ✅ 写了 + 文件名 / ⏸ 无新学习且无演化 + 显式说明 / ❌ 漏 |
| 5 | **WIP / TODO 双向刷新**（详见 §7.5.7 — 强制 Edit TODO.md 落地，不允许只写报告）| ✅ 必须列行号 / ⏸ 极少情况：本会话零任务零产出 |
| 6 | memory 维护（MEMORY.md 索引 + feedback 写入）| ✅ 检查 + 摘要 / ⏸ 不需要写 / ❌ 漏 |
| 7 | **文件联动检查 — 双重保险 + 两个并行真值更新**（① `sync-rules.sh` 全部规则脚本扫 ② CC 自己 Read `file-linkage/SKILL.md` 把本会话改过的文件人工对照规则表过一遍——脚本管正则匹配，CC 管语义判断 ③ 本会话改了 backend → `bash bin/export_openapi.sh` 重导接口总表，`git diff` 看字段层变化、对照 iOS / Android / 老师网页（itsuki 6-11 拍板：跟联动检查并行的查字段手段）④ `python3 bin/generate_file_inventory.py` 刷新文件清单 → 输出的「未批注」新文件 CC 读一眼补一句进 `00_admin/文件批注.tsv` 再跑一遍。**设计文档同步**（system_features + 5 端 DESIGN_LOG）已含在规则 3 / 14-18 里，不再单列；project-overview 字典同步也并在本项）| ✅ 全做 + 报告 / ⏸ 部分不触发（如没改 backend）+ 理由 / ❌ 漏 |
| 8 | git 状态确认（工作树 + 提交历史 + 别会话残留）| ✅ 看了 + 摘要 / ❌ 漏 + 理由 |
| 9 | 跨仓库 / 跨工作区同步（VPS / fork / **AC 状态快照** — 本会话有 AC 侧重大进展就刷新 iCloud `状态快照.md` 的 🤖 段，见 §5.5.12）| ✅ 同步 + 哪些 / ⏸ 不触发 + 理由 / ❌ 漏 |
| 10 | **全局环境清单同步**（本会话动过 `~/.claude/` 下 skill / hook / `settings.json` / 装新工具 → 同步 `~/.claude/我的环境.md` + `.html`）| ✅ 同步了 / ⏸ 没动全局环境 + 显式说明 / ❌ 漏 |
| 11 | **项目心智模型**（`00_admin/项目心智模型.md` — 三类触发 + 怎么更新见表下方「项 11 细则」）| ✅ 更新 + 哪节 / ⏸ 三类都不触发 + 显式说明 / ❌ 漏 |
| 12 | **daily-archive**（脚本 cp 本会话 jsonl 原文到 iCloud 共用池 `1_自动产出/对话原文/`，详见 §5.5.14）| ✅ 跑了 + 路径 / ❌ 漏 + 现在补 |
| 13 | **版本判定 + 当场 bump**（itsuki 6-11 拍板：版本结账并入收尾，不再攒着回头补标签。走 [[version-bump]] skill §2 决策树 — 该升当场升（改 bug = 补丁号 / feat 批次 = 次版本号，6-09 粒度规矩）+ 7 处联动同步；不该升也要报告理由（CC 否决权照旧）。**多会话叠加**：先看 `CHANGELOG.md` 顶部当前最新版，在它之上叠 — 先收尾的先叠，后收尾的看到新版接着叠）| ✅ 升到 vX.Y.Z + tag / ⏸ 决策树判不升 + 理由 / ❌ 漏 |
| 14 | **提交落地**（git commit，显式文件路径，按事分笔）| ✅ 提交 + 哈希 / ⏸ 没改仓库内文件 / ❌ 漏 |

**位置说明**：项 7 里的 project-overview 字典同步在 commit（项 14）之前做 = 字典校准跟当次结构改动**一起进 commit**，避免下次会话读到漂移版本。配套 hook `post-edit-project-overview-check.sh` 实时提醒 + 本项收尾必查 = 双层保险。

**项 11 细则 — 心智模型怎么更新**：心智模型 = 让 AI 开局懂项目的骨架，只装「几个月才变一次」的东西。收尾时拿本会话对照三类触发，命中任一就更新对应节：

1. **某端成熟度跳档 / 砍加大功能** → 改 §4 表对应行（成熟度档位 + 一句话定位一起改）
2. **大问题决了 / 新冒出大未决** → §6 加一条或移走一条；涉及 5 端契约的同步改 §3
3. **AI 认知缺口暴露** — 本会话 AI 因为不知道某个**项目级事实**而犯错 / 误判 / 被 itsuki 纠正 → 把这个事实写进心智模型（并入现有节或加小节），让下一个 AI 开局就知道。判断标准：「如果开局就知道 X，这个错就不会犯」且 X 是项目长期事实而非一次性细节。实例：6-11 AI 不知道 demo / 生产双轨设计，误判「demo 代码该删」→ 补 §4.1 每端隔离方式

写法铁律：写「现在是什么」不写历史过程；流水进度（做到哪 / 改了啥）归 WIP 绝不进心智模型；**膨胀红线 — 全文超 150 行就不许直接加新内容**：先删过期旧内容 / 把细节挪去 `PROJECT_GUIDE.md` 或对应设计文档（本文件只留一句指针）再加。分界标准：「开局不知道就会犯错」的骨架才配留在心智模型，深入了解 / 背景叙事归 PROJECT_GUIDE，两者不重叠；改完更新顶部「最后更新」一行。

### §7.5.2 状态字段写法规范

- **✅ 做了** — 必须给具体证据（文件路径 / 提交哈希 / 报告摘要）
- **⏸ 不触发** — 必须**显式说明**为什么不触发，**不允许默默跳过**
- **❌ 漏** — 必须说理由 + 是否补做（"现在补 / 记 TODO 后做"）

### §7.5.3 反模式

- ❌ 没跑第 N 项就报告"收尾完成"（默默跳过）
- ❌ 跑了但没在报告里说（itsuki 看不到 = 等于没做）
- ❌ 判断"N/A 不用做"就不显式说明（itsuki 不知道你跳了）
- ❌ 说"漏了"但没解决方案（不说"现在补"或"记 TODO"）

### §7.5.4 为什么这条规则存在

LLM 自觉性 = 工程不可靠组件（跟 cc-comm-rules v0.4.0 同根问题）。靠 CC 自己判断哪些 N/A 哪些漏 = 跳过。

**机制层修正**：强制每项都显式说状态 = 不让 CC 有"判断 N/A 跳过"的裁量空间。

### §7.5.5 触发场景

- 主触发：itsuki 说「收尾」/「总结今天」等 §0 触发词 → CC 跑完 §5.5 8 节后**必须**给 §7.5.1 14 项核对表
- 兜底：itsuki 反问"流程都做了吗" → CC 必须补出 §7.5.1 表

### §7.5.6 历史

- 2026-05-13 凌晨加 — 本会话 itsuki 反问"流程都做了吗"暴露 CC 漏 4 项后拍板。**这条规则是给所有未来会话用的**，不只本次。
- **2026-05-16 加项 8** — itsuki 拍板「要保证每次文件和结构做出改动，都会更新 project-overview / 最好加到收尾步骤里」。配套 hook `post-edit-project-overview-check.sh`（5-13 加 — Write/Edit 实时提醒）做不到强制（hook 是 warn 模式），所以加到 §7.5 强制清单兜底。项 8 → 9（commit）顺序调整。同时 SC26 + Tango 同步加。
- **2026-05-24 加 §7.5.7** — itsuki 怒怼「他妈的这些你记录到 todo 里面了没」暴露 CC 把「留 itsuki 拍板」5 大决策只写在收尾报告里没 Edit TODO 落地。根因：项 7 当前规则只写「WIP 加新条目 + TODO 加待办」太宽松 — CC 容易理解成「写在报告里就算」。拍板细化项 7 = §7.5.7「双向刷新强制规则 — 必须真的 Edit TODO 不能只写报告」。
- **2026-05-25 加项 11**（全局环境清单同步）— itsuki 反问「我有个 hook 是 CC 加新功能后提醒去改我的环境.md，有在用到吗？」暴露 CC 建完 HOW_TO_TALK.md 后**没主动**更新 `~/.claude/我的环境.md` — 违反全局 CLAUDE.md 「装新 skill / 写新 hook 后立刻更新清单」铁律。诊断 = 没 PostToolUse hook 注册（只能靠 CC 自觉，自觉性 = 工程不可靠组件）+ 收尾清单原本只到项 10 不查全局环境。拍板加项 11 = 收尾时强制扫本会话有没有动 `~/.claude/skills/` / `~/.claude/hooks/` / 装新工具 / 改 `~/.claude/settings.json` → 有动 → 必须同步 `我的环境.md` + `.html` + §11 历史日志 + 顶部"最后更新"行。SC26 / Tango 同步加（如这些项目也用 session-wrap skill）。
- **2026-05-29 加项 12**（项目心智模型同步）— 配合新建 `00_admin/项目心智模型.md`（AI 开局必读骨架，由 [[dmsd-startup]] §2 Step 3 每次开局自动读）。itsuki 拍板方案 A，解决「开新会话 AI 没有整个项目的样子、改前端却不知道后端写到哪」。收尾时若本会话改了某端状态 / 决了大问题 → 更新心智模型 §4 + §6，让下次会话开局读到的现状不漂移。

---

### §7.5.7 项 7「TODO 双向刷新」强制规则细化（2026-05-24 加）

> **核心铁律**：项 7 落地 = **真的 Edit TODO.md 文件**，不是「写在收尾报告里给 itsuki 看」。
>
> 收尾报告是临时给 itsuki 看的 chat 输出。**TODO.md 是真值文件**。两者不能混。

#### §7.5.7.1 强制 5 步机械执行（CC 收尾跑 §7.5.1 项 7 时按顺序跑）

**Step A — 列本会话所有任务**

从会话第一条 itsuki 消息扫到最后，列出 CC 接到的所有任务（用户指令 + CC 自己 propose 后做的 + 中途加的）。每条分类：

| 分类 | 含义 | 例子 |
|---|---|---|
| ✅ 完成 | 本次会话做完 + 验证通过 | 「修 .gitignore 加 7 类规则」已 Edit + commit |
| 🔧 未完成 | 还在做 / 半完成 / 阻塞 | 「修 system_features 引用废 repo」工作量大留下次 |
| 🎯 留拍板 | 大决策 / 选项 / 候选方案 | 「spec bump 版本号」选 a/b/c 等 itsuki 拍 |

**Step B — grep TODO.md 找现有对应条目**

```bash
# 每个 ✅ 完成的任务都查 TODO 现有条目
grep -nE "关键词1|关键词2" 00_admin/TODO.md
```

找到 → 准备 Edit 改成 ✅；找不到 → 该完成任务本来就没在 TODO（跳过）。

**Step C — 完成任务 Edit TODO 标 ✅**

| 旧（TODO 现状）| 新（Edit 后） |
|---|---|
| `- [ ] **修 .gitignore 漏覆盖** — 7 类规则` | `- [x] ~~**修 .gitignore 漏覆盖**~~ ✅ 2026-05-24 完成（commit \`abc1234\`，加 7 类规则）` |

**Step D — 未完成 + 留拍板 Edit TODO 加新条目**

定位插入段（按性质）：
- 漏洞相关 → `00_admin/系统bug专栏.md` 对应 A-/B-/C-/FC- 段
- 当下会话残留 → TODO §🔧「下次会话接续清单」
- 长尾治理 → TODO §🛠️ 对应字母段
- 等拍板大决策 → TODO §🎯「等 itsuki 拍板」段（没有就新建）

**新条目格式**（强制）：
```markdown
#### [本会话编号 / FC-XXX] [严重等级] [一句话描述]
- 位置: `file:line`（如有）
- 上下文: 本会话哪个动作触发 / 跟原条目什么关系
- 修法 / 选项: (a) X / (b) Y / (c) Z
- 决策: 2026-MM-DD 创建 / 待 itsuki 拍板
```

**Step E — 收尾报告项 7 引用真实 Edit**

报告项 7 不写「TODO 已加 N 条」这种含糊话。写：

> ✅ TODO 更新：
> - `TODO.md:113` 标 ✅ 2 条（§📁 文件加 project-overview 引用）
> - `TODO.md:115` 标 ✅ 3 条（§💾 commit + push）
> - `TODO.md:118` 新加 §🎯 5-22 修漏洞会话 D1-D5（5 条等拍板）

让 itsuki 能直接跳到 TODO 对应行号验证。

#### §7.5.7.2 不允许的反模式（5 条）

- ❌ **「报告写了就算」** — 「留你拍板 5 条 ... [列在报告里]」但 TODO 没 Edit
- ❌ **「跨段躲避」** — 「漏洞条目都在 系统bug专栏」当 TODO 不刷新理由（专栏 ≠ TODO，是两个不同档案）
- ❌ **「单向刷新」** — 加了新条目但没标 ✅ 已完成 / 或反过来
- ❌ **「⏸ 不触发」泛用** — 项 7 ⏸ 只有 1 种合法情况：**本会话零任务零产出**（极少，几乎不该用）
- ❌ **「等下次会话补」** — 收尾不补 TODO 等下次会话忘了 → 永久丢失（itsuki 5-22 凌晨原话「你他妈的记录到 TODO 里面了没」就是这场景）

#### §7.5.7.3 兜底触发（如果 §7.5.7.1 没跑）

itsuki 反问 / 表达不爽（如「你他妈的记录到 todo 里面了没」/ 「TODO 加了没」/ 「下次会话能看到吗」）→ CC 当场跑 Step A-E 补做，不允许「下次补 / 报告里有」类回避。

#### §7.5.7.4 出处

- **2026-05-24 itsuki 怒怼** — 5-22 修漏洞会话收尾报告列「留 itsuki 拍板 5 大决策」但只写报告没 Edit TODO。itsuki 原话「他妈的这些你记录到 todo 里面了没」+ 「要我的收尾 skill 每次都能把剩下没做完的放到 todo 里面，然后把做完的从 todo 里面去掉。要能做到这样，帮我改好」

---

## §7.6 收尾终判 — 「可以关会话」/「不能关」（2026-05-28 itsuki 拍板加）

> **背景**：itsuki 2026-05-28 拍板 — 他说「收尾」之后就想直接关掉会话，不用再回一次。所以收尾报告**最后一行必须是明确终判**，让他一眼知道能不能关。

**铁律**：CC 跑完 §7.5.1 全部 14 项核对后，收尾报告**最末尾必须**输出二选一终判，不允许含糊：

### §7.6.1 输出「✅ 可以关闭这个会话了」的条件

同时满足：
1. 14 项核对全部 ✅（做了）或 ⏸（有显式理由不触发），**没有任何 ❌ 漏项**
2. 没有「本次会话接到但没做完」的任务（🔧 未完成）
3. 没有等 itsuki 拍板的**大决策**（方向 / 选型 / 选项 a/b/c）

→ 满足就在报告最后单独一行写：**「✅ 可以关闭这个会话了」**

### §7.6.2 输出「❌ 不能关」的条件

只要满足任一条：
- 14 项里有 ❌ 漏项还没补
- 有本次会话没做完的任务（🔧）
- 有等 itsuki 拍板的大决策

→ 报告最后写：**「❌ 不能关，还有 N 件事没解决：」** + 逐条列 + 每条说清「要你做什么」

### §7.6.3 push 未决不算阻塞

未 push 的 commit **不阻塞关闭** — 本地 commit 安全，下次开会话再 push 也行。

- 有未 push commit → 报告里提一句「有 N 条没 push，现在告诉我或下次会话再 push 都行」
- 这一句之后仍可以给「✅ 可以关闭」终判（前提是其他 3 条满足）

### §7.6.4 反模式

- ❌ 收尾报告结尾含糊（「大致完成」/「应该没问题」/「基本都做了」）— 必须明确二选一
- ❌ 有阻塞却说「可以关」（itsuki 关了会话 = 阻塞永久丢失）
- ❌ 把「未 push」当成「不能关」理由（push 留本地不影响，见 §7.6.3）

### §7.6.5 终判前必须给「本会话汇总」（2026-05-30 itsuki 拍板加）

终判那一行之前，CC 必须先给一段**本会话做了什么的简单汇总** — 让 itsuki 关会话前一眼回顾今天干了啥。

- 3-6 条，每条一句话：改了什么 / 决了什么 / 学了什么
- 大白话，文件名后带一句它是干嘛的
- 不堆细节（细节在 raw / commit history 里）

### §7.6.6 CC 权限内的事自己做完，不许抛回 itsuki（2026-05-30 itsuki 拍板加）

itsuki 说「收尾」后，**不希望再看到任何「要不要我做 X / 你要不要 Y」的提问**。

- CC 权限内能做的（本地 commit / WIP 刷新 / 环境清单补登 / raw 落地）→ **直接做掉**，不问
- 只有**真正需要 itsuki 拍板的大决策**（方向 / 选型 / a-b-c）或**必须他亲手操作**的事 → 才列进「❌ 不能关」逐条说清
- 反模式（itsuki 2026-05-30 明确怒怼）：既说「✅ 可以关」又反手问「要不要我 commit / 要不要记 WIP」= 自相矛盾 + 甩活

机制依据：memory `feedback_no_handoff_work_back_to_itsuki` + `feedback_overruled_rule_means_update_rule` + `feedback_mechanism_over_self_discipline`。

### §7.6.7 收尾唯一红线 = 不 push，其余全部 CC 自主直接做（2026-05-30 itsuki 二次拍板）

itsuki 2026-05-30 再次怒怼：收尾时 CC 还在反问「要不要 dump AC / 要不要记 memory / 这个文档 /goal 会读吗」。把红线划死：

**收尾时 CC 唯一不能自作主张的只有一件事：`git push`。** 除 push 外，全部自主直接做、不问：

- commit（本地）/ 改任何文件 / raw 落地 / memory 写入 / TODO 刷新 / 文档修正 / 改 skill / 改 hook —— 一律直接做掉
- **不要因为「怕改错 / 怕被别的会话覆盖 / 怕 itsuki 不想要」而反问或跳过** — itsuki 原话「**后期再改再修都无所谓**」，先做了再说
- 收尾报告里**一个「要不要我…」的问号都不该有**（除非是 §7.6.2「❌ 不能关」里必须 itsuki 拍板的大决策 —— 那也是陈述「要你做 X」，不是「要不要我做 X」）

反模式（itsuki 2026-05-30 第二次明确）：「要不要我 dump / 要不要记 memory / 这文档 /goal 会读吗 → 那我自己重做」这种**先抛问题再自己答**的来回 = 浪费 itsuki 一次回复，等于没收尾。直接做完，最后只留一句可否关闭。

---

## §7.7 收尾执行纪律 — 防工具调用解析失败（2026-05-28 itsuki 拍板加）

> **背景**：2026-05-28 一次收尾里 CC 工具调用连续多次「could not be parsed」（解析失败）。根因不是流程 bug，是**会话上下文太满**（多次读超长文件 + 每轮 hook 注入大量内容）后，模型生成「带大段中文的长工具调用」时语法会坏。

**铁律 — 收尾时按这 3 条执行**：
1. **一轮只发一个工具调用** — 不在一条回复里批量发多个 Edit / Write，发完看结果再发下一个
2. **不读超长文件全文** — WIP / inbox 等用 offset/limit 局部读或 grep 定位，不整文件读
3. **工具调用参数尽量短** — 大段内容分多次小步写入，不一次塞几百行中文

**兜底**：连续 2 次工具调用解析失败 → 停下，提示 itsuki「本会话上下文已满，建议 commit 后开新会话继续收尾」，不硬撑。

---

## §8 失败模式（CC 不要犯）

- ❌ 只记决策事实 + 原话，缺过程（和决策日志没区别）
- ❌ 只写结论不写权衡（AC 素材要过程不要结论）
- ❌ 完全不猜全部留白让 itsuki 自己补（§3.8 零推测 < 标记推测）
- ❌ 因为"看起来不重要"而跳过记录（违反 §0.2）
- ❌ 不主动追问就放过潜在素材（违反 §5.3）
- ❌ 把 CC 写的代码直接归功给 itsuki 不区分（违反 §0.1）
- ❌ 模板套得太死，每条都填 7 字段（§3.3 推荐字段，必填只 2 项）

---

## §9 元规则

- itsuki 是主角，CC 是辅助
- 识别触发时**先看 itsuki 自己有没有意识到**，再追问
- 碎片 dump **默认主动写**（§5.2），高敏感场景例外
- 发现 itsuki 没意识到的认知改变（特别是模式 5），**主动挖出来记**
- 当 CC 不确定要不要记时，**默认记** — 筛选是月度回顾的事

---

## §10 一句话总结

> **记录阶段：宁多勿少，最大化捕捉。  
> 整理阶段（月度回顾）：精挑细选，提炼成品。**

不要在记录阶段过滤。任何 1% 可能用到的素材都先记下，最后再筛。

itsuki 的特殊优势是「通过 AI 协作的成长路径」 — 这是他独有的 story。**模式 5（认知改变型素材）是这个 story 的主线**，要重点挖掘。

---

## §11 闪念不归 CC 管

itsuki 临时灵感 / 闪念 / 还没成形的想法 → 走 iCloud `01_闪念/` 体系（详见 iCloud 总章 `AC入试记录指南_v3.md`）。CC **不读 / 不写 iCloud 任何东西**（§6.1 / §6.4 例外授权除外）。
